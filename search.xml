<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Vue.js プロジェクトの構造</title>
    <url>/2021/11/12/vue-project/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<p>Vue.jsプロジェクトファイルの構造とVueファイルの構造をメモする。</p>
<h2 id="Vue-jsプロジェクトの構造"><a href="#Vue-jsプロジェクトの構造" class="headerlink" title="Vue.jsプロジェクトの構造"></a>Vue.jsプロジェクトの構造</h2><ul>
<li>src<ul>
<li>components: 各モジュールはここで定義する</li>
<li>main.js: 入り口のjs</li>
<li>router <ul>
<li>index.js: ルーター設定</li>
</ul>
</li>
<li>App.vue </li>
<li>assets: 画像などのリソース</li>
</ul>
</li>
<li>index.html: 画面入り口</li>
<li>static: 静的リソース</li>
<li>test</li>
<li>config<ul>
<li>index.js: 各種設定</li>
</ul>
</li>
<li>build</li>
<li>package.json</li>
<li>node_modules</li>
</ul>
<h2 id="Vueファイルの構造"><a href="#Vueファイルの構造" class="headerlink" title="Vueファイルの構造"></a>Vueファイルの構造</h2><p>Vueファイルは以下３つの部分がある。</p>
<p>[1]HTML<br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">template</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"app"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">img</span> <span class="attr">alt</span>=<span class="string">"Vue logo"</span> <span class="attr">src</span>=<span class="string">"./assets/logo.png"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">HelloWorld</span> <span class="attr">msg</span>=<span class="string">"Welcome to Your Vue.js App"</span>/&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">template</span>&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>[2]JavaScript<br><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">&lt;script&gt;</span><br><span class="line"><span class="keyword">import</span> HelloWorld <span class="keyword">from</span> <span class="string">'./components/HelloWorld.vue'</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> &#123;</span><br><span class="line">  name: <span class="string">'App'</span>,</span><br><span class="line">  components: &#123;</span><br><span class="line">    HelloWorld</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">&lt;<span class="regexp">/script&gt;</span></span><br></pre></td></tr></table></figure></p>
<p>[3]css<br><figure class="highlight"><table><tr><td class="code"><pre><span class="line">&lt;<span class="selector-tag">style</span>&gt;</span><br><span class="line"><span class="selector-id">#app</span> &#123;</span><br><span class="line">  <span class="attribute">font-family</span>: Avenir, Helvetica, Arial, sans-serif;</span><br><span class="line">  <span class="attribute">-webkit-font-smoothing</span>: antialiased;</span><br><span class="line">  <span class="attribute">-moz-osx-font-smoothing</span>: grayscale;</span><br><span class="line">  <span class="attribute">text-align</span>: center;</span><br><span class="line">  <span class="attribute">color</span>: <span class="number">#2c3e50</span>;</span><br><span class="line">  <span class="attribute">margin-top</span>: <span class="number">60px</span>;</span><br><span class="line">&#125;</span><br><span class="line">&lt;/style&gt;</span><br></pre></td></tr></table></figure></p>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
  </entry>
  <entry>
    <title>JUnit5から使える @ParameterizedTest</title>
    <url>/2021/10/20/parameterizedtest/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<p><code>@ParameterizedTest</code>は指定のテストメソッドに違う引数を順番に渡して処理することで、複数のテストを実現する方法。<br>JUnit5の新しい機能として、使うとき以下のパッケージを依存関係に追加する必要がある。<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">org.junit.jupiter:junit-jupiter-params</span><br></pre></td></tr></table></figure></p>
<h2 id="使ってみる"><a href="#使ってみる" class="headerlink" title="使ってみる"></a>使ってみる</h2><p><code>@ParameterizedTest</code>でパラメーター指定のテストケースを声明する。そして<code>@ValueSource</code>を利用してパラメーターを指定する（整数:1,2,3）。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@ParameterizedTest</span></span><br><span class="line"><span class="meta">@ValueSource</span>(ints = &#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>&#125;)</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">test</span><span class="params">(<span class="keyword">int</span> i)</span> </span>&#123;</span><br><span class="line">    System.out.println(i);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>これで１つのメソッドでテストを3回実行した。<br><code>@ParameterizedTest</code>は<code>org.junit.jupiter.params.provider</code>パッケージ下のインタフェースと一緒に使う必要がある。</p>
<h2 id="DisplayName-of-ParameterizedTest"><a href="#DisplayName-of-ParameterizedTest" class="headerlink" title="DisplayName of ParameterizedTest"></a>DisplayName of ParameterizedTest</h2><p><code>@ParameterizedTest</code>はパラメーターごとにテストケースを命名できる。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@ParameterizedTest</span>(name = <span class="string">"input is &#123;0&#125; &#123;1&#125; &#123;2&#125;"</span>)</span><br><span class="line"><span class="meta">@CsvSource</span>(value = &#123;<span class="string">"1,a,true"</span>&#125;)</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">test</span><span class="params">(<span class="keyword">int</span> a, String b, <span class="keyword">boolean</span> c)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>使えるplaceholderは以下となる。</p>
<ul>
<li><code>{index}</code> テストケースの番号</li>
<li><code>{0~n}</code> 指定番号の引数の値（0から）</li>
<li><code>{arguments}</code> 全ても引数</li>
<li><code>{displayName}</code> <code>@DisplayName</code>で指定する内容、<code>@DisplayName</code>がない場合はテストメソッドの名前</li>
</ul>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
  </entry>
  <entry>
    <title>InjectMocksのインジェクト方法</title>
    <url>/2021/08/04/injectMocks/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<p>テスト対象のインスタンスに <code>@InjectMocks</code>をつけると、対象インスタンスのフィルドに<code>@Mock</code>されたインスタンスを差し込むことができる。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@InjectMocks</span></span><br><span class="line">Manager manager;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Mock</span></span><br><span class="line">Name name; <span class="comment">// manager.nameはモックされる</span></span><br></pre></td></tr></table></figure>
<h2 id="実現方法"><a href="#実現方法" class="headerlink" title="実現方法"></a>実現方法</h2><p><code>InjectMocks</code>のJavadocを読むと、フィルドのinject方法は3つあります。  </p>
<ol>
<li><p>Constructor injection;<br>1つ目はコンストラクターを利用してモックインスタンスを設定する。この方法はデフォルトで引数の数が一番多いコンストラクターを利用する。<br>要注意なことは、必要なモックインスタンスが見つからない場合、その引数は<code>null</code>となる。<br>引数にモックできないタイプ（基本型）がある場合、デフォルトのコンストラクターで<code>InjectMocks</code>のインスタンスを作って別の方法でInjectを実現する。</p>
</li>
<li><p>Property setter injection;<br>2つ目はとてもわかりやすくて、フィルドの<code>setter</code>メソッドを利用して、モックインスタンスを差し込む。<br>該当の<code>setter</code>メソッドを探すとき、まずフィルドのタイプを利用する。同じタイプのフィルドが存在する場合、モックインスタンスの名前を利用する。<br>という理由で、モックインスタンスを定義するとき、できる限り<code>Inject</code>対象のフィルドと同じ名前にする方がいい。<br><code>Constructor Injection</code>でデフォルトコンストラクターを利用する場合、モックインスタンスの差し込むはこの方法で実現される。</p>
</li>
</ol>
<ol>
<li>Field injection;<br>フィルドの<code>setter</code>がない場合、直接設定で<code>Inject</code>を実現する。ルールは<code>Property setter injection</code>と同じ。</li>
</ol>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
  </entry>
  <entry>
    <title>Mockito使い始めて見る</title>
    <url>/2021/07/21/junit-1/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<p>Mockitoはクラスの偽のオブジェクトを作ってくれます。</p>
<p><code>when().thenReturn()</code>を利用して、<code>public</code>メソッドの戻る値を指定できます。<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">add_with_when</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    List&lt;String&gt; mockedList = mock(List.class);</span><br><span class="line">    when(mockedList.size()).thenReturn(<span class="number">10</span>);</span><br><span class="line">    assertThat(mockedList.size(), is(<span class="number">10</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>ここは1つ注意しなければならないことがあります。モックで作成したオブジェクトの全てのメソッドが偽物になります。下記の<code>add()</code>はモックされたため、リストに追加する処理はありません。<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">add_without_when</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    List&lt;String&gt; mockedList = mock(List.class);</span><br><span class="line">    mockedList.add(<span class="string">"A"</span>);</span><br><span class="line">    mockedList.add(<span class="string">"B"</span>);</span><br><span class="line">    mockedList.add(<span class="string">"C"</span>);</span><br><span class="line">    assertThat(mockedList.size(), is(<span class="number">0</span>));</span><br></pre></td></tr></table></figure></p>
<p><code>Answers.CALL_REAL_METHODS</code>を利用すれば、本物のメソッドを呼べます。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">call_real_method</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Employee employee = mock(Employee.class, Answers.CALLS_REAL_METHODS);</span><br><span class="line">    employee.setName(<span class="keyword">new</span> Name(<span class="string">"Alice"</span>));</span><br><span class="line">    assertThat(employee.getName(), is(<span class="string">"Alice"</span>));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">call_real_method_with_when</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Employee employee = mock(Employee.class, Answers.CALLS_REAL_METHODS);</span><br><span class="line">    employee.setName(<span class="keyword">new</span> Name(<span class="string">"Alice"</span>));</span><br><span class="line">    when(employee.getName()).thenReturn(<span class="keyword">new</span> Name(<span class="string">"Bob"</span>));</span><br><span class="line">    assertThat(employee.getName(), is(<span class="string">"Bob"</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>Mockito</tag>
        <tag>JUnit</tag>
      </tags>
  </entry>
  <entry>
    <title>web.xml 的Servlet配置</title>
    <url>/2021/04/22/servlet-2-xml/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<p><a href="https://www.cnblogs.com/AlanWilliamWalker/p/11128688.html" target="_blank" rel="noopener">web.xml中servlet配置及其含义</a></p>
<h2 id="什么是-web-xml"><a href="#什么是-web-xml" class="headerlink" title="什么是 web.xml"></a>什么是 web.xml</h2><p>web.xml是Java工程中用来设置初始化配置信息的文件。xml的书写规则是有Schema文件来定义的。</p>
<p>web.xml使用的schema一般是Sun定义的，标注在标签<code>&lt;web-app&gt;</code>中。  </p>
<h2 id="web-xml中设置Servlet的常用标签"><a href="#web-xml中设置Servlet的常用标签" class="headerlink" title="web.xml中设置Servlet的常用标签"></a>web.xml中设置Servlet的常用标签</h2><h3 id="【1】servlet"><a href="#【1】servlet" class="headerlink" title="【1】servlet"></a>【1】servlet</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- servlet的配置 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">servlet</span>&gt;</span></span><br><span class="line">  <span class="comment">&lt;!-- 定义servlet的内部名称 --&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">servlet-name</span>&gt;</span><span class="tag">&lt;/<span class="name">servlet-name</span>&gt;</span></span><br><span class="line">  <span class="comment">&lt;!-- servlet的类名 此处为全路径--&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">servlet-class</span>&gt;</span><span class="tag">&lt;/<span class="name">servlet-class</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">servlet</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- servlet的映射配置 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">servlet-mapping</span>&gt;</span></span><br><span class="line">  <span class="comment">&lt;!-- 对应上面定义的servlet内部名称 --&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">servlet-name</span>&gt;</span><span class="tag">&lt;/<span class="name">servlet-name</span>&gt;</span></span><br><span class="line">  <span class="comment">&lt;!-- servlet的映射路径 --&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">url-pattern</span>&gt;</span><span class="tag">&lt;/<span class="name">url-pattern</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">servlet-maping</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>配置servlet标签需要配置两个，一个是servlet本身，另一个servlet-mapping是servlet的映射信息。<br>servlet的servlet-name可以自定义，一般为servlet的类名，而servlet-class则是servlet类的全路径。<br>servlet-mapping的servlet-name是对应的servlet名，url-pattern则是我们输入到浏览器地址栏中的用来调用servlet的url。</p>
<div class="table-container">
<table>
<thead>
<tr>
<th style="text-align:left"></th>
<th>url-pattern</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">精确匹配</td>
<td>/servlet</td>
</tr>
<tr>
<td style="text-align:left">模糊匹配</td>
<td>/*</td>
</tr>
<tr>
<td style="text-align:left">模糊匹配</td>
<td>/path/*</td>
</tr>
<tr>
<td style="text-align:left">模糊匹配</td>
<td>*.后缀</td>
</tr>
</tbody>
</table>
</div>
<p>url-pattern除了上面的基本写法之外，还要注意几点：</p>
<ol>
<li><code>/</code> 或者 <code>*</code>, url不能直接用servlet名开头。</li>
<li>同时使用两种模糊匹配是非法路径</li>
<li>匹配优先级：精确匹配最优先，后缀匹配最后</li>
</ol>
<h3 id="【2】初始参数"><a href="#【2】初始参数" class="headerlink" title="【2】初始参数"></a>【2】初始参数</h3><p>可以设置两种参数用来初始化servlet，一种是init-param，一种时context-param。<br>关于两种参数的区别：<a href="https://stackoverflow.com/questions/28392888/init-param-and-context-param" target="_blank" rel="noopener">init-param and context-param</a>  </p>
<p>init-param是给特定servlet设置的参数，所以写在<code>&lt;servlet&gt;</code>下面</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">servlet</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">servlet-name</span>&gt;</span><span class="tag">&lt;/<span class="name">servlet-name</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">servlet-class</span>&gt;</span><span class="tag">&lt;/<span class="name">servlet-class</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">init-param</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">param-name</span>&gt;</span>userName<span class="tag">&lt;/<span class="name">param-name</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">param-value</span>&gt;</span>Alice<span class="tag">&lt;/<span class="name">param-value</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">init-param</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">servlet</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>使用的时候通过<code>getServletConfig().getInitParamter(&quot;userName&quot;)</code>来调用。</p>
<p>context-param是为webApp全局准备的参数，直接定义在<code>&lt;web-app&gt;</code>标签下，通过<code>getServletContext().getInitParamter(&quot;userName&quot;)</code>来调用</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">web-app</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">context-param</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">param-name</span>&gt;</span>userName<span class="tag">&lt;/<span class="name">param-name</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">param-value</span>&gt;</span>Alice<span class="tag">&lt;/<span class="name">param-value</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">context-param</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">web-app</span>&gt;</span></span><br></pre></td></tr></table></figure>
<h3 id="【3】特殊标签"><a href="#【3】特殊标签" class="headerlink" title="【3】特殊标签"></a>【3】特殊标签</h3><ol>
<li>错误处理<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">error-page</span>&gt;</span> </span><br><span class="line">    <span class="tag">&lt;<span class="name">error-code</span>&gt;</span>404<span class="tag">&lt;/<span class="name">error-code</span>&gt;</span> </span><br><span class="line">    <span class="tag">&lt;<span class="name">location</span>&gt;</span>/error404.jsp<span class="tag">&lt;/<span class="name">location</span>&gt;</span> </span><br><span class="line"><span class="tag">&lt;/<span class="name">error-page</span>&gt;</span> </span><br><span class="line"></span><br><span class="line">...</span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">error-page</span>&gt;</span> </span><br><span class="line">    <span class="tag">&lt;<span class="name">exception-type</span>&gt;</span>java.lang.Exception<span class="tag">&lt;<span class="name">exception-type</span>&gt;</span> </span><br><span class="line">    <span class="tag">&lt;<span class="name">location</span>&gt;</span>/exception.jsp<span class="tag">&lt;<span class="name">location</span>&gt;</span> </span><br><span class="line"><span class="tag">&lt;/<span class="name">error-page</span>&gt;</span></span><br></pre></td></tr></table></figure></li>
<li><p>过滤器</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">filter</span>&gt;</span> </span><br><span class="line">    <span class="tag">&lt;<span class="name">filter-name</span>&gt;</span> <span class="tag">&lt;/<span class="name">filter-name</span>&gt;</span> </span><br><span class="line">    <span class="tag">&lt;<span class="name">filter-class</span>&gt;</span> <span class="tag">&lt;/<span class="name">filter-class</span>&gt;</span> </span><br><span class="line"><span class="tag">&lt;/<span class="name">filter</span>&gt;</span> </span><br><span class="line"><span class="tag">&lt;<span class="name">filter-mapping</span>&gt;</span> </span><br><span class="line">    <span class="tag">&lt;<span class="name">filter-name</span>&gt;</span> <span class="tag">&lt;/<span class="name">filter-name</span>&gt;</span> </span><br><span class="line">    <span class="tag">&lt;<span class="name">url-pattern</span>&gt;</span> <span class="tag">&lt;/<span class="name">url-pattern</span>&gt;</span> </span><br><span class="line"><span class="tag">&lt;/<span class="name">filter-mapping</span>&gt;</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>监听器</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">listener</span>&gt;</span> </span><br><span class="line">  <span class="tag">&lt;<span class="name">listener-class</span>&gt;</span><span class="tag">&lt;/<span class="name">listener-class</span>&gt;</span> </span><br><span class="line"><span class="tag">&lt;/<span class="name">listener</span>&gt;</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>会话</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">session-config</span>&gt;</span> </span><br><span class="line">  <span class="comment">&lt;!-- 以分为单位 --&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">session-timeout</span>&gt;</span> <span class="tag">&lt;/<span class="name">session-timeout</span>&gt;</span> </span><br><span class="line"><span class="tag">&lt;/<span class="name">session-config</span>&gt;</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>欢迎页</p>
</li>
</ol>
<p>找不到index1，就会加载index2</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">welcome-file-list</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">welcome-file</span>&gt;</span>index1.jsp<span class="tag">&lt;/<span class="name">welcome-file</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">welcome-file</span>&gt;</span>index2.jsp<span class="tag">&lt;/<span class="name">welcome-file</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">welcome-file-list</span>&gt;</span></span><br></pre></td></tr></table></figure>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>Servlet 基础</title>
    <url>/2021/04/20/servlet-1/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<p>内容总结自网络。</p>
<h2 id="定义"><a href="#定义" class="headerlink" title="定义"></a>定义</h2><p>Servlet(Server Applet) 可以翻译为Java小服务程序或者服务连接器。是用Java编写的服务器端程序，具有独立于平台和协议的特性。主要的功能是交互式地浏览和生成数据，生成动态Web内容。</p>
<p>狭义的Servlet是指Java的一个接口，广义的Servlet是指任何实现了这个接口的类。从原理上讲Servlet可以响应任何类型的请求，但绝大多数情况侠Servlet只用来扩展基于HTTP协议的Web服务器。</p>
<h2 id="实现过程"><a href="#实现过程" class="headerlink" title="实现过程"></a>实现过程</h2><p>Servlet交互式地浏览和生成数据的过程为：</p>
<ol>
<li>客户端发送请求到服务器</li>
<li>服务器将请求信息发送至Servlet</li>
<li>Servlet生成响应内容并传给服务器。</li>
<li>服务器将响应返回客户端</li>
</ol>
<h2 id="生命周期"><a href="#生命周期" class="headerlink" title="生命周期"></a>生命周期</h2><ol>
<li>客户端请求Servlet</li>
<li>加载Servlet到内存</li>
<li>生成Servlet实例，调用init()初始化</li>
<li>server调用service()方法，将一个请求对象和一个响应对象传递给service()</li>
<li>service()调用资源处理请求，使用响应对象的方法将响应传回server，再传回到客户端</li>
<li>(一般是server关闭时)调用destroy()方法</li>
</ol>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>Java</tag>
        <tag>Servlet</tag>
      </tags>
  </entry>
  <entry>
    <title>Day 3 面向对象进阶</title>
    <url>/2020/12/07/day-3/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<p>3/100</p>
<h2 id="slots"><a href="#slots" class="headerlink" title="slots"></a><strong>slots</strong></h2><p>Python是动态语言，我们可以随时为对象绑定新的属性和方法。但是如果我们想限制类型的对象所能绑定的属性，则需要用到<code>__slots__</code>。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Person</span><span class="params">(object)</span>:</span></span><br><span class="line"></span><br><span class="line">    __slots__ = (<span class="string">'_name'</span>, <span class="string">'_age'</span>, <span class="string">'_gender'</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, name, age)</span>:</span></span><br><span class="line">        self._name = name</span><br><span class="line">        self._age = age</span><br><span class="line"></span><br><span class="line"><span class="meta">    @property</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">name</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> self._name</span><br><span class="line"></span><br><span class="line"><span class="meta">    @property</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">age</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> self._age</span><br><span class="line"></span><br><span class="line"><span class="meta">    @age.setter</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">age</span><span class="params">(self, age)</span>:</span></span><br><span class="line">        self._age = age</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">play</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> self._age &lt;= <span class="number">16</span>:</span><br><span class="line">            print(<span class="string">'play game'</span>)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            print(<span class="string">'work'</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">main</span><span class="params">()</span>:</span></span><br><span class="line">    person = Person(<span class="string">'Alice'</span>, <span class="number">20</span>)</span><br><span class="line">    person.play()</span><br><span class="line">    person._gender = <span class="string">'male'</span></span><br><span class="line">    <span class="comment"># Error</span></span><br><span class="line">    <span class="comment"># person._family = True</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">'__main__'</span>:</span><br><span class="line">    main()</span><br></pre></td></tr></table></figure>
<h2 id="特殊方法"><a href="#特殊方法" class="headerlink" title="特殊方法"></a>特殊方法</h2><ol>
<li>静态方法使用 <code>@staticmethod</code> 修饰</li>
<li>类方法使用 <code>@classmethod</code> 来修饰, 类方法的第一个参数必须是<code>cls</code>，表示当前类的对象</li>
</ol>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>100 days of code</tag>
      </tags>
  </entry>
  <entry>
    <title>Day2 面向对象相关</title>
    <url>/2020/12/07/day-2/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<p>2/100</p>
<h2 id="定义类"><a href="#定义类" class="headerlink" title="定义类"></a>定义类</h2><p>python使用关键字<code>class</code>来定义类，在类名后的括号里写父类。<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Student</span><span class="params">(object)</span>:</span></span><br><span class="line"></span><br><span class="line">  <span class="comment"># 相当于Java的构造体，类的field可以直接赋值，不用事先定义</span></span><br><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, name, age)</span>:</span></span><br><span class="line">      self.name = name </span><br><span class="line">      self.age = age</span><br><span class="line"></span><br><span class="line">  <span class="comment"># 参数self相当于Java中的this，在调用方法的时候不需要指定，单如果没有这个参数就无法使用类的field</span></span><br><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">study</span><span class="params">(self, course)</span>:</span></span><br><span class="line">      print(<span class="string">f'studing: <span class="subst">&#123;course&#125;</span>'</span>)</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">watch_movie</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> self.age &lt; <span class="number">18</span>:</span><br><span class="line">            print(<span class="string">'%s只能观看《熊出没》.'</span> % self.name)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="comment">#  这是原作者 [jackfrued] 的爱好</span></span><br><span class="line">            print(<span class="string">'%s正在观看岛国爱情大电影.'</span> % self.name)</span><br></pre></td></tr></table></figure><br><strong>可见性</strong></p>
<p>python并没有复杂而严格的可见性设置，虽然可以通过在方法和变量前加两个下划线来设置私有性，但这其实只是改变了访问规则而已。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Test</span>:</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, foo)</span>:</span></span><br><span class="line">        self.__foo = foo</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__bar</span><span class="params">(self)</span>:</span></span><br><span class="line">        print(self.__foo)</span><br><span class="line">        print(<span class="string">'__bar'</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">main</span><span class="params">()</span>:</span></span><br><span class="line">    test = Test(<span class="string">'hello'</span>)</span><br><span class="line">    <span class="comment"># AttributeError: 'Test' object has no attribute '__bar'</span></span><br><span class="line">    test.__bar()</span><br><span class="line">    <span class="comment"># AttributeError: 'Test' object has no attribute '__foo'</span></span><br><span class="line">    print(test.__foo)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 在前面添加 _Test 就可以访问了</span></span><br><span class="line">    test._Test__bar()</span><br><span class="line">    print(test._Test__foo)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    main()</span><br></pre></td></tr></table></figure>
<p>一般使用单下划线来表明方法或变量是受保护的，但这也只是一种隐含的意思而已。</p>
<p><code>@property</code>装饰器<br>可以使用@property来包装属性，这样属性将会被封装起来，使用@property装饰的方法可以作为getter来使用，想要使用setter则需要@xxx.setter来修饰。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Person</span><span class="params">(object)</span>:</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, name, age)</span>:</span></span><br><span class="line">        self._name = name¬</span><br><span class="line">        self._age = age¬</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 如果没有这个修饰，下面的person.name = 'Bob' 就不会报错</span></span><br><span class="line"><span class="meta">    @property</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">name</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> self._name</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">    @property</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">age</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> self._age</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">    @age.setter</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">age</span><span class="params">(self, age)</span>:</span></span><br><span class="line">        self._age = age</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">play</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> self.age &lt;= <span class="number">16</span>:</span><br><span class="line">            print(<span class="string">'plage game'</span>)</span><br><span class="line">        <span class="keyword">else</span>:¬</span><br><span class="line">            print(<span class="string">'work'</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">main</span><span class="params">()</span>:</span></span><br><span class="line">    person = Person(<span class="string">'Alcie'</span>, <span class="number">12</span>)</span><br><span class="line">    person.play()</span><br><span class="line">    person.age = <span class="number">22</span></span><br><span class="line">    person.play()</span><br><span class="line">    <span class="comment"># AttributeError: can't set attribute</span></span><br><span class="line">    person.name = <span class="string">'Bob'</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">'__main__'</span>:</span><br><span class="line">    main()</span><br></pre></td></tr></table></figure></div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>100 days of code</tag>
      </tags>
  </entry>
  <entry>
    <title>Day1 Python 基础</title>
    <url>/2020/12/06/day-1/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<p>1/100</p>
<h2 id="【1】基本元素"><a href="#【1】基本元素" class="headerlink" title="【1】基本元素"></a>【1】基本元素</h2><h3 id="常见数据类型"><a href="#常见数据类型" class="headerlink" title="常见数据类型"></a>常见数据类型</h3><ol>
<li>整形 python3开始整数不区分int和long，支持二，八，十六进制。</li>
<li>浮点型 可以理解成小数</li>
<li>字符串 单引号 双引号都可以，三个引号输入多行内容</li>
<li>布尔型 True False 开头大写</li>
</ol>
<blockquote>
<p>变量名使用<em>连接 <code>my_var_name</code><br>`_name</em><code>单下划线开头为受保护的实例属性</code><strong>name</strong>`  双下划线开头的为私有属性</p>
</blockquote>
<p>使用 <code>type()</code> 来确认类型，使用下面的方法来转换类型：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">int() <span class="comment">#转换成整数 </span></span><br><span class="line">float() <span class="comment">#转换成浮点数</span></span><br><span class="line">str() <span class="comment">#转换成字符串，可以是字符编码</span></span><br><span class="line">chr() <span class="comment">#字符串编码（整数）转换成字符串</span></span><br><span class="line">ord() <span class="comment">#活的字符串对应的编码（整数）</span></span><br></pre></td></tr></table></figure></p>
<h2 id="【2】逻辑语句"><a href="#【2】逻辑语句" class="headerlink" title="【2】逻辑语句"></a>【2】逻辑语句</h2><h3 id="运算符"><a href="#运算符" class="headerlink" title="运算符"></a>运算符</h3><p>除了常见的运算符外，python常用的还有：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">is</span> </span><br><span class="line"><span class="keyword">is</span> <span class="keyword">not</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">in</span> </span><br><span class="line"><span class="keyword">not</span> <span class="keyword">in</span></span><br></pre></td></tr></table></figure>
<h3 id="if分支"><a href="#if分支" class="headerlink" title="if分支"></a>if分支</h3><p>python的if分支跟函数，类一样都是使用缩进来表示代码的层次结构。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> name == <span class="string">'a'</span>:</span><br><span class="line">    print(<span class="string">'a'</span>)</span><br><span class="line"><span class="keyword">elif</span> name == <span class="string">'b'</span>:</span><br><span class="line">    print(<span class="string">'b'</span>)</span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">    print(<span class="string">'c'</span>)</span><br></pre></td></tr></table></figure>
<h3 id="循环结构"><a href="#循环结构" class="headerlink" title="循环结构"></a>循环结构</h3><p>for-in<br>python一般使用<code>for name in collection</code>的方式来写循环语句</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">sum = <span class="number">0</span></span><br><span class="line"><span class="keyword">for</span> x <span class="keyword">in</span> range(<span class="number">100</span>):</span><br><span class="line">    sum += x</span><br><span class="line">print(sum)</span><br></pre></td></tr></table></figure>
<h2 id="【3】函数"><a href="#【3】函数" class="headerlink" title="【3】函数"></a>【3】函数</h2><h3 id="函数定义和参数"><a href="#函数定义和参数" class="headerlink" title="函数定义和参数"></a>函数定义和参数</h3><p>定义函数的关键字是 <code>def</code><br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">foo</span><span class="params">(a)</span>:</span></span><br><span class="line">    print(a)</span><br></pre></td></tr></table></figure></p>
<p>python中函数的参数可以设置默认值，所以使用的时候可以忽略部分参数或者直接指定某些参数，所以python的函数不需要像Java一样重载</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">add</span><span class="params">(a=<span class="number">0</span>, b=<span class="number">0</span>, c=<span class="number">0</span>)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> a + b + c</span><br></pre></td></tr></table></figure>
<p>还可以定义可变参数来接收无法确定个数的参数：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">add</span><span class="params">(*args)</span>:</span></span><br><span class="line">    total = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> val <span class="keyword">in</span> args:</span><br><span class="line">        total += val</span><br><span class="line">    <span class="keyword">return</span> total</span><br></pre></td></tr></table></figure></p>
<h3 id="模块"><a href="#模块" class="headerlink" title="模块"></a>模块</h3><p>python一个文件就算一个模块，通过<code>import</code>来导入指定模块的内容。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># module1.py</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">foo</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">'hello, world'</span>)</span><br></pre></td></tr></table></figure>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># module2.py</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">foo</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">'hello, world'</span>)</span><br></pre></td></tr></table></figure>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># test</span></span><br><span class="line"><span class="keyword">import</span> module1 <span class="keyword">as</span> m1</span><br><span class="line"><span class="keyword">import</span> module2 <span class="keyword">as</span> m2</span><br><span class="line"></span><br><span class="line">m1.foo()</span><br><span class="line">m2.foo()</span><br></pre></td></tr></table></figure>
<p>还可以直接从模块导入具体的函数<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> module1 <span class="keyword">import</span> foo</span><br><span class="line"><span class="keyword">from</span> module2 <span class="keyword">import</span> foo</span><br><span class="line"></span><br><span class="line"><span class="comment">#  后导入的foo会覆盖之前导入的foo，所以这里使用的是module2的foo</span></span><br><span class="line">foo()</span><br></pre></td></tr></table></figure></p>
<h3 id="name-‘main‘"><a href="#name-‘main‘" class="headerlink" title="name == ‘main‘"></a><strong>name</strong> == ‘<strong>main</strong>‘</h3><p>模块中如果需要写入可执行语句，一般需要包装在 <code>__name__ == &#39;__main__&#39;</code> 中，这样除非直接运行该模块，这些可执行语句是不会被执行的<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">foo</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">'hello, world'</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">'__main__'</span>:</span><br><span class="line">    foo()</span><br></pre></td></tr></table></figure></p>
<p><code>__name__</code> 是python的一个隐藏属性：模块名。 被python解释器直接执行的模块名字叫 <code>__main__</code></p>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>100 days of code</tag>
      </tags>
  </entry>
  <entry>
    <title>PlantUML（一） 类图</title>
    <url>/2020/10/12/er-1/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<p>ER图笔记（一）</p>
<p><strong>ER图</strong> Entity Relationship Diagram</p>
<h3 id="Cardinality"><a href="#Cardinality" class="headerlink" title="Cardinality"></a>Cardinality</h3><p>在线的端点通过下列符号来指定数量关系<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">--  1</span><br><span class="line">--||  1 &amp;&amp; only 1</span><br><span class="line">--o|  0 or 1</span><br></pre></td></tr></table></figure></p>
<p><a href="https://www.plantuml.com/plantuml/svg/SzJDqKtaSjAjgKbIu7BMpQz91W00" class="gallery-item"><img src="https://www.plantuml.com/plantuml/svg/SzJDqKtaSjAjgKbIu7BMpQz91W00" /></a><br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">--&#123;  multi</span><br><span class="line">--|&#123;  1 or more</span><br><span class="line">--o&#123;  0 or more</span><br></pre></td></tr></table></figure><br><a href="https://www.plantuml.com/plantuml/svg/Sz6jJk5orQsfJkLoqyslJWC0" class="gallery-item"><img src="https://www.plantuml.com/plantuml/svg/Sz6jJk5orQsfJkLoqyslJWC0" /></a></p>
<h3 id="Direction"><a href="#Direction" class="headerlink" title="Direction"></a>Direction</h3><p>在线的中央插入下列关键字来指定方向<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">up, u</span><br><span class="line">down, d, do</span><br><span class="line">left, l, le</span><br><span class="line">right, r, ri</span><br></pre></td></tr></table></figure></p>
<p><a href="https://www.plantuml.com/plantuml/svg/SzGjrRKBBU1oqatHjNF9Byy30000" class="gallery-item"><img src="https://www.plantuml.com/plantuml/svg/SzGjrRKBBU1oqatHjNF9Byy30000" /></a><br><a href="https://www.plantuml.com/plantuml/svg/SzRDqRNpIKqhuNBHBTAr2yfCpoW10000" class="gallery-item"><img src="https://www.plantuml.com/plantuml/svg/SzRDqRNpIKqhuNBHBTAr2yfCpoW10000" /></a></p>
<h3 id="package-style"><a href="#package-style" class="headerlink" title="package style"></a>package style</h3><p>可以使用自带的模版来定义每个包<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">package foo1 &lt;&lt;Node&gt;&gt; &#123;</span><br><span class="line">  class Class1</span><br><span class="line">&#125;</span><br><span class="line">package foo2 &lt;&lt;Rectangle&gt;&gt; &#123;</span><br><span class="line">  class Class2</span><br><span class="line">&#125;</span><br><span class="line">package foo3 &lt;&lt;Folder&gt;&gt; &#123;</span><br><span class="line">  class Class3</span><br><span class="line">&#125;</span><br><span class="line">package foo4 &lt;&lt;Frame&gt;&gt; &#123;</span><br><span class="line">  class Class4</span><br><span class="line">&#125;</span><br><span class="line">package foo5 &lt;&lt;Cloud&gt;&gt; &#123;</span><br><span class="line">  class Class5</span><br><span class="line">&#125;</span><br><span class="line">package foo6 &lt;&lt;Database&gt;&gt; &#123;</span><br><span class="line">  class Class6</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p><a href="https://www.plantuml.com/plantuml/svg/RSon3O0W443HdYbk1GMj38t6qi8DJa0BqJEYbM5tiJ84cb-z_0FLYek1cQY0fXb86odXOG3AedFGVYsOPySFom17eoxS5vlHPQnvq3rPRSwKyfYAZvwuPQOYbbMGhQLRfxAAPHraXnTEw3BRclaN" class="gallery-item"><img src="https://www.plantuml.com/plantuml/svg/RSon3O0W443HdYbk1GMj38t6qi8DJa0BqJEYbM5tiJ84cb-z_0FLYek1cQY0fXb86odXOG3AedFGVYsOPySFom17eoxS5vlHPQnvq3rPRSwKyfYAZvwuPQOYbbMGhQLRfxAAPHraXnTEw3BRclaN" /></a></p>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>PlantUML</tag>
      </tags>
  </entry>
  <entry>
    <title>实用Optional时需要注意的二三事</title>
    <url>/2020/08/31/java-optional-best-practice/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<h2 id="使用了Optional为什么还发生NullPointerException"><a href="#使用了Optional为什么还发生NullPointerException" class="headerlink" title="使用了Optional为什么还发生NullPointerException"></a>使用了Optional为什么还发生NullPointerException</h2><h2 id="对于空值应该怎么设置"><a href="#对于空值应该怎么设置" class="headerlink" title="对于空值应该怎么设置"></a>对于空值应该怎么设置</h2><h2 id="如何高效的实用Optional返回值"><a href="#如何高效的实用Optional返回值" class="headerlink" title="如何高效的实用Optional返回值"></a>如何高效的实用Optional返回值</h2><h2 id="如何避免Optional相关的反模式"><a href="#如何避免Optional相关的反模式" class="headerlink" title="如何避免Optional相关的反模式"></a>如何避免Optional相关的反模式</h2><h2 id="怎么把Optional用的更专业"><a href="#怎么把Optional用的更专业" class="headerlink" title="怎么把Optional用的更专业"></a>怎么把Optional用的更专业</h2><h2 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h2><p><a href="https://blogs.oracle.com/javamagazine/12-recipes-for-using-the-optional-class-as-its-meant-to-be-used" target="_blank" rel="noopener">The Java Optianl class (12 recipes)</a></p>
<p><a href="https://blogs.oracle.com/javamagazine/the-java-optional-class-11-more-recipes-for-preventing-null-pointer-exceptions" target="_blank" rel="noopener">The Java Optianl class (11 recipes)</a></p>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>关于Enums的小问题</title>
    <url>/2020/02/14/oracle-quiz-enums/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<p><a href="https://blogs.oracle.com/javamagazine/quiz-yourself-understanding-enums-advanced" target="_blank" rel="noopener">原文链接</a></p>
<p>首先给出以下Enum：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> Size&#123;</span><br><span class="line">  SMALL,</span><br><span class="line">  MEDIUM,</span><br><span class="line">  LARGE</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>问下面的代码执行的结果是：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">final</span> <span class="keyword">var</span> size = Size.SMALL; <span class="comment">//line n1</span></span><br><span class="line">    <span class="keyword">switch</span> (size) &#123; <span class="comment">//line n2</span></span><br><span class="line">        <span class="keyword">case</span> SMALL: &#123;System.out.println(size);&#125; <span class="comment">//line n3</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<ol>
<li>n1处编译失败，初始化的写法错误</li>
<li>n2处编译失败，没对应把所有Size包含在case内</li>
<li>n3处编译失败，case后面应该是完整的<code>Size.SMALL</code></li>
<li>输出 <code>SMALL</code></li>
<li>输出 <code>Size@3cb5cdba</code></li>
</ol>
<h2 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h2><p>选项1是错的，局部变量可以使用final来修饰，从<code>Java 10</code>开始，可以使用关键字<code>var</code>来声明局部变量，声明的变量类型由初始化的类型决定。<br>选项2也是错的，switch-case从来都不需要面对所有情况写case。<br>选项3也是错的，当switch的判断条件是enum类型时，case里不需要写类名。<br>选项4是对的，enum类型默认重写了<code>toString</code>方法，输出name的值。<br>选项5是错的，上面说了，enum的toString输出的是name，而不是HashCode.</p>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>Java</tag>
        <tag>Quiz Yourself</tag>
      </tags>
  </entry>
  <entry>
    <title>使用Java读写CSV文件</title>
    <url>/2020/02/06/java-io-csv/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<p>最近针对一个Issue，需要给excel文件的读写功能增加CSV的支持，本来以为使用类似的方法可以直接写到DTO里面，结果一查，Java好像对CSV的支持还是最原始最基本的写法（并非贬义，可能是容易了反而不需要什么工具来读写CSV）。</p>
<p>总结成一句话就是，读写CSV就相当于读写一个纯字符串文件。</p>
<h2 id="读取CSV"><a href="#读取CSV" class="headerlink" title="读取CSV"></a>读取CSV</h2><p>读取CSV文件大致分为以下几步：</p>
<ol>
<li>使用<code>FileInputStream</code>读取csv文件</li>
<li>使用<code>BufferedReader</code>来对输入流进行更高效的处理</li>
<li>将每一行的数据作为字符串读取，使用<code>split(&quot;,&quot;)</code>分割</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 字符编码 设置下没坏处</span></span><br><span class="line">Charset charset = StandardCharsets.UTF_8;</span><br><span class="line"><span class="comment">// 缓存大小 提高读取效率 </span></span><br><span class="line"><span class="keyword">int</span> bufferSize = <span class="number">5</span> * <span class="number">1024</span> * <span class="number">1024</span>;</span><br><span class="line"><span class="keyword">try</span> (BufferedReader reader = <span class="keyword">new</span> BufferedReader(</span><br><span class="line">        <span class="keyword">new</span> InputStreamReader(<span class="keyword">new</span> FileInputStream(<span class="string">"input/sample.csv"</span>), charset), bufferSize)) &#123;</span><br><span class="line">    String line;</span><br><span class="line">    List&lt;String[]&gt; data = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">    <span class="comment">// 这里假设文件不是很大，不然这么些内存是不够用的</span></span><br><span class="line">    <span class="keyword">while</span> (Objects.nonNull(line = reader.readLine())) &#123;</span><br><span class="line">        data.add(line.split(<span class="string">","</span>));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 然后 想怎么用就怎么用了</span></span><br><span class="line">&#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">    <span class="comment">// IOException 就囊括了读取文件可能发生的全部意外</span></span><br><span class="line">    e.printStackTrace();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="写CSV"><a href="#写CSV" class="headerlink" title="写CSV"></a>写CSV</h2><p>跟上面读取CSV的操作正好反过来，写CSV文件分为以下几步:</p>
<ol>
<li>使用<code>FileOutputStream</code>输出文件</li>
<li>将要输出的一行数据使用指定的符号（csv一般是逗号）连接成字符串</li>
<li>输出到文件，使用<code>BufferedWriter.write()</code>的话记得换行</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 文件路径和文件名</span></span><br><span class="line">String file = <span class="string">"output/output.csv"</span>;</span><br><span class="line"><span class="comment">// 指定字符编码</span></span><br><span class="line">Charset charset = StandardCharsets.UTF_8;</span><br><span class="line"><span class="comment">// 指定缓存</span></span><br><span class="line"><span class="keyword">int</span> bufferSize = <span class="number">5</span> * <span class="number">1024</span> * <span class="number">1024</span>;</span><br><span class="line"><span class="keyword">try</span> (BufferedWriter writer = <span class="keyword">new</span> BufferedWriter(</span><br><span class="line">        <span class="keyword">new</span> OutputStreamWriter(<span class="keyword">new</span> FileOutputStream(file), charset), bufferSize</span><br><span class="line">)) &#123;</span><br><span class="line">    <span class="keyword">for</span> (String[] datum : data) &#123;</span><br><span class="line">        writer.write(String.join(<span class="string">","</span>, datum));</span><br><span class="line">        <span class="comment">// 换行</span></span><br><span class="line">        writer.newLine();</span><br><span class="line">    &#125;</span><br><span class="line">&#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">    e.printStackTrace();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>Java</tag>
        <tag>CSV</tag>
      </tags>
  </entry>
  <entry>
    <title>二进制大对象和字符大对象</title>
    <url>/2020/01/28/clob-octet/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<blockquote>
<p>这都是啥啥啥</p>
</blockquote>
<h2 id="CLOB-和-BLOB"><a href="#CLOB-和-BLOB" class="headerlink" title="CLOB 和 BLOB"></a>CLOB 和 BLOB</h2><p><strong>CLOB</strong> 是可变长度的字符（char）大（long）对象（Object），用于基于字符的数据，以字节Byte或者标准八字节OCTET为单位，最大长度是2G（2,147,483,647）</p>
<p><strong>BLOB</strong> 是可变长度的二进制（Binary）大（long）对象（Object），用于保存语音，混合媒体等非传统数据，以字节Byte为单位，最大长度也是2G（2,147,483,647）</p>
<h2 id="OCTET-和-Byte"><a href="#OCTET-和-Byte" class="headerlink" title="OCTET 和 Byte"></a>OCTET 和 Byte</h2><p>我们都知道计算机中一个0/1表示一个比特bit,八个比特表示一个字节byte。但是在实际应用中，byte可以表示4-10比特的不同长度，为了统一标准，将严格意义上的8bits也称为<a href="https://zh.wikipedia.org/wiki/%E5%85%AB%E4%BD%8D%E5%85%83%E7%B5%84" target="_blank" rel="noopener">OCTET</a></p>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
  </entry>
  <entry>
    <title>Python中类的使用</title>
    <url>/2020/01/28/python-class/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<blockquote>
<p>类就是Class</p>
</blockquote>
<h2 id="类的基本"><a href="#类的基本" class="headerlink" title="类的基本"></a>类的基本</h2><ol>
<li>类和实体 我们可以简单的将实体（instance）理解成具体的实物对象，把类看作是生产实体的蓝图。比如我们定义一个类<code>Car</code>，之后就可以在任何地方反复调用这个蓝图来生产<code>Car</code>的实体</li>
<li>方法 在Python中我们可以像写脚本一样随便写个方法就执行，但是这可能带来诸多麻烦。比如我们为汽车的启动和停止写了两个方法<code>run()</code>和<code>stop()</code>，为了确定让哪辆车启动哪辆车停下，我们需要把对象的车多为参数传递进方法，是不是有一种拿遥控器开车还要每次都设置开的哪辆车的感觉？ 当我们需要在不同的地方使用这两个方法时，就要重新写一遍。如果我们把<code>run()</code> 和<code>stop()</code>写到类<code>Car</code>里面，就可以通过每个<code>Car</code>的实体，直接调用启动和停止的方法，方法属于车的实体，不同实体间的启动停止互不影响，这就有开车那味儿了。</li>
<li>成员变量 类的成员变量分为两种(此处为Java思维）：静态变量和实例变量。 所谓静态变量，我们可以理解成刻在蓝图里的，比如我们给<code>Car</code>定义一个静态变量<code>轮子数 = 4</code>,这样我们生成的每个车都可以使用这个变量，甚至不生成实例的时候，也可以使用<code>Car.轮子数</code>, 只要看一下蓝图，就知道这个变量值。相比之下，实例变量就是指从属于实例的变量，比如<code>排气量 长度 宽度</code>,如果要调用这些变量，直接通过<code>Car.排气量</code>是不行的，因为图纸上可没具体写这变量是多少，需要先生成一个实体<code>my_car = Car()</code>，然后给<code>my_car</code>设置并使用</li>
</ol>
<h2 id="Python中类的使用"><a href="#Python中类的使用" class="headerlink" title="Python中类的使用"></a>Python中类的使用</h2><h3 id="类的定义"><a href="#类的定义" class="headerlink" title="类的定义"></a>类的定义</h3><p>Python中定义类使用的是关键字<code>class</code>，类的范围通过统一的缩进来管理<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Car</span>:</span></span><br><span class="line">  id = <span class="number">12345</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, id)</span>:</span></span><br><span class="line">    self.id = id</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self)</span>:</span></span><br><span class="line">    print(<span class="string">f'run <span class="subst">&#123;self.id&#125;</span>!'</span>)</span><br></pre></td></tr></table></figure></p>
<p>类的内部使用关键字<code>def</code>来定义方法，方法必须至少有一个参数<code>self</code>，<code>self</code>的含义相当于Java的<code>this</code>，指的是类的实例，可以通过<code>self</code>来访问实例变量和方法。  </p>
<p><code>__init__</code>是构造体，当我们使用类名来生成实例的时候，会根据参数来调用对应的构造体生成实例，最基本的写法就是直接通过构造体给实例变量初始化，比如使用上面代码里得构造体：<code>Car(54321)</code>。  </p>
<p>Python中使用<code>__</code>双下划线来定义私有变量和私有方法，私有的元素无法在外部通过实例名或者类名访问到</p>
<p>就像Java中所有的类都是Object的字类，都继承了一些基本的方法一样，Python中也有一些类的专有方法, 且可以重写：</p>
<ul>
<li><strong>init</strong> : 构造函数，在生成对象时调用</li>
<li><strong>del</strong> : 析构函数，释放对象时使用</li>
<li><strong>repr</strong> : 打印，转换</li>
<li><strong>setitem</strong> : 按照索引赋值</li>
<li><strong>getitem</strong>: 按照索引获取值</li>
<li><strong>len</strong>: 获得长度</li>
<li><strong>cmp</strong>: 比较运算</li>
<li><strong>call</strong>: 函数调用</li>
<li><strong>add</strong>: 加运算</li>
<li><strong>sub</strong>: 减运算</li>
<li><strong>mul</strong>: 乘运算</li>
<li><strong>truediv</strong>: 除运算</li>
<li><strong>mod</strong>: 求余运算</li>
<li><strong>pow</strong>: 乘方</li>
</ul>
<h2 id="后记"><a href="#后记" class="headerlink" title="后记"></a>后记</h2><p>跟同事聊了聊为什么要使用类，想起了一个很重要的点：内存管理。<br>如果我们不适用类来管理方法和变量的话，程序运行过程中，定义的变量和方法就会一直存在，占用内存空间；但是当我们使用了类之后，则可以在需要的时候声明类的实例来调用内存，当一个实例不再被使用的时候，又可以通过垃圾回收自动收回内存（垃圾回收机制不同语言有不同的做法）。这样我们的程序才可以根据需求灵活地使用资源。<br>当然，方法和变量的管理更方便什么的，也算使用类的好处</p>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>Python</tag>
      </tags>
  </entry>
  <entry>
    <title>Git Prune命令</title>
    <url>/2020/01/24/git-prune/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<p><code>git prune</code> 简单地说就是一个清扫（housekeeping）的命令。</p>
<p>使用git查看分支，可以看到分支有三个重要分类：本地分支，本地-远程分支的参照，远程分支<br>比如我们在github上merge了一个PR，顺手删除了一个分支，这时在本地的电脑上本删除的分支对应的本地分支，远程分支，远程本地的参照关系都还存在，如果我们使用<code>git branch -D name</code>删除该分支， 会发现只删除了本地的分支，所参照的远程分支<code>origin/name</code>依然存在，清理这种残留垃圾的命令的时候，就是用到了prune。</p>
<h3 id="git-remove-prune-和-git-fetch-prune"><a href="#git-remove-prune-和-git-fetch-prune" class="headerlink" title="git remove prune 和 git fetch --prune"></a><code>git remove prune</code> 和 <code>git fetch --prune</code></h3><p><code>git remove prune</code> 清理所有已经删掉的远程分支参照，但是不会删除本地分支</p>
<p><code>git fetch --prune</code> 则是清理完远程的无用分支之后，再把最近状态fetch到本地</p>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>git</tag>
      </tags>
  </entry>
  <entry>
    <title>AngularJS 页面载入事件的相关方法</title>
    <url>/2020/01/21/ng-state-event/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<p>AngularJS的uirouter一个重要的内容就是使用<code>state</code>的使用，这里记录一组关于<code>state</code>的事件，每当路由转移过程中发生对应事件时，都会在<code>$rootScrope</code>广播，使用<code>$rootScope.$on(&#39;$stateEventName&#39;, function(event, ...))</code>获取该事件。</p>
<h2 id="0-引入router-state-events"><a href="#0-引入router-state-events" class="headerlink" title="0. 引入router.state.events"></a>0. 引入router.state.events</h2><p>首先引入JavaScript源文件：<br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">"stateEvents.js"</span>&gt;</span><span class="undefined"></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure><br>然后导入模块：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">angular.module(<span class="string">"myApplication"</span>, [<span class="string">'ui.router'</span>, <span class="string">'ui.router.state.events'</span>]</span><br></pre></td></tr></table></figure></p>
<h2 id="1-事件"><a href="#1-事件" class="headerlink" title="1. 事件"></a>1. <a href="https://ui-router.github.io/ng1/docs/latest/modules/ng1_state_events.html" target="_blank" rel="noopener">事件</a></h2><ol>
<li>$stateChangeCancel</li>
<li>$stateChangeStart</li>
<li>$stateNotFound</li>
<li>$stateChangeError</li>
<li>$stateChangeSuccess</li>
</ol>
<h3 id="stateChangeCancel"><a href="#stateChangeCancel" class="headerlink" title="$stateChangeCancel"></a>$stateChangeCancel</h3><p>当一个页面的转移被取消时，会在<code>$rootScope</code>广播<code>$stateChangeCancel</code>事件<br>有以下参数可以使用：</p>
<ul>
<li>toState 目标state</li>
<li>toParams 前往目标state时的参数</li>
<li>fromState 来源state</li>
<li>fromParams 带来的参数</li>
<li>options 选项</li>
<li>$transition$ </li>
</ul>
<p>和开始事件<code>$stateChangeStart</code> ， 成功事件<code>$stateChangeSuccess</code> 提供的参数相同</p>
<h3 id="stateChangeError"><a href="#stateChangeError" class="headerlink" title="$stateChangeError"></a>$stateChangeError</h3><p>相比于开始和成功的事件，错误事件多一个参数<code>error</code>，其内容当然是错误信息。</p>
<h3 id="stateNotFound"><a href="#stateNotFound" class="headerlink" title="$stateNotFound"></a>$stateNotFound</h3><p>没有找到state时的事件，提供一下参数可以用：</p>
<ul>
<li>unfoudState 未找到的state信息，提供<code>to</code> <code>toParams</code> <code>options</code>等属性</li>
<li>fromState 来源state</li>
<li>fromParams 来源参数</li>
<li>options 选项</li>
</ul>
<h2 id="2-更新到-Transition-Hooks"><a href="#2-更新到-Transition-Hooks" class="headerlink" title="2. 更新到 Transition Hooks"></a>2. 更新到 <a href="https://ui-router.github.io/ng1/docs/1.0.0/classes/transition.transitionservice.html" target="_blank" rel="noopener">Transition Hooks</a></h2><p>uirouter已经更新了IHookRegistry接口，可以对<code>transition</code>进行更为详细控制。<br><code>$transition</code> 和<code>$transitionService</code>都实现了该接口，但是<code>$transition</code>对象只能在转换开始前使用。  </p>
<p>该接口提供以下方法：</p>
<ol>
<li>getHooks 返回所有已登录的hook方法，参数时方法名，比如<code>onBefore onEnter</code>等等</li>
<li>onBefore 开始前</li>
<li>onStart 开始后</li>
<li>onExit 退出，onStart之后transition会推出state</li>
<li>onRetain 保留，onExit之后onRetain会被执行，子state会被优先执行</li>
<li>onEnter 进入，在onRetain结束后，transition会进入state</li>
<li>onFinish 结束前，这是最后可以取消或者定向transition的方法</li>
<li>onError 错误</li>
<li>onSuccess 成功</li>
</ol>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
  </entry>
  <entry>
    <title>CSS 文字对齐</title>
    <url>/2020/01/16/css-vertical-align/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<p>本来想直接把代码内嵌到markdown里，但是hexo好像不支持，残念</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">table</span> <span class="attr">style</span>=<span class="string">"width:1000px"</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">tr</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">th</span>&gt;</span><span class="tag">&lt;/<span class="name">th</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">th</span>&gt;</span>A<span class="tag">&lt;/<span class="name">th</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">th</span>&gt;</span>B<span class="tag">&lt;/<span class="name">th</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">th</span>&gt;</span>C<span class="tag">&lt;/<span class="name">th</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">tr</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">tr</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">th</span> &gt;</span>0<span class="tag">&lt;/<span class="name">th</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">td</span> <span class="attr">style</span>=<span class="string">"text-align: right;"</span>&gt;</span>text-align: right;<span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">td</span> <span class="attr">style</span>=<span class="string">"text-align: left;"</span>&gt;</span>text-align: left;<span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">td</span> <span class="attr">style</span>=<span class="string">"text-align: center;"</span>&gt;</span>text-align: center;<span class="tag">&lt;/<span class="name">td</span>&gt;</span>                                             </span><br><span class="line">  <span class="tag">&lt;/<span class="name">tr</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">tr</span> <span class="attr">style</span>=<span class="string">"height:100px;"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">th</span>&gt;</span>1<span class="tag">&lt;/<span class="name">th</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">td</span> <span class="attr">style</span>=<span class="string">"vertical-align: top;"</span>&gt;</span>vertical-align: top;<span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">td</span> <span class="attr">style</span>=<span class="string">"vertical-align: bottom;"</span>&gt;</span>vertical-align: bottom;<span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">td</span> <span class="attr">style</span>=<span class="string">"vertical-align: middle;"</span>&gt;</span>vertical-align: middle;<span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">tr</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">table</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>水平方向对齐：</p>
<ol>
<li><code>text-align: right;</code></li>
<li><code>text-align: center;</code></li>
<li><code>text-align: left;</code></li>
</ol>
<p>垂直方向对齐：</p>
<ol>
<li><code>vertical-align: top;</code></li>
<li><code>vertical-align: middle;</code></li>
<li><code>vertical-align: bottom;</code></li>
</ol>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>css</tag>
      </tags>
  </entry>
  <entry>
    <title>pipenv 基本的介绍和使用方法</title>
    <url>/2019/12/12/pipenv-introduction/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<blockquote>
<p>根据项目需要，搞一个配置文件Pipfile，传到github里，完事儿。</p>
</blockquote>
<h2 id="为什么要用pipenv"><a href="#为什么要用pipenv" class="headerlink" title="为什么要用pipenv"></a>为什么要用pipenv</h2><p>首先，我们假设….算了，不扯那些没用的。做Python的开发，无论你用的什么工具，最麻烦的事儿应该莫过于包的管理。<a href="https://pypi.org/project/pipenv/" target="_blank" rel="noopener">pipenv</a>简单来说就是一个python官方推荐的包管理工具。<br>相比于pip等其他工具，pipenv最大的特点是可以根据项目，生成不同的环境，在ProjectA中设置的Pipfile不会影响到ProjectB,更不会影响到系统默认的pip列表。这个为项目定制的虚拟环境，我们称之为<code>shell</code>。  </p>
<h3 id="安装pipenv"><a href="#安装pipenv" class="headerlink" title="安装pipenv"></a>安装pipenv</h3><p>安装pipenv之前，首先确定系统里有以下两样东西：</p>
<ol>
<li>python</li>
<li>pip</li>
</ol>
<p>pipenv是通过pip安装的工具，所以执行<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">pip install pipenv</span><br></pre></td></tr></table></figure><br>由于pipenv可以在系统里不同版本的Python间任意切换，所以执行pip的时候不同太在乎Python版本问题。</p>
<h3 id="requirements-txt-和-Pipfile"><a href="#requirements-txt-和-Pipfile" class="headerlink" title="requirements.txt 和 Pipfile"></a>requirements.txt 和 Pipfile</h3><p>pipenv 管理Python环境的配置文件是<code>Pipfile</code>，大致长这个样子：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[[source]]</span><br><span class="line">name &#x3D; &quot;pypi&quot;</span><br><span class="line">url &#x3D; &quot;https:&#x2F;&#x2F;pypi.org&#x2F;simple&quot;</span><br><span class="line">verify_ssl &#x3D; true</span><br><span class="line"></span><br><span class="line">[dev-packages]</span><br><span class="line"></span><br><span class="line">[packages]</span><br><span class="line">pandas &#x3D; &quot;&#x3D;&#x3D;0.25.1&quot;</span><br><span class="line"></span><br><span class="line">[requires]</span><br><span class="line">python_version &#x3D; &quot;3.7&quot;</span><br></pre></td></tr></table></figure>
<p>我们可以直接写，也可以用<code>pipenv install</code>来生成默认的Pipfile, 不过还有个更方便的方法是直接由pip生成的requirements.txt配置文件来生成Pipfile。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 首先在当前路径里使用pip生成requirements.txt</span><br><span class="line"></span><br><span class="line">pip freeze &gt; requirements.txt</span><br><span class="line"></span><br><span class="line"># 然后生成Pipfile</span><br><span class="line"># -r 的意思是将后面配置文件里的包添加到Pipfile里</span><br><span class="line"></span><br><span class="line">pipenv install -r requirements.txt</span><br></pre></td></tr></table></figure>
<h3 id="指定Python版本"><a href="#指定Python版本" class="headerlink" title="指定Python版本"></a>指定Python版本</h3><p>pipenv可以指定虚拟环境的python版本，随时通过修改Pipfile来切换（修改是通过命令行修改的意思，当然你要直接修改也没什么区别，但是不用命令来修改配置文件就会觉得很low啊）<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">## 使用下面的命令，从当前系统已经安装的不同版本的Python中选择一个</span><br><span class="line">pipenv --python 版本号</span><br><span class="line"></span><br><span class="line">## 比如</span><br><span class="line">pipenv --python 3.5</span><br><span class="line"></span><br><span class="line">pipenv --python 3.7</span><br><span class="line"></span><br><span class="line">pipenv --python 2.6</span><br></pre></td></tr></table></figure><br>如果Pipfile中指定的Python版本在当前系统下不存在，之后启动虚拟环境时会跳出警告。</p>
<h3 id="耗时警报-锁定"><a href="#耗时警报-锁定" class="headerlink" title="(耗时警报) 锁定"></a>(耗时警报) 锁定</h3><p>更新Pipfile之后，启动虚拟环境之前，我们需要把Pipfile中列出的包安装好，或者删除已经不用的包，这个操作就是<code>pipenv lock</code></p>
<p>由于需要从服务器下载相应的包（这是我猜的，鬼晓得为什么这玩意儿怎么那么慢，<a href="https://github.com/pypa/pipenv/issues/1785" target="_blank" rel="noopener">官方</a>似乎也没有什么行之有效的办法）</p>
<h3 id="去吧-皮卡丘！"><a href="#去吧-皮卡丘！" class="headerlink" title="去吧 皮卡丘！"></a>去吧 皮卡丘！</h3><p>都搞定之后，<code>pipenv shell</code>跑起来，我们就进入定制的虚拟环境了。</p>
<h3 id="直接看这里就可以了"><a href="#直接看这里就可以了" class="headerlink" title="直接看这里就可以了"></a>直接看这里就可以了</h3><p>说了那么老多，其实安装好pipenv之后，直接一个<code>pipenv -h</code>查看help文档就可以了<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Options:</span><br><span class="line">  --where             Output project home information.</span><br><span class="line">  --venv              Output virtualenv information.</span><br><span class="line">  --py                Output Python interpreter information.</span><br><span class="line">  --envs              Output Environment Variable options.</span><br><span class="line">  --rm                Remove the virtualenv.</span><br><span class="line">  --bare              Minimal output.</span><br><span class="line">  --completion        Output completion (to be eval&#39;d).</span><br><span class="line">  --man               Display manpage.</span><br><span class="line">  --support           Output diagnostic information for use in GitHub issues.</span><br><span class="line">  --site-packages     Enable site-packages for the virtualenv.  [env var:</span><br><span class="line">                      PIPENV_SITE_PACKAGES]</span><br><span class="line">  --python TEXT       Specify which version of Python virtualenv should use.</span><br><span class="line">  --three &#x2F; --two     Use Python 3&#x2F;2 when creating virtualenv.</span><br><span class="line">  --clear             Clears caches (pipenv, pip, and pip-tools).  [env var:</span><br><span class="line">                      PIPENV_CLEAR]</span><br><span class="line">  -v, --verbose       Verbose mode.</span><br><span class="line">  --pypi-mirror TEXT  Specify a PyPI mirror.</span><br><span class="line">  --version           Show the version and exit.</span><br><span class="line">  -h, --help          Show this message and exit.</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">Usage Examples:</span><br><span class="line">   Create a new project using Python 3.7, specifically:</span><br><span class="line">   $ pipenv --python 3.7</span><br><span class="line"></span><br><span class="line">   Remove project virtualenv (inferred from current directory):</span><br><span class="line">   $ pipenv --rm</span><br><span class="line"></span><br><span class="line">   Install all dependencies for a project (including dev):</span><br><span class="line">   $ pipenv install --dev</span><br><span class="line"></span><br><span class="line">   Create a lockfile containing pre-releases:</span><br><span class="line">   $ pipenv lock --pre</span><br><span class="line"></span><br><span class="line">   Show a graph of your installed dependencies:</span><br><span class="line">   $ pipenv graph</span><br><span class="line"></span><br><span class="line">   Check your installed dependencies for security vulnerabilities:</span><br><span class="line">   $ pipenv check</span><br><span class="line"></span><br><span class="line">   Install a local setup.py into your virtual environment&#x2F;Pipfile:</span><br><span class="line">   $ pipenv install -e .</span><br><span class="line"></span><br><span class="line">   Use a lower-level pip command:</span><br><span class="line">   $ pipenv run pip freeze</span><br><span class="line"></span><br><span class="line">Commands:</span><br><span class="line">  check      Checks for security vulnerabilities and against PEP 508 markers</span><br><span class="line">             provided in Pipfile.</span><br><span class="line">  clean      Uninstalls all packages not specified in Pipfile.lock.</span><br><span class="line">  graph      Displays currently-installed dependency graph information.</span><br><span class="line">  install    Installs provided packages and adds them to Pipfile, or (if no</span><br><span class="line">             packages are given), installs all packages from Pipfile.</span><br><span class="line">  lock       Generates Pipfile.lock.</span><br><span class="line">  open       View a given module in your editor.</span><br><span class="line">  run        Spawns a command installed into the virtualenv.</span><br><span class="line">  shell      Spawns a shell within the virtualenv.</span><br><span class="line">  sync       Installs all packages specified in Pipfile.lock.</span><br><span class="line">  uninstall  Un-installs a provided package and removes it from Pipfile.</span><br><span class="line">  update     Runs lock, then sync.</span><br></pre></td></tr></table></figure></p>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>Python</tag>
        <tag>Pipenv</tag>
      </tags>
  </entry>
  <entry>
    <title>均方误差和决定系数</title>
    <url>/2019/11/28/measures-for-in-sample-evaluation/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<h2 id="均方误差-Mean-Square-Error"><a href="#均方误差-Mean-Square-Error" class="headerlink" title="均方误差 Mean Square Error"></a>均方误差 Mean Square Error</h2><p>是对误差的一个估计，其定义为所有估值和样本的误差平方的平均值。  </p>
<p>在python中这样写：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> mean_squared_error</span><br><span class="line"></span><br><span class="line">mean_sqlared_error(df[<span class="string">'data'</span>],predict)</span><br></pre></td></tr></table></figure></p>
<h2 id="决定系数-Coefficient-of-determination-R-2"><a href="#决定系数-Coefficient-of-determination-R-2" class="headerlink" title="决定系数 Coefficient of determination / $R^2$"></a>决定系数 Coefficient of determination / $R^2$</h2><p>维基百科的定义是：<em>用于度量因变量的变异中可由自变量解释部分所占的比例，以此来判断统计模型的解释力。</em>  </p>
<ul>
<li>首先得到一组因变量 取得平均值 $\bar{y}$</li>
<li>由此可以得到总的方差 $SS_{tot}$</li>
<li>再取得预估值的方差 $SS_{res}$</li>
<li>最后得到决定系数 $R^2 = 1 - \frac{SS<em>{tot}}{SS</em>{res}}$</li>
</ul>
<p>在python中这样写：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.linear_model <span class="keyword">import</span> LinearRegression</span><br><span class="line"></span><br><span class="line">lm=LinearRegression()</span><br><span class="line"></span><br><span class="line">lm.score(X,Y)</span><br></pre></td></tr></table></figure></p>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>Python</tag>
        <tag>Data Science</tag>
      </tags>
  </entry>
  <entry>
    <title>使用Python的线性回归分析</title>
    <url>/2019/11/26/linear-regression/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<h2 id="线性回归是啥？"><a href="#线性回归是啥？" class="headerlink" title="线性回归是啥？"></a>线性回归是啥？</h2><p>线性回归是统计学中的一种回归分析方法，我们可以简单的记住线性回归中有两个重要变量：</p>
<ol>
<li>The predictor(independent) variable 自变量 x</li>
<li>the target(dependent) variable 因变量 y</li>
</ol>
<p>如果线性回归方程中只有一个x，则称为简单线性回归，大于一个x时，成为多元线性回归。<br>举个例子<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">y &#x3D; b0 + b1*x</span><br></pre></td></tr></table></figure><br>这个简单线性回归方程中，x是自变量，y是因变量，b0是intercept, b1叫做slope</p>
<h2 id="Python中的简单线性回归"><a href="#Python中的简单线性回归" class="headerlink" title="Python中的简单线性回归"></a>Python中的简单线性回归</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.linear_model <span class="keyword">import</span> LinearRegression</span><br><span class="line"></span><br><span class="line">lm=LinearRegression()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 假设我们有一个dataframe df</span></span><br><span class="line">X = df[[<span class="string">'highway-mpg'</span>]] <span class="comment">#作为自变量 X可以指定多列</span></span><br><span class="line">Y = df[<span class="string">'Price'</span>] <span class="comment">#因变量Y只有一列</span></span><br><span class="line"></span><br><span class="line">lm.fit(X,Y) <span class="comment">#使用线性回归分析</span></span><br><span class="line"></span><br><span class="line">Yhat = lm.predict(X_0) <span class="comment"># X_0 跟X又相同的列数</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 可以查看线性回归方程的参数</span></span><br><span class="line">lm.intercept_</span><br><span class="line"></span><br><span class="line">lm.coef_</span><br></pre></td></tr></table></figure></div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>Data Analysis</tag>
      </tags>
  </entry>
  <entry>
    <title>关于Java的stream执行顺序的笔记</title>
    <url>/2019/11/20/about-java-stream-lazy/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<h2 id="Java-util-stream-基本"><a href="#Java-util-stream-基本" class="headerlink" title="Java.util.stream 基本"></a>Java.util.stream 基本</h2><p>首先来个例子：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Stream.of(<span class="string">"aaa"</span>, <span class="string">"aa"</span>, <span class="string">"bbbb"</span>, <span class="string">"bb"</span>, <span class="string">"ccccc"</span>, <span class="string">"cc"</span>, <span class="string">"dddddd"</span>, <span class="string">"eeeeeee"</span>)</span><br><span class="line">        .filter(len -&gt; len.length() &gt; <span class="number">3</span>)</span><br><span class="line">        .forEach(System.out::println);</span><br></pre></td></tr></table></figure><br>stream在使用的时候，分为中间操作（intermediate operation)和终端操作（terminal operation），例子中的<code>filter()</code>就是中间操作，中间操作可以有多个，跟火车厢一样接起来就可以。<br>而终端操作（或者叫终结操作)只能有一个。终端操作很容易理解成是对stream的结束，事实上终端操作最主要的作用是执行。如果没有终端操作，中间操作的内容将不会生效。所以，下面这个stream执行后是什么效果？<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Stream.of(<span class="string">"aaa"</span>, <span class="string">"aa"</span>, <span class="string">"bbbb"</span>, <span class="string">"bb"</span>, <span class="string">"ccccc"</span>, <span class="string">"cc"</span>, <span class="string">"dddddd"</span>, <span class="string">"eeeeeee"</span>)</span><br><span class="line">        .mapToInt(String::length)</span><br><span class="line">        .filter(len -&gt; len &gt; <span class="number">3</span>)</span><br><span class="line">        .peek(System.out::println)</span><br><span class="line">        .limit(<span class="number">2</span>);</span><br></pre></td></tr></table></figure><br>答案是：什么也不输出。因为<code>limit()</code>是个中间方法，对这个stream的操作将不会生效。  </p>
<h3 id="执行顺序"><a href="#执行顺序" class="headerlink" title="执行顺序"></a>执行顺序</h3><p>弄懂了什么中间操作和终端操作的区别以后，我们就要面对下一个疑问了：中间操作和终端操作的执行顺序是什么？<br>很简单，来个例子瞧瞧：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Stream.of(<span class="string">"aaa"</span>, <span class="string">"aa"</span>, <span class="string">"bbbb"</span>, <span class="string">"bb"</span>, <span class="string">"ccccc"</span>, <span class="string">"cc"</span>, <span class="string">"dddddd"</span>, <span class="string">"eeeeeee"</span>)</span><br><span class="line">        .filter(len -&gt; len.length() &gt; <span class="number">3</span>)</span><br><span class="line">        .peek(System.out::println)</span><br><span class="line">        .mapToInt(String::length)</span><br><span class="line">        .limit(<span class="number">2</span>)</span><br><span class="line">        .forEach(System.out::println);</span><br></pre></td></tr></table></figure><br>输出结果有几行？内容是?</p>
<a href="/2019/11/20/about-java-stream-lazy/1.png" class="gallery-item"><img src="/2019/11/20/about-java-stream-lazy/1.png" class="" title="result"></a>
<p>这里重点在<code>peek()</code>，<code>limit()</code>和<code>forEach()</code>的生效顺序，从结果上来看,应该是stream中的每一个元素，对应一组打包的操作，所以输出字符串b之后紧接着输出的是b的长度，而不是字符串c。而<code>limit()</code>限制了处理的元素个数，大于limit的元素到达不了终端操作，自然不会生效。</p>
<p>我们换个方法再验证一遍，这次终端操作用<code>sum()</code>来替代<code>forEach()</code>，终端操作不是对元素逐一的操作了，对中间操作的执行顺序会有什么影响吗？<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Stream.of(<span class="string">"aaa"</span>, <span class="string">"aa"</span>, <span class="string">"bbbb"</span>, <span class="string">"bb"</span>, <span class="string">"ccccc"</span>, <span class="string">"cc"</span>, <span class="string">"dddddd"</span>, <span class="string">"eeeeeee"</span>)</span><br><span class="line">        .filter(len -&gt; len.length() &gt; <span class="number">3</span>)</span><br><span class="line">        .peek(System.out::println)</span><br><span class="line">        .mapToInt(String::length)</span><br><span class="line">        .limit(<span class="number">2</span>)</span><br><span class="line">        .peek(System.out::println)</span><br><span class="line">        .sum();</span><br></pre></td></tr></table></figure><br>结果跟<code>forEach()</code>是一样的。</p>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>Java</tag>
        <tag>stream</tag>
      </tags>
  </entry>
  <entry>
    <title>Log4j 节能写法</title>
    <url>/2019/11/14/log4j-using-lambda/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<h2 id="尽量使用Supplier-lt-gt-来生成log"><a href="#尽量使用Supplier-lt-gt-来生成log" class="headerlink" title="尽量使用Supplier&lt;&gt; 来生成log"></a>尽量使用Supplier&lt;&gt; 来生成log</h2><p>比如debug，我们可以使用下面三种方法<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Logs a message object with the &#123;<span class="doctag">@link</span> Level#DEBUG DEBUG&#125; level.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> message the message string to log.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">debug</span><span class="params">(String message)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Logs a message with parameters at the &#123;<span class="doctag">@link</span> Level#DEBUG DEBUG&#125; level.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> message the message to log; the format depends on the message factory.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> params parameters to the message.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@see</span> #getMessageFactory()</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">debug</span><span class="params">(String message, Object... params)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Logs a message with parameters which are only to be constructed if the logging level is the &#123;<span class="doctag">@link</span> Level#DEBUG</span></span><br><span class="line"><span class="comment"> * DEBUG&#125; level.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> message the message to log; the format depends on the message factory.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> paramSuppliers An array of functions, which when called, produce the desired log message parameters.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@since</span> 2.4</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">debug</span><span class="params">(String message, Supplier&lt;?&gt;... paramSuppliers)</span></span>;</span><br></pre></td></tr></table></figure></p>
<h3 id="第一种：直接输出信息。"><a href="#第一种：直接输出信息。" class="headerlink" title="第一种：直接输出信息。"></a>第一种：直接输出信息。</h3><p>最不环保的写法，甚至新手的话可能会出现<code>logger.debug(&quot;A is &quot; + a + &quot;, B is&quot; + b + &quot;.&quot;);</code>这种写法，首先使用<code>+</code>来生成log文本是非常耗时的，其次，无论log输出的级别是否低于debug，这段字符串的处理都会被执行。</p>
<h3 id="第二种：使用MessageFactory和参数生成log"><a href="#第二种：使用MessageFactory和参数生成log" class="headerlink" title="第二种：使用MessageFactory和参数生成log"></a>第二种：使用MessageFactory和参数生成log</h3><p>OpenJDK 8之前这么写是没问题的，使用MessageFactory来生成log既增加了可读性，又比字符串连接要节能。不过问题是log的生成还是总需要被执行。</p>
<h3 id="第三种：使用Lambda"><a href="#第三种：使用Lambda" class="headerlink" title="第三种：使用Lambda"></a>第三种：使用Lambda</h3><p>doc中写了:<code>only to be constructed if the logging level is the DEBUG level.</code>，还需要解释吗？</p>
<h3 id="那么，当需要把objectA写到debug里时"><a href="#那么，当需要把objectA写到debug里时" class="headerlink" title="那么，当需要把objectA写到debug里时"></a>那么，当需要把objectA写到debug里时</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">logger.debug(objectA.toString()); <span class="comment">// 一般写法</span></span><br><span class="line"></span><br><span class="line">logger.debug(() -&gt; objectA.toString()); <span class="comment">// 略动脑筋的写法</span></span><br><span class="line"></span><br><span class="line">logger.debug(objectA::toString); <span class="comment">//文艺写法</span></span><br></pre></td></tr></table></figure>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>Lambda</tag>
      </tags>
  </entry>
  <entry>
    <title>HAS-A 和 IS-A的区别</title>
    <url>/2019/11/14/has-a-and-is-a/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<h2 id="一句话总结"><a href="#一句话总结" class="headerlink" title="一句话总结"></a>一句话总结</h2><p>不妨通过字面意思来理解，HAS-A 就是有什么的意思，而IS-A就是是什么的意思。在面向对象编程（OOP）中，类的继承就是IS-A的关系，简单来说就是“张三是个人，旺财是条狗”的意思。而类的对象使用，多数时候可以看作是HAS-A的关系，比如把身高，体重都看作是类的话，我们可以说“张三有身高，有体重”。</p>
<ul>
<li>Inheritance 继承 IS-A</li>
<li>composition 组成 HAS-A</li>
</ul>
<h2 id="举个栗子"><a href="#举个栗子" class="headerlink" title="举个栗子"></a>举个栗子</h2><p>谷歌搜到的第一篇说明就很<a href="https://www.w3resource.com/java-tutorial/inheritance-composition-relationship.php" target="_blank" rel="noopener">浅显易懂</a></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Class Car&#123; ... &#125; <span class="comment">// 车是一个类 </span></span><br><span class="line"></span><br><span class="line">Class Engine&#123; ... &#125; <span class="comment">// 引擎也是一个类</span></span><br><span class="line"></span><br><span class="line">Class Toyota extends Car &#123; <span class="comment">// 丰田车作为一个有继承关系的子类，跟Car是IS-A关系，跟Engine是HAS-A的关系</span></span><br><span class="line">  <span class="keyword">private</span> Engine;</span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>Inheritance</tag>
      </tags>
  </entry>
  <entry>
    <title>Concurrency - Java的并发编程</title>
    <url>/2019/08/05/concurrency/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<p>Concurrency的意思是并发性，或者并行性（日语），顾名思义指的是同时处理不同的任务。并发编程的核心线程，可以简单地理解成RTS游戏里的“工人“单位:一个工人同一时间只能执行一个建造任务，增加工人可以增加建造序列的数量，也可以通过给工人切换建造任务来<strong>看起来</strong>增加建造序列，而这里的微操，全都是由系统来控制的。<br>所以，并发编程并不针对多核CPU，即使只有一个CPU，也可以通过分时段的线程切换来实现多个线程的并发执行。</p>
<h2 id="Java的基本多线程写法"><a href="#Java的基本多线程写法" class="headerlink" title="Java的基本多线程写法"></a>Java的基本多线程写法</h2><p>首先是线程的最基本写法。Java中有两种最基本的线程编写方法：</p>
<ol>
<li><p>继承Thread类，在<code>run()</code>中书写程序内容，使用<code>start()</code>来启动线程。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Sample</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String... args)</span> </span>&#123;</span><br><span class="line">        WorkerA a = <span class="keyword">new</span> WorkerA();</span><br><span class="line">        WorkerB b = <span class="keyword">new</span> WorkerB();</span><br><span class="line">        <span class="comment">// 这里我们有两个工人，可以同时启动两个建造任务了</span></span><br><span class="line">        a.start();</span><br><span class="line">        b.start();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">WorkerA</span> <span class="keyword">extends</span> <span class="title">Thread</span> </span>&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">    * 在run里写上具体要给这个工人分配的任务</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) &#123;</span><br><span class="line">            System.out.print(<span class="string">"Yes, my lord. "</span> + i);</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">"Building cpmolete."</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">WorkerB</span> <span class="keyword">extends</span> <span class="title">Thread</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) &#123;</span><br><span class="line">            System.out.print(<span class="string">"了解。"</span> + i);</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">"任務完了。"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>实现Runnable接口。<code>Thread</code>的构造体可以接受一个Runnable的参数，所以我们也可以通过写一个实现了Runnable接口的类来生成线程。虽然也是在run方法中写所要执行的任务，但是实现Runnable的类是作为生成线程的参数来使用，所以我们可以理解为创建了一类工作，然后以该工作为参数创建(灵魂召唤)多个工人来执行。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">  <span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Sample</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 我们创建了两个同样的工人，会执行同一种建造任务，比如都去造房子（卡人口不可取）</span></span><br><span class="line">        Work work = <span class="keyword">new</span> Work();</span><br><span class="line"></span><br><span class="line">        Thread workerA = <span class="keyword">new</span> Thread(work);</span><br><span class="line">        Thread workerB = <span class="keyword">new</span> Thread(work);</span><br><span class="line"></span><br><span class="line">        workerA.start();</span><br><span class="line">        workerB.start();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Work</span> <span class="keyword">implements</span> <span class="title">Runnable</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) &#123;</span><br><span class="line">            System.out.print(<span class="string">"Yes, my lord. "</span> + i);</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">"Building cpmolete."</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><strong>Runnable也可以使用Lambda表达式来定义</strong>，这样我们就可以直接写run方法的内容而不用去定义一整个类了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Thread c = <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) &#123;</span><br><span class="line">        System.out.println(<span class="string">"C "</span> + i);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br><span class="line">c.start();</span><br></pre></td></tr></table></figure>
</li>
</ol>
<h3 id="线程的状态和优先级"><a href="#线程的状态和优先级" class="headerlink" title="线程的状态和优先级"></a>线程的状态和优先级</h3><p><code>java.lang</code>的列举型Thread.State中定义了6中线程状态：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* A Thread which has not yet started.</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line">NEW,</span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* A Thread which is running or suspended.</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line">RUNNABLE,</span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* A Thread which is blocked on a monitor.</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line">BLOCKED, </span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* A Thread which is waiting with no timeout.</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line">WAITING,</span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* A Thread which is waiting with a timeout.</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line">TIMED_WAITING, </span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* A thread which is no longer alive.</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line">TERMINATED</span><br></pre></td></tr></table></figure></p>
<p>一个线程只能由<code>start()</code>启动一次，多次启动会产生<code>IllegalThreadStateException</code></p>
<p>在Thread类中定义了三个优先级常数：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * The maximum priority value for a Thread.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">static</span> <span class="keyword">int</span> MAX_PRIORITY = <span class="number">10</span>;		<span class="comment">// Maximum allowed priority for a thread</span></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * The minimum priority value for a Thread.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">static</span> <span class="keyword">int</span> MIN_PRIORITY = <span class="number">1</span>;			<span class="comment">// Minimum allowed priority for a thread</span></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * The default priority value for a Thread.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">static</span> <span class="keyword">int</span> NORM_PRIORITY = <span class="number">5</span>;		<span class="comment">// Normal priority for a thread</span></span><br></pre></td></tr></table></figure><br>线程的优先级可以通过<code>priority</code>的getter和setter来设置和确认，当两个进行中的线程发生冲突时，一般优先级高的线程会先运行，但这并不等于说优先级高的线程一定会被优先启动。系统在启动线程的时候并不看优先级的高低。</p>
<h3 id="Thread类"><a href="#Thread类" class="headerlink" title="Thread类"></a>Thread类</h3><p>Thread类提供了各种用来控制线程启动，暂停，再开，终止的方法，这里记录以下两个点的理解：</p>
<ol>
<li>InterruptedException. 当线程进入待机或者睡眠等状态时，就需要针对InterruptedException写一个try-catch，简单理解就是被人吵醒时应该怎么办。</li>
<li>sleep可以设置一定的时间，但并不等于时间到了线程就会停止挂起，继续运行。sleep只是线程在不被interrupt的情况下最短的睡眠时长，时间到了之后，也要视系统的资源分配情况来决定什么时候继续运行。</li>
</ol>
<h3 id="同步与排他"><a href="#同步与排他" class="headerlink" title="同步与排他"></a>同步与排他</h3><p>并发编程一个很常见的问题就是不同线程对同一资源的读写控制，这里就要提到两个很基本的概念，同步和排他（锁）。<br>排他锁比较好理解，就是通过关键字或者方法来指定某一部分同一时间只能被一个线程使用，被调用时这部分资源会被锁定，其他想要使用该资源的线程则会被挂起，知道资源解锁，轮班使用。<br><code>synchronized</code>是用来指定锁定资源的关键字，可以用来修饰方法，也可以用来指定代码块。<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">add</span><span class="params">(<span class="keyword">int</span> a)</span></span>&#123;...&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 或者</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">add</span><span class="params">(<span class="keyword">int</span> a)</span></span>&#123;</span><br><span class="line">    <span class="keyword">synchronized</span>(想要锁定的对象)&#123;</span><br><span class="line">        <span class="comment">//指定的对象在这段代码运行过程中被上锁</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>同步则是指使用wait(),notify(),notifyAll()等方法，具体的控制线程的运行。<br>wait()可以指定也可以不指定超时的值，wait可以让线程进入等待，直到等待超时或者其他的线程使用了notify(),notifyAll()或者interrupt()方法，使等待的线程进入可执行的状态。<br>notify()会随机让一个等待的线程再开，notifyAll()则是让所有该对象的线程再开。<br>wait(),notify(),notifyAll()都是用来控制线程访问被锁资源的，所以如果没有synchronized锁定资源，这些方法的会抛出<code>IllegalMonitorStateException</code>  </p>
<p>排他锁无法避免会出现不同的线程锁定不同的资源大家都干不了活儿的局面，这就是死锁；或者因为要使用的资源不断被其他线程占用而导致某一线程进展缓慢，这叫活锁。<br>死锁和活锁都不会在编译或者运行时报错，所以要留意设置超时，一定时间后手动解锁资源。等待资源访问时线程会处于<code>饥饿</code>的状态，饥饿的线程越多，则说明程序效率越低。为了减少饥饿，资源的调用顺序，锁定的粒度都需要好好思考。</p>
<h3 id="并发集合"><a href="#并发集合" class="headerlink" title="并发集合"></a>并发集合</h3><h2 id="高效的线程管理"><a href="#高效的线程管理" class="headerlink" title="高效的线程管理"></a>高效的线程管理</h2><h3 id="执行器"><a href="#执行器" class="headerlink" title="执行器"></a>执行器</h3><h3 id="线程池"><a href="#线程池" class="headerlink" title="线程池"></a>线程池</h3><h4 id="线程池的四种-BlockedQueue"><a href="#线程池的四种-BlockedQueue" class="headerlink" title="线程池的四种 BlockedQueue"></a>线程池的四种 BlockedQueue</h4><h2 id="多线程中的问题"><a href="#多线程中的问题" class="headerlink" title="多线程中的问题"></a>多线程中的问题</h2><h3 id="内存释放-shutdown-和-shutdownNow"><a href="#内存释放-shutdown-和-shutdownNow" class="headerlink" title="内存释放 shutdown 和 shutdownNow"></a>内存释放 shutdown 和 shutdownNow</h3><h3 id="InterruptException"><a href="#InterruptException" class="headerlink" title="InterruptException"></a>InterruptException</h3><h3 id="使用TimeUnit"><a href="#使用TimeUnit" class="headerlink" title="使用TimeUnit"></a>使用TimeUnit</h3><h3 id=""><a href="#" class="headerlink" title=" "></a> </h3></div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>未填之坑</tag>
      </tags>
  </entry>
  <entry>
    <title>Dockerfile 命令</title>
    <url>/2019/07/30/docker-instruction-commands/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<p>内容整理自<a href="https://legacy.gitbook.com/book/yeasy/docker_practice/details" target="_blank" rel="noopener">Docker入门到实践</a></p>
<h2 id="什么是Dockerfile"><a href="#什么是Dockerfile" class="headerlink" title="什么是Dockerfile"></a>什么是Dockerfile</h2><p>Dockerfile 是一种用来定制镜像的文本文件。我们可以在Dockerfile中指定基础镜像，执行命令，开放端口，设置环境变量等等。<br><strong>每一条指令</strong>会构建一层。参考<a href="https://docs.docker.com/storage/storagedriver/" target="_blank" rel="noopener">docker 文档</a>，docker的镜像具有下面的层结构(image layers)，增加层的厚度自然会增加镜像的大小，所以写Dockerfile时，尽量使用<code>\</code> 和 <code>&amp;&amp;</code> 巧妙地将命令的内容合并到一层。<br><a href="/2019/07/30/docker-instruction-commands/layer.png" class="gallery-item"><img src="/2019/07/30/docker-instruction-commands/layer.png" class="" title="layer"></a></p>
<p>比如<a href="https://legacy.gitbook.com/book/yeasy/docker_practice/details" target="_blank" rel="noopener">Docker入门到实践</a>中的例子，写一个构建<code>nginx</code>镜像的Dockerfile：</p>
<figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line"><span class="keyword">FROM</span> nginx</span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> <span class="built_in">echo</span> <span class="string">'&lt;h1&gt;Hello, Docker!&lt;/h1&gt;'</span> &gt; /usr/share/nginx/html/index.html</span></span><br></pre></td></tr></table></figure>
<p>这个Dockerfile包含了两个指令，<code>FROM</code>制定了基础镜像，基础镜像可以是各种服务类的官方镜像，也可以是干净的操作系统镜像，还可以是空白镜像<code>scratch</code>。<code>RUN</code>是最常用的指令，用来执行命令行命令。</p>
<p>接下来我们执行<br><figure class="highlight cmd"><table><tr><td class="code"><pre><span class="line">docker build -t nginx:v3 .</span><br></pre></td></tr></table></figure><br>来构建镜像，<code>it nginx:v3</code>的的意思是构建的目标名为nginx，标签为v3。</p>
<p>docker构建镜像的log将每一条指令分的很清楚，每一条指令作为一个step来执行，完成后我们就可以看到新的镜像<code>nginx:v3</code>了。</p>
<p>执行下面命令，通过新构建的镜像生成一个容器：<br><figure class="highlight cmd"><table><tr><td class="code"><pre><span class="line">docker run --name hello -d -p <span class="number">8080</span>:<span class="number">80</span> nginx:v3</span><br></pre></td></tr></table></figure><br>然后我们访问 <code>localhost:8080</code> 就可以看到欢迎页了：<br><a href="/2019/07/30/docker-instruction-commands/hello.png" class="gallery-item"><img src="/2019/07/30/docker-instruction-commands/hello.png" class="" title="hello"></a></p>
<h3 id="Dockerfile中用到的命令"><a href="#Dockerfile中用到的命令" class="headerlink" title="Dockerfile中用到的命令"></a>Dockerfile中用到的命令</h3><p>docker的指令一般有两种书写方式，一种是指令后跟命令行，一种是使用<code>[]</code>的函数调用写法，这里主要记录一下命令行写法。</p>
<ul>
<li><p>COPY<br><code>COPY &lt;context path&gt; &lt;target path&gt;</code><br>这个指令将上下文目录中的文件或者目录复制到新一层的目标路径中，要复制的文件或者目录可以有多个，可以使用通配符（符合golang的filepath.Match规则)，目标路径可以是绝对路径，也可以是工作目录的相对路径。<br>还可以加上<code>--chown=&lt;user&gt;:&lt;group&gt;</code>来改变文件的所属用户和所属组</p>
<blockquote>
<p>上下文路径？context path?</p>
</blockquote>
</li>
<li><p>ADD<br>使用方法跟COPY基本一样。功能会丰富一点，比如源路径可以设置为url，使用url路径时docker会讲文件打包下载到目标路径。因为需要多余的RUN来处理权限，解压缩，筛选内容，这个命令并不是很实用。</p>
</li>
<li><p>CMD<br>用于指定默认的容器主进程的启动命令。指令的写法也有两种：  </p>
<ul>
<li>shell 格式 <code>CMD 命令</code></li>
<li>exec 格式 <code>CMD [&quot;可执行文件&quot;, &quot;参数&quot;, ...&quot;参数&quot; ]</code>  </li>
</ul>
<p>使用shell格式写的内容会被解析为<code>CMD [&quot;sh&quot;, &quot;-c&quot;, &quot;命令&quot;]</code>，所以命令中可以使用环境变量，但是命令必须前台执行，如果命令结束后直接结束了<code>sh</code>程序，则容器也会推出。</p>
<p>Dockerfile中设置的启动命令可以在运行时重新指定，比如我们写<code>docker run -it imagename</code>的话，就会执行默认的命令，在镜像名后面添加命令，比如<code>cat etc/os-release</code>，就可以了。</p>
</li>
<li><p>ENTRYPOINT<br>跟CMD一样用来设置启动时的命令行和参数，不过在重新定义的时候，要在<code>docker run</code>后面使用命令<code>--entrypoint</code>来设置。<br>ENTRYPOINT和CMD共存的意义在于，定义了ENTRYPOINT之后，CMD的内容将不再被直接执行，而是作为参数传递给ENTRYPOINT。比如<a href="https://legacy.gitbook.com/book/yeasy/docker_practice/details" target="_blank" rel="noopener">Docker入门-Dockerfile指令详解-NETRYPOINT入口点</a>里面介绍的例子：</p>
<ol>
<li>如果我们想在执行的时候为<code>CMD [ &quot;curl&quot;, &quot;-s&quot;, &quot;https://ip.cn&quot; ]</code>添加参数，直接在镜像名后面加肯定是不行的，那样会替换掉所有命令（全部重写那就另当别论了），如果使用CMD来获取参数，定义<code>ENTRYPOINT [ &quot;curl&quot;, &quot;-s&quot;, &quot;https://ip.cn&quot; ]</code>，就可以实现参数的自由设置了。</li>
<li><p>用ENTRYPOINT来处理CMD做不到的工作。</p>
 <figure class="highlight docker"><table><tr><td class="code"><pre><span class="line"><span class="keyword">FROM</span> alpine:<span class="number">3.4</span></span><br><span class="line">...</span><br><span class="line"><span class="keyword">RUN</span><span class="bash"> addgroup -S redis &amp;&amp; adduser -S -G redis redis</span></span><br><span class="line">...</span><br><span class="line"><span class="keyword">ENTRYPOINT</span><span class="bash"> [<span class="string">"docker-entrypoint.sh"</span>]</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">EXPOSE</span> <span class="number">6379</span></span><br><span class="line"><span class="keyword">CMD</span><span class="bash"> [ <span class="string">"redis-server"</span> ]</span></span><br></pre></td></tr></table></figure>
<p> docker-entrypoint.sh的内容为：</p>
 <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="meta">#!/bin/sh</span></span><br><span class="line">...</span><br><span class="line"><span class="comment"># allow the container to be started with `--user`</span></span><br><span class="line"><span class="keyword">if</span> [ <span class="string">"<span class="variable">$1</span>"</span> = <span class="string">'redis-server'</span> -a <span class="string">"<span class="variable">$(id -u)</span>"</span> = <span class="string">'0'</span> ]; <span class="keyword">then</span></span><br><span class="line">    chown -R redis .</span><br><span class="line">    <span class="built_in">exec</span> su-exec redis <span class="string">"<span class="variable">$0</span>"</span> <span class="string">"<span class="variable">$@</span>"</span></span><br><span class="line"><span class="keyword">fi</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">exec</span> <span class="string">"<span class="variable">$@</span>"</span></span><br></pre></td></tr></table></figure>
<p> 这里将cmd作为参数获取的方法，在ENTRYPOINT中设置了一个脚本，通过参数的内容来区别启动用户的身份。</p>
</li>
</ol>
</li>
<li><p>ENV<br>定义环境变量，就这么简单。可以用<code>=</code>，可以用空格，可以用等号加空格一行定义一群，也可以用<code>\</code>加入换行。</p>
</li>
<li><p>ARG<br>定义参数。参数和环境变量不同的是，参数只在构建的时候有用，在容器运行时不会被引入容器内部。参数的定义可以在<code>docker build</code>时使用<code>--build-arg &lt;name&gt;=&lt;value&gt;</code>的格式来覆盖。</p>
</li>
<li><p>VOLUME<br>匿名卷</p>
</li>
<li><p>EXPOSE<br>暴露端口</p>
</li>
</ul>
<ul>
<li><p>WORKDIR<br>指定工作目录</p>
</li>
<li><p>USER<br>指定当前用户</p>
</li>
<li><p>HEALTHCHECK<br>健康检查</p>
</li>
<li><p>ONBUILD<br>构建时执行</p>
</li>
</ul>
<h2 id="镜像的使用"><a href="#镜像的使用" class="headerlink" title="镜像的使用"></a>镜像的使用</h2><h3 id="镜像的获取，管理和删除"><a href="#镜像的获取，管理和删除" class="headerlink" title="镜像的获取，管理和删除"></a>镜像的获取，管理和删除</h3><h3 id="commit-命令"><a href="#commit-命令" class="headerlink" title="commit 命令"></a>commit 命令</h3></div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
  </entry>
  <entry>
    <title>使用DB2的Docker镜像建立本地开发用的数据库</title>
    <url>/2019/07/23/docker-ibmdb2/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<p>这个是2019-07-23的笔记，DB2的社区免费版授权和镜像的使用方法随时可能改变。</p>
<h2 id="从哪儿找DB2"><a href="#从哪儿找DB2" class="headerlink" title="从哪儿找DB2"></a>从哪儿找DB2</h2><p>DB2的免费版本在 <a href="https://www.ibm.com/cloud/blog/announcements/ibm-db2-developer-community-edition" target="_blank" rel="noopener">2019年的社区版DB2</a> 中可以找到，提供了三种免费版的使用方法：</p>
<ol>
<li>使用docker镜像</li>
<li>下载DB2程序</li>
<li>IBM Cloud的DB2服务（免费套餐）</li>
</ol>
<p>这里记一下使用第一种方法Docker镜像的构建过程。</p>
<h2 id="开工"><a href="#开工" class="headerlink" title="开工"></a>开工</h2><h3 id="第一步，打开冰箱…-啊不，下载镜像，可以在docker-hub搜DB2，这篇笔记使用的是-ibmcom-db2"><a href="#第一步，打开冰箱…-啊不，下载镜像，可以在docker-hub搜DB2，这篇笔记使用的是-ibmcom-db2" class="headerlink" title="第一步，打开冰箱….啊不，下载镜像，可以在docker hub搜DB2，这篇笔记使用的是 ibmcom/db2"></a>第一步，打开冰箱….啊不，下载镜像，可以在docker hub搜DB2，这篇笔记使用的是 <a href="https://hub.docker.com/r/ibmcom/db2" target="_blank" rel="noopener">ibmcom/db2</a></h3><h3 id="第二步，创建容器，在控制台执行"><a href="#第二步，创建容器，在控制台执行" class="headerlink" title="第二步，创建容器，在控制台执行:"></a>第二步，创建容器，在控制台执行:</h3><figure class="highlight docker"><table><tr><td class="code"><pre><span class="line">docker <span class="keyword">run</span><span class="bash"> --name db2 --privileged -p 50000:50000 -e LICENSE=accept -e DB2INST1_PASSWORD=password -e DBNAME=MYDATABASE ibmcom/db2</span></span><br></pre></td></tr></table></figure>
<p>简单解释一下</p>
<ol>
<li><code>--name</code> 容器的名字，以后启动关闭的时候用 </li>
<li><code>--privileged</code> 不知道（弄懂了再写）</li>
<li><code>-p</code> 端口映射，这里的意思是本地的50000端口映射到容器的50000，这样我们就可以通过<code>localhost:50000</code>来访问容器里的数据库了</li>
<li><code>-e</code> 给容器里设置一些环境变量<ol>
<li><code>LICENSE</code> 嗯。。。</li>
<li><code>DB2INST1_PASSWORD</code> 为默认的用户名db2inst1设置的密码。</li>
<li><code>DBNAME</code> 直接使用变量作为名字建立一个database</li>
</ol>
</li>
<li>最后那个<code>ibmcom/db2</code>是镜像名</li>
</ol>
<h3 id="第三步，连接："><a href="#第三步，连接：" class="headerlink" title="第三步，连接："></a>第三步，连接：</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">url: jdbc:db2:&#x2F;&#x2F;localhost:50000&#x2F;MYDATABASE</span><br><span class="line">username: db2inst1</span><br><span class="line">password: password</span><br><span class="line">driver: db2jcc4.jar</span><br></pre></td></tr></table></figure>
<p>驱动文件可以在IBM的服务网页找到 <a href="http://www-01.ibm.com/support/docview.wss?uid=swg21363866" target="_blank" rel="noopener">db2jcc4.jar</a></p>
<h2 id="结束"><a href="#结束" class="headerlink" title="结束"></a>结束</h2><p>有时候会提示port无法使用，容器不能启动。先确定port50000没有被占用，如果可以使用，就重启docker。</p>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>Docker</tag>
        <tag>DB2</tag>
      </tags>
  </entry>
  <entry>
    <title>关于枚举型的一些理解</title>
    <url>/2019/07/17/enum-things/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<p>最近工作中写了一个枚举类，在code-review的时候被佐藤老师指摘了好几处，特别总结一下加深理解。</p>
<h2 id="什么是枚举型-Enum"><a href="#什么是枚举型-Enum" class="headerlink" title="什么是枚举型 Enum"></a>什么是枚举型 Enum</h2><p>Enum是Java 5开始引入的一个接口，关键字 <code>enum</code> 创建的类会自动使用Enum接口。  </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> WeekDay &#123;</span><br><span class="line">    MONDAY, TUESDAY, WEDNESDAY,</span><br><span class="line">    THURSDAY, FRIDAY, SATURDAY, SUNDAY</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>像这样定义了<code>WeekDay</code>之后，我们就不必为周一到周日创建整形或者字符串的静态变量了，可以直接把<code>WeekDay</code>中定义的七个名字当做基本型来使用: <code>WeekDay.MONDAY</code> , <code>WeekDay.TUESDAY</code> , <code>WeekDay.WEDNESDAY</code> ,  <code>WeekDay.THURSDAY</code> ,  <code>WeekDay.FRIDAY</code> ,  <code>WeekDay.SATURDAY</code> ,  <code>WeekDay.SUNDAY</code> 。 </p>
<p>枚举型的构造体是私有的，不可以使用构造体来构建枚举类的实体，但是枚举类中的元素是公开的，可以像基本型一样直接取来使用。<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">WorkDay day = WorkDat.SUNDAY;</span><br></pre></td></tr></table></figure></p>
<h2 id="用枚举类的思维方式来使用枚举类"><a href="#用枚举类的思维方式来使用枚举类" class="headerlink" title="用枚举类的思维方式来使用枚举类"></a>用枚举类的思维方式来使用枚举类</h2><p>引入枚举类的一个主要目的，是可以有一种类型安全，不可变的方法来定义常量。在引入枚举类之前，一般通过定义基本型的常量来表达一些固定的值。<br>比如<code>static final int MONDAY = 1;</code> 这种写法看似没什么问题，可没有什么实际意义的<code>1</code>在这里承担了很重要的职责，不免让人产生疑问，改成<code>0</code>可不可以呢？<br>而在判断是不是<code>MONDAY</code>时，又必须和<code>1</code>作比较，这就让程序逐渐变得复杂而又危险了。</p>
<p>引入枚举类之后，MONDAY本身就可以起到表达数值，作为判断条件的作用，从而减少了产生bug的可能性。<br>所以，像下面这种强行使用枚举类的<code>name</code> <code>toString</code>来判断对象是否为枚举类中的一个元素的写法，是很不符合枚举类的思维方式的，浪费了使用枚举类的好处。<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">String a = System.getEnv(“TODAY”)；</span><br><span class="line">String today = <span class="string">"Undefined"</span>;</span><br><span class="line"><span class="keyword">if</span>(WeekDay.MONDAY.name.equals(a))&#123;</span><br><span class="line">  today = WeekDay.MONDAY.name;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>我们可以给<code>WeekDay</code>添加一个方法来返回元素:<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> WeekDay <span class="title">as</span><span class="params">(String candidate)</span></span>&#123;</span><br><span class="line">  <span class="keyword">if</span>(MONDAY.name.equals(candidate))&#123;</span><br><span class="line">    <span class="keyword">return</span> MONDAY;</span><br><span class="line">  &#125;<span class="keyword">else</span> <span class="keyword">if</span>(...)&#123;</span><br><span class="line">    ...</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> UNDEFINED; <span class="comment">//添加一个未定义的元素</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这样我们在外面就可以告别那个中间的String，直接获取WeekDay的对象：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">WeekDay today = WeekDay.as(System.getEnv(<span class="string">"TODAY"</span>));</span><br><span class="line">If(today == WeekDay.UNDEFINED)&#123;</span><br><span class="line">  <span class="comment">// 对于未定义，空的设定值，或者默认值，可以在这里自由的设置</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span>(today == WeekDay.MONDAY)&#123;</span><br><span class="line">  <span class="comment">// 想干啥干啥咯</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h2 id="关联-使用私有构造体或者枚举类型来强化单例性"><a href="#关联-使用私有构造体或者枚举类型来强化单例性" class="headerlink" title="关联 [使用私有构造体或者枚举类型来强化单例性]"></a>关联 [使用私有构造体或者枚举类型来强化单例性]</h2></div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>Enum</tag>
      </tags>
  </entry>
  <entry>
    <title>区分 Authentication(AuthN) 和 Authorization(AuthZ)</title>
    <url>/2019/07/09/AuthN-vs-AuthZ/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<p>最近写的一段代码里，关于关键字<code>Auth</code>的使用在review的时候被佐藤老师批评了，一直以来对于认证和授权都没有好好的理解，在这里整理一下。</p>
<h2 id="别直接用Auth来命名"><a href="#别直接用Auth来命名" class="headerlink" title="别直接用Auth来命名"></a>别直接用Auth来命名</h2><p>之前对于<code>Auth</code>这个关键字的理解，一直都处于大概知道什么意思的模糊状态。其实使用<code>Auth</code>作为关键字来命名方法和变量，并不是很合适，因为<code>Authentication</code> 和 <code>Authorization</code> 的缩写都可以是<code>Auth</code>。  </p>
<p><code>Authentication</code> 的中文翻译是<code>认证</code>，日语翻译是<code>認証</code>，可以理解为判断<strong>你是不是谁</strong>的操作，比如输入用户名和密码，系统判断这个用户名存在，且密码匹配，就是认证。</p>
<p><code>Authorization</code> 的中文翻译是<code>授权</code>，日语翻译是<code>認可</code>, 我的理解是<strong>你可不可以</strong>的意思，例如用户名密码通过认证之后，系统判断该用户是否有访问权限，给与权限的操作，就是授权。</p>
<h2 id="401-和-403"><a href="#401-和-403" class="headerlink" title="401 和 403"></a>401 和 403</h2><p>HTTP的返回值401和403是一对很容易弄混的数字。</p>
<ul>
<li>401 Unauthorized</li>
<li>403 Forbidden</li>
</ul>
<p>401是发生用户认证错误时的返回值，而403是拒绝访问。那么结合上面对<code>Authentication</code>和<code>Authorization</code>的理解不难发现:</p>
<ul>
<li>401 虽然写着Unauthorized的名字，返回的却是Authentication的错误</li>
<li>403 则承担着返回Unauthorized错误的职责</li>
</ul>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
  </entry>
  <entry>
    <title>代码重构 - 改善方法的结构</title>
    <url>/2019/04/24/refactoring-C6/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<p>代码重构做的最多的就是改善方法的结构。去掉方法中不需要的元素，修改不明确的名称，将复杂而冗长的方法改成精简而明确的小方法群组，是重构方法的主要思路。  </p>
<p>方法重构用的最多的是【提取方法】，有种将一地零落的玩具分类整理装箱的感觉；而【方法内联】一般在方法提取的过于详细以至于产生了反作用，或者需要重新整理方法间关系时使用。  </p>
<p>【提取方法】的最大问题，是如何处理局部变量，这就用到了对局部变量的重构技巧。  </p>
<h2 id="提取方法-Extract-Method"><a href="#提取方法-Extract-Method" class="headerlink" title="提取方法 Extract Method"></a>提取方法 Extract Method</h2><p>如果发现方法内有一个代码块在很具体的做一件事儿，或者有一句漂亮的注释解释了接下来好几行的操作，那就试试把它提取成一个方法吧，然后取个足以说明其作用的方法名。比如：  </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">printOwing</span><span class="params">(<span class="keyword">double</span> amount)</span> </span>&#123;</span><br><span class="line">    printBanner();</span><br><span class="line"></span><br><span class="line">    <span class="comment">//print details</span></span><br><span class="line">    System.out.println (<span class="string">"name:"</span> + _name);</span><br><span class="line">    System.out.println (<span class="string">"amount"</span> + amount);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>重构后是这个样子：  </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">printOwing</span><span class="params">(<span class="keyword">double</span> amount)</span> </span>&#123;</span><br><span class="line">    printBanner();</span><br><span class="line">    printDetails(amount);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">printDetails</span> <span class="params">(<span class="keyword">double</span> amount)</span> </span>&#123;</span><br><span class="line">    System.out.println (<span class="string">"name:"</span> + _name);</span><br><span class="line">    System.out.println (<span class="string">"amount"</span> + amount);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>漂亮的代码往往方法都很简短，有着意思明确的方法名。方法越简洁，被重复利用的机会就越大；方法名如果足以传达信息，又可以省去不少的注释。<br>方法的简洁程度和方法名的好坏是联系在一起的，提取方法可以看做是用方法名来取代原位置的代码块，如果不能通过方法名理解原本代码块要执行的操作，重构也就失去了意义。<br>重构的时候不用刻意去思考方法或者方法名的长度，重构的关键在于方法名和代码块之间的语义距离（semantic distance)。</p>
<h3 id="局部变量的处理"><a href="#局部变量的处理" class="headerlink" title="局部变量的处理"></a>局部变量的处理</h3><p>如果要提取的代码块使用了局部变量，提取方法的操作就要变得稍微复杂一些了。<br>首先我们需要观察一下局部变量定义、赋值以及被调用的位置，如果局部变量相关的代码可以移动位置，不妨先优化一下，因为优化前后，接下来的重构操作可能会有很大不同。</p>
<ol>
<li>如果局部变量足够“局部”，可以直接放到新方法内部，当然是最简单的情况。  </li>
<li>如果局部变量的赋值是在新方法的处理范围外，新方法只是调用了变量值而没有进行修改，我们可以把局部变量当做参数传达给新的方法。  </li>
<li>如果需要新方法的处理来给变量赋值，而变量值的使用又在新方法的范围外，则需要为新方法添加返回值，返回变量值。</li>
</ol>
<blockquote>
<p>想返回多个变量值咋整嘞？这里涉及到了[单一返回值]的问题。编程语言普遍使用的是单一返回值的方法结构，这样可以保证代码的可读性，避免混乱。如果遇到了需要返回多个值的情况，不妨试着细化方法的划分，使用多个单一返回值的方法来实现。</p>
</blockquote>
<p>栗子，重构下面代码：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">printOwing</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    Enumeration e = _orders.elements();</span><br><span class="line">    <span class="keyword">double</span> outstanding = <span class="number">0.0</span>;</span><br><span class="line"></span><br><span class="line">    printBanner();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// calculate outstanding</span></span><br><span class="line">    <span class="keyword">while</span> (e.hasMoreElements()) &#123;</span><br><span class="line">        Order each = (Order) e.nextElement();</span><br><span class="line">        outstanding += each.getAmount();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    printDetails(outstanding);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>书中直接进行了如下的重构:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">printOwing</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    printBanner();</span><br><span class="line">    <span class="keyword">double</span> outstanding = getOutstanding();</span><br><span class="line">    printDetails(outstanding);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">getOutstanding</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Enumeration e = _orders.elements();</span><br><span class="line">    <span class="keyword">double</span> outstanding = <span class="number">0.0</span>;</span><br><span class="line">    <span class="keyword">while</span> (e.hasMoreElements()) &#123;</span><br><span class="line">        Order each = (Order) e.nextElement();</span><br><span class="line">        outstanding += each.getAmount();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> outstanding;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>书中说将<code>outstanding</code>的计算提取成了一个独立的方法，而前面的局部变量因为只在计算中被使用到，所以一并提取了出来。这里有一个看似理所当然却值得做笔记的操作：作者并没有将<code>printBanner()</code>或者<code>printDetails()</code>放在新方法内。<br>如果将<code>pringDetails()</code>放到<code>getOutstanding()</code>里，不就不需要设返回值了？ 这是因为重构的对象是计算<code>outstanding</code>的代码块，如果将不相关的方法一并放到新方法内，就偏离了重构的目的。<br>假设将<code>printDetails()</code>放在了<code>getOutstanding()</code>内，再看<code>printOwing()</code>会发现，可以获取的信息变少了，<code>printDetails()</code>这个操作被隐藏在了<code>getOutStanding</code>里，而这并不在我们重构的计划内。<br>提取方法的重构目的，是用简明的方法调用来代替具体的代码块，提高原位置代码的可读性。如果将代码的方法调用看做是一个树状结构，提取方法就是在增加分支的深度。我们在读代码的时候，并不能看到下层方法的内容，所以将原本需要在上层直接读到的内容放在了很深的位置，反而会降低代码的可读性。</p>
<h2 id="方法内联-Inline-Method"><a href="#方法内联-Inline-Method" class="headerlink" title="方法内联 Inline Method"></a>方法内联 Inline Method</h2><p>重构的一个重要目的是用简洁明了的方法名来替代代码原本所在的位置，以提高可读性。但有时会发现一些方法的内容已经简洁的跟方法名不相上下，这时就可以考虑舍弃方法了。  </p>
<p>比如下面代码，<code>moreThanFiveLateDeliveries</code>和<code>_numberOfLateDeliveries &gt; 5</code>几乎没什么区别，也就没有必要留着方法了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">getRating</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (moreThanFiveLateDeliveries()) ? <span class="number">2</span> : <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">boolean</span> <span class="title">moreThanFiveLateDeliveries</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> _numberOfLateDeliveries &gt; <span class="number">5</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">getRating</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (_numberOfLateDeliveries &gt; <span class="number">5</span>)? <span class="number">2</span> : <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="内联局部变量"><a href="#内联局部变量" class="headerlink" title="内联局部变量"></a>内联局部变量</h2><p>如果有一个局部变量像下面的<code>basePrice</code>一样只做了一次很简单的赋值操作，又没有被多个位置引用，那就没必要留着它了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">double</span> basePrice = anOrder.basePrice();</span><br><span class="line"><span class="keyword">return</span> (basePrice &gt; <span class="number">1000</span>)</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">return</span> (anOrder.basePrice() &gt; <span class="number">1000</span>)</span><br></pre></td></tr></table></figure>
<h2 id="使用查找方法替代局部变量"><a href="#使用查找方法替代局部变量" class="headerlink" title="使用查找方法替代局部变量"></a>使用查找方法替代局部变量</h2><p>有时我们为了重复使用某一个表达式的结果，会将其保存在局部变量中，但是局部变量的访问有范围限制，想要使用该变量，就需要在同一个方法内，结果导致方法过长，过长的方法往往有着复杂的结构而又不好重构。如果使用查找方法来替代局部变量，便可以摆脱局部变量的范围限制，在类的各个地方都能使用表达式的结果，重构的时候也就少了很多顾虑。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">double</span> basePrice = _quantity * _itemPrice;</span><br><span class="line"><span class="keyword">if</span> (basePrice &gt; <span class="number">1000</span>)</span><br><span class="line">    <span class="keyword">return</span> basePrice * <span class="number">0.95</span>;</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">    <span class="keyword">return</span> basePrice * <span class="number">0.98</span>;</span><br></pre></td></tr></table></figure>
<p>将<code>basePrice</code>提取成为一个方法：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (basePrice() &gt; <span class="number">1000</span>)</span><br><span class="line">    <span class="keyword">return</span> basePrice() * <span class="number">0.95</span>;</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">    <span class="keyword">return</span> basePrice() * <span class="number">0.98</span>;</span><br><span class="line">...</span><br><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">basePrice</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> _quantity * _itemPrice;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h2 id="引入解释用变量"><a href="#引入解释用变量" class="headerlink" title="引入解释用变量"></a>引入解释用变量</h2><p>比如下面代码<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> ( (platform.toUpperCase().indexOf(<span class="string">"MAC"</span>) &gt; -<span class="number">1</span>)&amp;&amp;</span><br><span class="line">      (browser.toUpperCase().indexOf(<span class="string">"IE"</span>) &gt; -<span class="number">1</span>)&amp;&amp;</span><br><span class="line">       wasInitialized() &amp;&amp; resize &gt; <span class="number">0</span> )</span><br><span class="line"> &#123;</span><br><span class="line">   <span class="comment">// do something</span></span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure><br>条件语句的表达式非常复杂，难以理解，可以引入解释变量，提高条件语句的可读性：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">final</span> <span class="keyword">boolean</span> isMacOs     = platform.toUpperCase().indexOf(<span class="string">"MAC"</span>) &gt; -<span class="number">1</span>;</span><br><span class="line"><span class="keyword">final</span> <span class="keyword">boolean</span> isIEBrowser = browser.toUpperCase().indexOf(<span class="string">"IE"</span>)  &gt; -<span class="number">1</span>;</span><br><span class="line"><span class="keyword">final</span> <span class="keyword">boolean</span> wasResized  = resize &gt; <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (isMacOs &amp;&amp; isIEBrowser &amp;&amp; wasInitialized() &amp;&amp; wasResized) &#123;</span><br><span class="line">    <span class="comment">// do something</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>不难发现，这个重构操作对条件语句的重构有很不错的效果，但是会引入新的局部变量。我们完全可以通过提取方法来简化表达式，所以作者也有提到：一般在无法提取方法时，才会想起来引入解释变量。<br>有些方法由于使用了大量局部变量，使用提取方法来重构会很麻烦，这时引入解释变量可能有助于梳理算法结构，对进一步的重构有很大帮助。</p>
<h2 id="分割局部变量"><a href="#分割局部变量" class="headerlink" title="分割局部变量"></a>分割局部变量</h2><p>我们可以这样理解:局部变量在方法中的作用大致有两种，</p>
<ol>
<li>一种是在循环的处理中，用来当做index或者flag。</li>
<li>另一种是用来保存值或参照，方便多次的使用，这种局部变量应该只能被赋值一次。<br>如果一个局部变量承担了超出上述范围的责任，就该考虑分割它了。让一个局部变量承担多种责任，会降低代码的可读性，造成混乱。所以遇到这种情况，尽可能一个变量一个责任的进行分割。分割之后，再考虑其他的重构操作。比如：</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">double</span> temp = <span class="number">2</span> * (_height + _width);</span><br><span class="line">System.out.println (temp);</span><br><span class="line">temp = _height * _width;</span><br><span class="line">System.out.println (temp);</span><br></pre></td></tr></table></figure>
<p><code>temp</code>两次每调用时，保存的内容不一样，所以应该分割为两个变量：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">final</span> <span class="keyword">double</span> perimeter = <span class="number">2</span> * (_height + _width);</span><br><span class="line">System.out.println (perimeter);</span><br><span class="line"><span class="keyword">final</span> <span class="keyword">double</span> area = _height * _width;</span><br><span class="line">System.out.println (area);</span><br></pre></td></tr></table></figure>
<h2 id="移除对参数的赋值操作"><a href="#移除对参数的赋值操作" class="headerlink" title="移除对参数的赋值操作"></a>移除对参数的赋值操作</h2><p><strong>参数可以引用，修改，但不可以直接使用 <code>=</code> 赋值 </strong><br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">aMethod</span><span class="params">(Object foo)</span> </span>&#123;</span><br><span class="line">foo.modifyInSomeWay();           <span class="comment">// √ 这个可以</span></span><br><span class="line">foo = anotherObject;             <span class="comment">// × 这个不行</span></span><br></pre></td></tr></table></figure><br>首先要弄懂赋值的概念，使用<code>=</code>的操作，其作用不是修改变量的值，而是改变了该变量名所参照的对象。在方法中改变参数的参照（进行赋值操作），我的理解是有两个问题：</p>
<ol>
<li>如果想通过方法的调用来改变参数的参照对象，可以通过返回值来进行赋值，直接在方法中使用<code>=</code>，调用方法的地方看不到该赋值操作，会降低代码的可读性。</li>
<li>如果是<strong>Pass By Value</strong>的编程语言，改变参数的参照（Reference），并不会影响到函数外的原变量。这就造成了混乱。</li>
</ol>
<h3 id="Pass-By-Value-amp-amp-Pass-By-Reference"><a href="#Pass-By-Value-amp-amp-Pass-By-Reference" class="headerlink" title="Pass By Value &amp;&amp; Pass By Reference"></a>Pass By Value &amp;&amp; Pass By Reference</h3><p>复习一下值传递和引用传递</p>
<ul>
<li>值传递<ul>
<li>方法的参数是变量值的拷贝，在方法内修改参数值不会影响方法外变量的值</li>
</ul>
</li>
<li>引用传递<ul>
<li>方法的参数时变量的地址，在方法内修改参数会直接影响该内存地址的内容，方法外变量的值也会改变。</li>
</ul>
</li>
</ul>
<p>Java是值传递的编程语言，对于基本数据类型的值传递很好理解，对于引用类型的参数，<a href="https://blog.csdn.net/javazejian/article/details/51192130" target="_blank" rel="noopener">java基本数据类型传递与引用传递区别</a> 这篇文章里有个图很有助于理解</p>

<p>方法的代码如下<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.zejian.test;</span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * java中的按值调用</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> zejian</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CallByValue</span> </span>&#123;</span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">static</span> User user=<span class="keyword">null</span>;</span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">updateUser</span><span class="params">(User student)</span></span>&#123;</span><br><span class="line">		student.setName(<span class="string">"Lishen"</span>);</span><br><span class="line">		student.setAge(<span class="number">18</span>);</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	</span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">		user = <span class="keyword">new</span> User(<span class="string">"zhangsan"</span>,<span class="number">26</span>);</span><br><span class="line">		System.out.println(<span class="string">"调用前user的值："</span>+user.toString());</span><br><span class="line">		updateUser(user);</span><br><span class="line">		System.out.println(<span class="string">"调用后user的值："</span>+user.toString());</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>我们可以把引用类型的对象看作是一个值，引用类型的参数所传递的，是这个对象（图中的user），这就是引用类型的值传递。<br>通过<code>student</code>调用对象的方法是可以修改<code>user</code>内容的，但如果使用<code>=</code>来对<code>student</code>进行赋值，只是让<code>student</code>指向了一个新的对象，并不会影响到<code>user</code>。为了防止参数赋值造成混乱，Java其实可以将参数设置为<code>final</code>，只不过好像没怎么见谁用过…</p>
<h2 id="把方法换成对象"><a href="#把方法换成对象" class="headerlink" title="把方法换成对象"></a>把方法换成对象</h2><p>如果一个方法中有太多的局部变量，以至于无法通过提取方法进行重构（可是不进行重构方法又太丑陋了），不妨把方法整体提取为一个独立的对象，局部变量变为该对象的属性后，就可以在对象内轻松地提取方法进行下一步的重构了。</p>
<p>书中例子：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Class Account</span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">gamma</span> <span class="params">(<span class="keyword">int</span> inputVal, <span class="keyword">int</span> quantity, <span class="keyword">int</span> yearToDate)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> importantValue1 = (inputVal * quantity) + delta();</span><br><span class="line">        <span class="keyword">int</span> importantValue2 = (inputVal * yearToDate) + <span class="number">100</span>;</span><br><span class="line">        <span class="keyword">if</span> ((yearToDate - importantValue1) &gt; <span class="number">100</span>)</span><br><span class="line">            importantValue2 -= <span class="number">20</span>;</span><br><span class="line">        <span class="keyword">int</span> importantValue3 = importantValue2 * <span class="number">7</span>;</span><br><span class="line">        <span class="comment">// and so on.</span></span><br><span class="line">        <span class="keyword">return</span> importantValue3 - <span class="number">2</span> * importantValue1;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><br>上面代码中，如果想要将计算返回值的部分提取出来，就需要将好多局部变量作为参数传递，非常复杂。我们可以使用对象来替代方法，首先写一个<code>Gamma</code>类：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Gamma</span>...</span></span><br><span class="line"><span class="class">    <span class="title">private</span> <span class="title">final</span> <span class="title">Account</span> <span class="title">_account</span></span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> inputVal;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> quantity;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> yearToDate;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> importantValue1;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> importantValue2;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> importantValue3;</span><br></pre></td></tr></table></figure><br>再为<code>Gamma</code>类添加一个构造体：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Gamma (Account source, <span class="keyword">int</span> inputValArg, <span class="keyword">int</span> quantityArg, <span class="keyword">int</span> yearToDateArg) &#123;</span><br><span class="line">        _account = source; <span class="comment">// 为了使用方法delta()</span></span><br><span class="line">        inputVal = inputValArg;</span><br><span class="line">        quantity = quantityArg;</span><br><span class="line">        yearToDate = yearToDateArg;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><br>然后将原方法的处理内容转移到<code>Gamma</code>中，作为一个待重构的方法：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">compute</span> <span class="params">()</span> </span>&#123;</span><br><span class="line">        importantValue1 = (inputVal * quantity) + _account.delta();</span><br><span class="line">        importantValue2 = (inputVal * yearToDate) + <span class="number">100</span>;</span><br><span class="line">        <span class="keyword">if</span> ((yearToDate - importantValue1) &gt; <span class="number">100</span>)</span><br><span class="line">            importantValue2 -= <span class="number">20</span>;</span><br><span class="line">        <span class="keyword">int</span> importantValue3 = importantValue2 * <span class="number">7</span>;</span><br><span class="line">        <span class="comment">// and so on.</span></span><br><span class="line">        <span class="keyword">return</span> importantValue3 - <span class="number">2</span> * importantValue1;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><br>这时，由于局部变量都已经变成了<code>Gamma</code>类的属性，我们提取方法时不需要再担心局部变量了：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">compute</span> <span class="params">()</span> </span>&#123;</span><br><span class="line">       importantValue1 = (inputVal * quantity) + _account.delta();</span><br><span class="line">       importantValue2 = (inputVal * yearToDate) + <span class="number">100</span>;</span><br><span class="line">       importantThing();</span><br><span class="line">       <span class="keyword">int</span> importantValue3 = importantValue2 * <span class="number">7</span>;</span><br><span class="line">       <span class="comment">// and so on.</span></span><br><span class="line">       <span class="keyword">return</span> importantValue3 - <span class="number">2</span> * importantValue1;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="function"><span class="keyword">void</span> <span class="title">importantThing</span><span class="params">()</span> </span>&#123;</span><br><span class="line">       <span class="keyword">if</span> ((yearToDate - importantValue1) &gt; <span class="number">100</span>)</span><br><span class="line">            importantValue2 -= <span class="number">20</span>;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure></p>
<h2 id="优化算法"><a href="#优化算法" class="headerlink" title="优化算法"></a>优化算法</h2><p>书中用的是<strong>substitue algorithm</strong>, 意思是用简明易懂的写法来替代旧算法，并没有追求提高算法效率或者降低消耗，我在理解的时候把这个重构操作也归类为<strong>优化</strong>  </p>
<p>意思很简单：即使做的还是那些事儿，如果可以让你的算法看起来更容易理解，Just do it.</p>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>Refactoring</tag>
        <tag>重构</tag>
      </tags>
  </entry>
  <entry>
    <title>代码重构 - 简化条件语句</title>
    <url>/2019/04/22/C9/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<blockquote>
<p>原文 <a href="https://books.google.co.jp/books/about/Refactoring.html?id=HmrDHwgkbPsC&amp;printsec=frontcover&amp;source=kp_read_button&amp;redir_esc=y#v=onepage&amp;q&amp;f=false" target="_blank" rel="noopener">publisher logo Refactoring: Improving the Design of Existing Code</a> Chapter 9 Simplifying Conditianal Expressions</p>
</blockquote>
<h2 id="分离条件-Decompose-Conditional"><a href="#分离条件-Decompose-Conditional" class="headerlink" title="分离条件 - Decompose Conditional"></a>分离条件 - Decompose Conditional</h2><p>很多程序之所以“复杂难懂”，是因为出现了“复杂难懂”的条件语句。当人们看到条件语句时，最想获取的信息是：if XXX 的时候，do YYY； else do ZZZ。所以简化条件语句最简单直接的方法就是把长长的判断语句或者大段大段的处理分离出来，留下最基本的<code>if else</code>结构，一目了然，至于具体的判断方法和处理内容，则去具体的方法中查看。</p>
<blockquote>
<p>遇到有嵌套结构的条件语句时，不妨先试试先整理成卫语句(Guard Clauses)，行不通再进行分解处理。</p>
</blockquote>
<p>比如我们遇到下面这段代码：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (date.before (SUMMER_START) || date.after(SUMMER_END))&#123;</span><br><span class="line">  charge = quantity * winterRate + winterServiceCharge;</span><br><span class="line">&#125;<span class="keyword">else</span>&#123;</span><br><span class="line">  charge = quantity * summerRate;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>对条件和判断后的代码分别施放<strong>Extract Method</strong>法术:<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (notSummer(date))&#123;</span><br><span class="line">  charge = winterCharge(quantity);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">  charge = summerCharge (quantity);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">boolean</span> <span class="title">notSummer</span><span class="params">(Date date)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> date.before (SUMMER_START) || date.after(SUMMER_END);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">double</span> <span class="title">summerCharge</span><span class="params">(<span class="keyword">int</span> quantity)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> quantity * summerRate;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">double</span> <span class="title">winterCharge</span><span class="params">(<span class="keyword">int</span> quantity)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> quantity * winterRate + winterServiceCharge;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>当然，每次抽离出方法的时候，都不要忘记进行测试，<strong>保证重构不会影响原本的处理结果</strong>，很多时候条件语句并不是十分复杂，看起来好像没有抽离成为方法的必要，这就要看条件语句读起来是否顺畅易懂了，如果可以把原本机器感十足的语句变得更像一段流畅直白的描述，条件语句的重构就是有意义的。</p>
<h2 id="整合条件-Consolidate-Conditional-Expression"><a href="#整合条件-Consolidate-Conditional-Expression" class="headerlink" title="整合条件 - Consolidate Conditional Expression"></a>整合条件 - Consolidate Conditional Expression</h2><p>有些条件语句的虽然有着不同的判断条件，却执行着相同的动作。如果可以确定这些判断不需要完全的独立，不妨整合到一起，再抽离出一个方法，这样，条件语句就更有“一个判断”的感觉了。</p>
<p>比如下面的代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">disabilityAmount</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (_seniority &lt; <span class="number">2</span>) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">if</span> (_monthsDisabled &gt; <span class="number">12</span>) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">if</span> (_isPartTime) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">  <span class="comment">// compute the disability amount</span></span><br></pre></td></tr></table></figure>
<p>可以整合成：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">disabilityAmount</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (isNotEligableForDisability()) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">  <span class="comment">// compute the disability amount</span></span><br></pre></td></tr></table></figure></p>
<p>整合的对象不仅仅是同等级的条件语句(使用ors），有一些嵌套的条件语句更有整合的必要（使用ands）：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (onVacation())</span><br><span class="line">    <span class="keyword">if</span> (lengthOfService() &gt; <span class="number">10</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line"><span class="keyword">return</span> <span class="number">0.5</span>;</span><br></pre></td></tr></table></figure>
<p>如果内层的<code>if</code>只是起到筛选的作用，而没有产生分支，就可以将内层条件语句跟外层整合到一起：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (onVacation() &amp;&amp; lengthOfService() &gt; <span class="number">10</span>) <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line"><span class="keyword">else</span> <span class="keyword">return</span> <span class="number">0.5</span>;</span><br></pre></td></tr></table></figure></p>
<p>如果条件语句的处理只是返回不同的值，不妨使用<strong>三元运算符(ternary operator)</strong> 。不过要注意，三元运算符的使用因人而异，有些项目并不喜欢代码里出现三元运算符。<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">return</span> (onVacation() &amp;&amp; lengthOfService() &gt; <span class="number">10</span>) ? <span class="number">1</span> : <span class="number">0.5</span>;</span><br></pre></td></tr></table></figure></p>
<h2 id="整合重复的条件语句块-Consolidate-Duplicate-Conditional-Fragments"><a href="#整合重复的条件语句块-Consolidate-Duplicate-Conditional-Fragments" class="headerlink" title="整合重复的条件语句块 Consolidate Duplicate Conditional Fragments"></a>整合重复的条件语句块 Consolidate Duplicate Conditional Fragments</h2><p>当条件语句的<code>if</code>和<code>else</code>里出现了相同的处理时，不妨试试将其移动到条件语句之前或者之后，使条件语句中的处理更有“分歧”的感觉。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (isSpecialDeal()) &#123;</span><br><span class="line">    total = price * <span class="number">0.95</span>;</span><br><span class="line">    send();</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> &#123;</span><br><span class="line">    total = price * <span class="number">0.98</span>;</span><br><span class="line">    send();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>send()</code>出现在具有分歧的处理之后，可以移动到条件语句的后面；如果出现在具有分歧的处理之前，则移动到条件语句的前面。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (isSpecialDeal())</span><br><span class="line">    total = price * <span class="number">0.95</span>;</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">    total = price * <span class="number">0.98</span>;</span><br><span class="line">send();</span><br></pre></td></tr></table></figure>
<p>如果无法确定整合的内容在前还是应该在后，不妨先看一看在条件语句块内，该内容的位置变化是否影响处理结果，<br>在不影响处理结果的前提下将要整合的内容前移或者后撤之后，再进行重构就可以了。</p>
<h2 id="移除控制标记-Remove-Control-Flag"><a href="#移除控制标记-Remove-Control-Flag" class="headerlink" title="移除控制标记 Remove Control Flag"></a>移除控制标记 Remove Control Flag</h2><p>如果你使用一个变量的值控制真假的判断，不妨考虑在变量值的修改处用<code>break</code>或者<code>return</code>来替代。</p>
<p>比如下面的代码<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">checkSecurity</span><span class="params">(String[] people)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">boolean</span> found = <span class="keyword">false</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; people.length; i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (! found) &#123;</span><br><span class="line">           <span class="keyword">if</span> (people[i].equals (<span class="string">"Don"</span>))&#123;</span><br><span class="line">             sendAlert();</span><br><span class="line">             found = <span class="keyword">true</span>;</span><br><span class="line">           &#125;</span><br><span class="line">           <span class="keyword">if</span> (people[i].equals (<span class="string">"John"</span>))&#123;</span><br><span class="line">             sendAlert();</span><br><span class="line">             found = <span class="keyword">true</span>;</span><br><span class="line">           &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>这段代码使用变量<code>found</code>来判断来逃离<code>for</code>循环，显然可以使用<code>break</code>来代替：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">checkSecurity</span><span class="params">(String[] people)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; people.length; i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (people[i].equals (<span class="string">"Don"</span>))&#123;</span><br><span class="line">          sendAlert();</span><br><span class="line">          <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (people[i].equals (<span class="string">"John"</span>))&#123;</span><br><span class="line">          sendAlert();</span><br><span class="line">          <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这样不仅是代码看起来简洁了许多，也避免了后续得无意义的判断。当然，这里的条件语句还可以整合一下，就不细说了。  </p>
<p>有些时候控制标记不仅仅是用来充当真假的判断条件，也用来保存返回值，这种情况可以考虑在适当的时候使用<code>return</code>直接结束方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">checkSecurity</span><span class="params">(String[] people)</span> </span>&#123;</span><br><span class="line">    String found = <span class="string">""</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; people.length; i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (found.equals(<span class="string">""</span>)) &#123;</span><br><span class="line">           <span class="keyword">if</span> (people[i].equals (<span class="string">"Don"</span>))&#123;</span><br><span class="line">              sendAlert();</span><br><span class="line">              found = <span class="string">"Don"</span>;</span><br><span class="line">           &#125;</span><br><span class="line">           <span class="keyword">if</span> (people[i].equals (<span class="string">"John"</span>))&#123;</span><br><span class="line">              sendAlert();</span><br><span class="line">              found = <span class="string">"John"</span>;</span><br><span class="line">           &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    someLaterCode(found);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>我们可以先把<code>for</code>循环的内容抽离出来：  </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">checkSecurity</span><span class="params">(String[] people)</span> </span>&#123;</span><br><span class="line">    String found = foundMiscreant(people);</span><br><span class="line">    someLaterCode(found);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function">String <span class="title">foundMiscreant</span><span class="params">(String[] people)</span></span>&#123;</span><br><span class="line">    String found = <span class="string">""</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; people.length; i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (found.equals(<span class="string">""</span>)) &#123;</span><br><span class="line">           <span class="keyword">if</span> (people[i].equals (<span class="string">"Don"</span>))&#123;</span><br><span class="line">             sendAlert();</span><br><span class="line">             found = <span class="string">"Don"</span>;</span><br><span class="line">           &#125;</span><br><span class="line">           <span class="keyword">if</span> (people[i].equals (<span class="string">"John"</span>))&#123;</span><br><span class="line">             sendAlert();</span><br><span class="line">             found = <span class="string">"John"</span>;</span><br><span class="line">           &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> found;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>可以看到标记<code>found</code>成为了返回值，程序的意思很明显是：[如果<code>found</code>不是空的，就返回其内容], 所以我们可以直接返回要赋给<code>found</code>的值，取消<code>found</code>这个变量。 </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function">String <span class="title">foundMiscreant</span><span class="params">(String[] people)</span></span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; people.length; i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (people[i].equals (<span class="string">"Don"</span>))&#123;</span><br><span class="line">           sendAlert();</span><br><span class="line">           <span class="keyword">return</span> <span class="string">"Don"</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (people[i].equals (<span class="string">"John"</span>))&#123;</span><br><span class="line">           sendAlert();</span><br><span class="line">           <span class="keyword">return</span> <span class="string">"John"</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">""</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>细心观察 return 的位置， 你会有意想不到的收获</p>
</blockquote>
<h2 id="使用卫语句替代嵌套的条件语句-Replace-Nested-Conditional-with-Guard-Clauses"><a href="#使用卫语句替代嵌套的条件语句-Replace-Nested-Conditional-with-Guard-Clauses" class="headerlink" title="使用卫语句替代嵌套的条件语句 Replace Nested Conditional with Guard Clauses"></a>使用卫语句替代嵌套的条件语句 Replace Nested Conditional with Guard Clauses</h2><p>条件语句在程序中的使用可以有两种思路：</p>
<ol>
<li>用来处理明确的分支，程序往哪个方向走都有可能，完全取决于条件的真假。这种情况使用基本的<code>if else</code>来处理。</li>
<li>用来处理特殊情况，程序有着一个基本的前进方向，出现一些特殊情况时，需要立即返回特定值或者转为进行特定的处理。这种情况的条件语句，我们可以称之为 卫语句(guard clause)</li>
</ol>
<p>当我们遇到层层嵌套的<code>if else</code>语句时，可以梳理一下程序的语义，如果发现条件语句的作用不是用来产生“平等”的分支，而是一种“出现了这种情况就赶快退出去吧”的感觉的话，不妨将<code>if else</code>简化成<code>guard clause</code>.</p>
<blockquote>
<p>这里作者介绍的是 嵌套的条件语句 ，实际上使用卫语句的思路在单一<code>if-else</code>时也适用，每当我们写出一个完全体的<code>if-else</code>时，都应该思考一下条件语句属于那种类型，很多时候<code>if-else</code>的分歧处理都可以用<code>if</code>的卫语句解决。</p>
</blockquote>
<p>比如下面的这段代码：  </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">getPayAmount</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">double</span> result;</span><br><span class="line">  <span class="keyword">if</span> (_isDead) result = deadAmount();</span><br><span class="line">  <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">if</span> (_isSeparated) result = separatedAmount();</span><br><span class="line">      <span class="keyword">else</span> &#123;</span><br><span class="line">          <span class="keyword">if</span> (_isRetired) result = retiredAmount();</span><br><span class="line">          <span class="keyword">else</span> result = normalPayAmount();</span><br><span class="line">      &#125;;</span><br><span class="line">  &#125;</span><br><span class="line"><span class="keyword">return</span> result;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>这种梯田式的条件语句，而且处理的对象都是返回值，很明显可以使用卫语句来简化，我们从外向里，一层层的将<code>result</code>的赋值改为<code>return</code>，最后可以得到：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">getPayAmount</span><span class="params">()</span> </span>&#123;  </span><br><span class="line">  <span class="keyword">if</span> (_isDead) <span class="keyword">return</span> deadAmount();  </span><br><span class="line">  <span class="keyword">if</span> (_isSeparated) <span class="keyword">return</span> separatedAmount();  </span><br><span class="line">  <span class="keyword">if</span> (_isRetired) <span class="keyword">return</span> retiredAmount();  </span><br><span class="line">  <span class="keyword">return</span> normalPayAmount();&#125;;</span><br></pre></td></tr></table></figure>
<p>使用卫语句进行重构并不局限于这一种情况，有时候我们可以发现条件语句筛选了部分情况来进行特定的处理，剩下的情况则返回默认值。这时我们可以考虑反转判断的条件式，来得到一个卫语句，今儿简化代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">double</span> <span class="title">getAdjustedCapital</span><span class="params">()</span> </span>&#123;  </span><br><span class="line">  <span class="keyword">double</span> result = <span class="number">0.0</span>;  </span><br><span class="line">  <span class="keyword">if</span> (_capital &gt; <span class="number">0.0</span>) &#123;    </span><br><span class="line">    <span class="keyword">if</span> (_intRate &gt; <span class="number">0.0</span> &amp;&amp; _duration &gt; <span class="number">0.0</span>) &#123;     </span><br><span class="line">      result = (_income / _duration) * ADJ_FACTOR;    </span><br><span class="line">    &#125;  </span><br><span class="line">  &#125;  </span><br><span class="line">  <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>书中作者给出了详细的重构过程，这里就不细说了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">double</span> <span class="title">getAdjustedCapital</span><span class="params">()</span> </span>&#123;    </span><br><span class="line">  <span class="keyword">if</span> (_capital &lt;= <span class="number">0.0</span>) <span class="keyword">return</span> <span class="number">0.0</span>;    </span><br><span class="line">  <span class="keyword">if</span> (_intRate &lt;= <span class="number">0.0</span> || _duration &lt;= <span class="number">0.0</span>) <span class="keyword">return</span> <span class="number">0.0</span>;    </span><br><span class="line">  <span class="keyword">return</span> (_income / _duration) * ADJ_FACTOR;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>工作中很常用到的一个思路就是，如果有一个大大的if框住了几乎整个方法的内容，一定要看看可不可以反转if的条件在最前面直接return，从{}中解放这段代码。</p>
</blockquote>
<h2 id="利用多态来重构条件语句-Replace-Conditional-with-Polymorphism"><a href="#利用多态来重构条件语句-Replace-Conditional-with-Polymorphism" class="headerlink" title="利用多态来重构条件语句 Replace Conditional with Polymorphism"></a>利用多态来重构条件语句 Replace Conditional with Polymorphism</h2><p>比如我们有下面这个结构, <code>Employee</code>的<code>type</code>为抽象类<code>EmployeeType</code>，被三个类实现，每个类返回的<code>typeCode</code>不同：</p>
<p><a href="https://www.plantuml.com/plantuml/svg/bP312i8m38RlVOgmanvsyEf19kBHaxv0tC8ej6bfCx6ClRkTOx0X2Bi4Glxl_vDKKJ18Put2PvI0jK1jC4QuMczS8deaJxq2wEVv09e5Mf9nSd4DxVPgK6fDZSGeyvZErdKiMGHByDlxe5yejCe2oMLXoArceW3q5kBcTwy_ozoGiTs8JkGT3KMBl2rr5a5Pdg0u5EkxZjDbLVRBx_u0" class="gallery-item"><img src="https://www.plantuml.com/plantuml/svg/bP312i8m38RlVOgmanvsyEf19kBHaxv0tC8ej6bfCx6ClRkTOx0X2Bi4Glxl_vDKKJ18Put2PvI0jK1jC4QuMczS8deaJxq2wEVv09e5Mf9nSd4DxVPgK6fDZSGeyvZErdKiMGHByDlxe5yejCe2oMLXoArceW3q5kBcTwy_ozoGiTs8JkGT3KMBl2rr5a5Pdg0u5EkxZjDbLVRBx_u0" /></a></p>
<p><code>Employee</code>的方法<code>payAmount()</code>的内容为：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">payAmount</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">switch</span> (getType()) &#123;</span><br><span class="line">        <span class="keyword">case</span> EmployeeType.ENGINEER:</span><br><span class="line">            <span class="keyword">return</span> _monthlySalary;</span><br><span class="line">        <span class="keyword">case</span> EmployeeType.SALESMAN:</span><br><span class="line">            <span class="keyword">return</span> _monthlySalary + _commission;</span><br><span class="line">        <span class="keyword">case</span> EmployeeType.MANAGER:</span><br><span class="line">            <span class="keyword">return</span> _monthlySalary + _bonus;</span><br><span class="line">        <span class="keyword">default</span>:</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException(<span class="string">"Incorrect Employee"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>因为<code>type</code>的其实是<code>EmployeeType</code>子类的属性，所以我们可以将这个方法移动到<code>EmployeeType</code>中，利用子类来实现不同的处理。首先，移动方法到<code>EmployeeType</code>里，由于使用了<code>Employee</code>才有的属性，所以我们需要将<code>employee</code>作为参数传递进去：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">payAmount</span><span class="params">(Employee emp)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">switch</span> (getTypeCode()) &#123;</span><br><span class="line">        <span class="keyword">case</span> ENGINEER:</span><br><span class="line">            <span class="keyword">return</span> emp.getMonthlySalary();</span><br><span class="line">        <span class="keyword">case</span> SALESMAN:</span><br><span class="line">            <span class="keyword">return</span> emp.getMonthlySalary() + emp.getCommission();</span><br><span class="line">        <span class="keyword">case</span> MANAGER:</span><br><span class="line">            <span class="keyword">return</span> emp.getMonthlySalary() + emp.getBonus();</span><br><span class="line">        <span class="keyword">default</span>:</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException(<span class="string">"Incorrect Employee"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>EmployeeType</code>是抽象类，所以我们要继续将方法<code>payAmout()</code>在具体的子类中进行实现，不过子类中就用不到<code>switch</code>了。在重构的过程中，我们可以在<code>EmployeeType</code>中添加例外处理，比如当我们在<code>Engineer</code>中实现了<code>payAmount()</code>后:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Engineer</span> ...</span></span><br><span class="line"><span class="class">  <span class="title">int</span> <span class="title">payAmount</span>(<span class="title">Employee</span> <span class="title">emp</span>) </span>&#123;      </span><br><span class="line">    <span class="keyword">return</span> emp.getMonthlySalary();  </span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">EmployeeType</span>...</span></span><br><span class="line"><span class="class">  <span class="title">int</span> <span class="title">payAmount</span>(<span class="title">Employee</span> <span class="title">emp</span>) </span>&#123;</span><br><span class="line">      <span class="keyword">switch</span> (getTypeCode()) &#123;</span><br><span class="line">          <span class="keyword">case</span> ENGINEER:</span><br><span class="line">             <span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException (<span class="string">"Should be being overridden"</span>);</span><br><span class="line">          <span class="keyword">case</span> SALESMAN:</span><br><span class="line">             <span class="keyword">return</span> emp.getMonthlySalary() + emp.getCommission();</span><br><span class="line">          <span class="keyword">case</span> MANAGER:</span><br><span class="line">             <span class="keyword">return</span> emp.getMonthlySalary() + emp.getBonus();</span><br><span class="line">          <span class="keyword">default</span>:</span><br><span class="line">             <span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException(<span class="string">"Incorrect Employee"</span>);</span><br><span class="line">      &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>
<p>如此下去，直到在所有的子类中都实现了<code>payAmount</code>之后，将<code>EmployeeType</code>的方法改为<code>abstract</code><br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">EmployeeType</span>...  </span></span><br><span class="line"><span class="class">  <span class="title">abstract</span> <span class="title">int</span> <span class="title">payAmount</span>(<span class="title">Employee</span> <span class="title">emp</span>)</span>;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Engineer</span> ...</span></span><br><span class="line"><span class="class">  <span class="title">int</span> <span class="title">payAmount</span>(<span class="title">Employee</span> <span class="title">emp</span>) </span>&#123;      </span><br><span class="line">    <span class="keyword">return</span> emp.getMonthlySalary();  </span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Salesman</span>...  </span></span><br><span class="line"><span class="class">  <span class="title">int</span> <span class="title">payAmount</span>(<span class="title">Employee</span> <span class="title">emp</span>) </span>&#123;      </span><br><span class="line">    <span class="keyword">return</span> emp.getMonthlySalary() + emp.getCommission();  </span><br><span class="line">  &#125; </span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Manager</span>...  </span></span><br><span class="line"><span class="class">  <span class="title">int</span> <span class="title">payAmount</span>(<span class="title">Employee</span> <span class="title">emp</span>) </span>&#123;      </span><br><span class="line">    <span class="keyword">return</span> emp.getMonthlySalary() + emp.getBonus();  </span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure></p>
<h2 id="使用空对象-Introduce-Null-Object"><a href="#使用空对象-Introduce-Null-Object" class="headerlink" title="使用空对象 Introduce Null Object"></a>使用空对象 Introduce Null Object</h2><blockquote>
<p>空对象模式是一种需要小心使用的设计模式，而且在JavaSE8中引入<code>Optional&lt;&gt;</code>后，几乎用不到空对象。</p>
</blockquote>
<h2 id="插入断言-Introduce-Assertion"><a href="#插入断言-Introduce-Assertion" class="headerlink" title="插入断言 Introduce Assertion"></a>插入断言 Introduce Assertion</h2><blockquote>
<p>善用 <code>Assert.isTrue()</code>， 确保一些必须成立的前提条件，从而让后面的条件判断达到最简化。</p>
</blockquote>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>未填之坑</tag>
        <tag>Refactoring</tag>
        <tag>重构</tag>
      </tags>
  </entry>
  <entry>
    <title>通过命令行从本地推送代码到GitHub</title>
    <url>/2019/04/15/git-connect/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<p>首先，Git和GitHub不是一个东西。Git是一个版本管理系统，而GitHub是一个用来保存软件代码的平台。我们不妨把git看作是通讯技术，而github只是<strong>一个</strong>通讯服务商。也就是说除了github之外，还有很多的代码托管平台，大家都是用的git来上传下载代码，只不过github名气最大。</p>
<ul>
<li>为了不受各种不同的软件影响，这里说的都是通过命令行操作.</li>
<li>这里只介绍一些最基本的命令和用法，在命令后面加<code>--help</code>可以查看详细的文档.</li>
</ul>
<h2 id="环境搭建"><a href="#环境搭建" class="headerlink" title="环境搭建"></a>环境搭建</h2><p>首先要做的事安装<code>git</code>，下载地址在<a href="https://git-scm.com/downloads" target="_blank" rel="noopener">这里</a>.</p>
<p>git安装完之后，打开命令行执行<code>git --version</code>看看是否可以查看git的版本，如果返回版本信息，就OK了. git会自己安装一个<code>git Bash</code>, 不过git的命令行并不局限于bash，windows下直接在cmd或者powershell都可以使用.</p>
<p>本地的代码推送到github时，github需要验证用户的信息. 就好比你可以每次进门都填一次我叫什么，我从哪儿来，我是干什么的，也可以提前登陆好身份信息，“哔”的一声刷脸进门，这个登录的身份信息就是<code>ssh key</code>.</p>
<p>接下来的操作分为三步：</p>
<ol>
<li><a href="https://help.github.com/en/enterprise/2.15/user/articles/checking-for-existing-ssh-keys" target="_blank" rel="noopener">确定本地是否已经有建立好的ssh key</a><ul>
<li>打开git Bash（这里需要用到Bash, windows的cmd是没有ls命令的) 输入<code>ls -al ~/.ssh</code> 这里一个搜索命令，返回名称为<code>.ssh</code>的目录信息。如果有<code>id_rsa.pub</code>文件，就说明已经有ssh key了.  </li>
</ul>
</li>
<li><a href="https://help.github.com/en/enterprise/2.15/user/articles/generating-a-new-ssh-key-and-adding-it-to-the-ssh-agent" target="_blank" rel="noopener">如果没有，建立一组新的ssh key</a><ul>
<li>执行命令<code>ssh-keygen -t rsa -b 4096 -C &quot;your_email@example.com&quot;</code>来生成一组ssh key，接下来直接按回车就可以了（文件位置使用默认位置不需要输入，passphrase默认空白，不设置，设置的话以后每次推送代码都要输密码）。</li>
<li>将生成的key登录到ssh-agent. 执行命令启动ssh-agent，windows：<code>eval $(ssh-agent -s)</code>  Mac或者Linux：<code>eval &quot;$(ssh-agent -s)&quot;</code>.然后执行命令添加key，Mac：<code>ssh-add -K ~/.ssh/id_rsa</code> windows或者Linux：<code>ssh-add ~/.ssh/id_rsa</code></li>
</ul>
</li>
<li><a href="https://help.github.com/en/enterprise/2.15/user/articles/adding-a-new-ssh-key-to-your-github-account" target="_blank" rel="noopener">将建立好的ssh key登录到github</a><ul>
<li>将ssh key的内容复制到剪贴板. window可以使用命令：<code>clip &lt; ~/.ssh/id_rsa.pub</code> Mac可以使用：<code>pbcopy &lt; ~/.ssh/id_rsa.pub</code> Linux可以使用：<code>sudo apt-get install xclip</code> 和 <code>xclip -sel clip &lt; ~/.ssh/id_rsa.pub</code></li>
</ul>
</li>
</ol>
<p>添加到github这一步我们单独说一下，以防弄错。打开github页面，点击用户头像，进入<code>setting</code>，在菜单中间的位置找到<code>SSH and GPG keys</code>，点击右上角的绿色按钮<code>New SSH key</code>，在<code>Title</code>里填写要添加的ssh key的介绍，比如”公司那台贼慢的电脑/家里的老爷机/学校的大屁股显示器”,然后在将复制的SSH key粘贴到<code>Key</code>里面，点击<code>Add SSH key</code>就可以了。</p>
<h2 id="克隆项目代码"><a href="#克隆项目代码" class="headerlink" title="克隆项目代码"></a>克隆项目代码</h2><p>我们来到<code>GitHub</code>的代码页，可以在代码的右上角看到一个绿色按钮<code>Clone or Download</code>，点开之后可以看到克隆代码用的链接。  </p>
<p>链接有两种，<code>https</code> 和 <code>ssh</code>， 简单来讲，使用https的链接克隆的项目需要用github的用户名密码来推送，使用ssh链接克隆的项目则使用ssh-key验证<br><a href="/2019/04/15/git-connect/usehttps.PNG" class="gallery-item"><img src="/2019/04/15/git-connect/usehttps.PNG" class="" title="https"></a></p>
<a href="/2019/04/15/git-connect/usessh.PNG" class="gallery-item"><img src="/2019/04/15/git-connect/usessh.PNG" class="" title="ssh"></a>
<p>由于我们已经设置好了ssh-key，所以使用格式为<code>git@github.com:用户名/项目名.github.io.git</code>的ssh链接.<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git clone git@github.com:用户名&#x2F;项目名.github.io.git</span><br></pre></td></tr></table></figure></p>
<h3 id="新建的repository"><a href="#新建的repository" class="headerlink" title="新建的repository"></a>新建的repository</h3><p>如果我们要将本地的工程文件推送到一个新建的GitHub库,首先确定两件事儿：</p>
<ol>
<li>GitHub上建立了一个空的库，README都没有的那种，可以看到这个页面 <a href="/2019/04/15/git-connect/new_repository.PNG" class="gallery-item"><img src="/2019/04/15/git-connect/new_repository.PNG" class="" title="NEW"></a></li>
<li>本地的工程文件初始化了git，并且没有关联其他的库. git的初始化可以使用<code>git init</code>命令，移除其他的关联，可以使用<code>git remote remove name</code> 这里的name一般是<code>origin</code>.</li>
</ol>
<p>然后执行下面的命令就可以了：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git commit -m &quot;first commit&quot;</span><br><span class="line">git remote add origin ssh链接</span><br><span class="line">git push -u origin master</span><br></pre></td></tr></table></figure></p>
<h2 id="remote-和-local"><a href="#remote-和-local" class="headerlink" title="remote 和 local"></a>remote 和 local</h2><p>git管理的代码有两个地方: </p>
<ol>
<li>一个是<strong>remote</strong> 可以理解为远端，也就是指托管代码的地方，使用github时，值得也就是github服务器端</li>
<li>一个是<strong>local</strong> 直译本地，意译本地，他也就是本地的意思</li>
</ol>
<p>从远端向本地同步代码，叫做<code>pull</code> 也就是拉，将本地代码同步到远端，叫做<code>push</code> 推送，简单而形象。</p>
<p>local值不需要，当然也不能修改，因为代码就在本地. remote的值可以随时添加，删除或者修改，remote是name和url成对设置的，一般默认的remote名是origin</p>
<ul>
<li><code>git remote</code> 确认remote，会返回remote的名称</li>
<li><code>git remote get-url remoteName</code> 获得remote的url</li>
<li><code>git remote set-url remoteName url</code> 设置remote的url</li>
</ul>
<h2 id="branch"><a href="#branch" class="headerlink" title="branch"></a>branch</h2><p>为了实现并行的开发，代码通过<code>branch</code>（分支）来进行管理，不妨branch理解成时间线、河流，可分成几个分支并行前进，也可以随时汇集到一起（只要内容没有冲突）。<br>可以通过<code>git branch</code>来查当前本地的分支。<br>切换分支的命令为 <code>git checkout &quot;branch_name&quot;</code><br>常用的生成分支命令为 <code>git checkout -b &quot;new_branch_name&quot; &quot;base_branch_name&quot;</code><br>使用<code>git branch &quot;new_branch_name</code>也可以生成新的分支，不过该命令并不切换到新生成的分支.</p>
<ul>
<li>分支的名称不可以有空格，所以一般通过下划线连接单词，当分支名含有特殊符号比如 # 时，需要在分支名前后加 “ 来防止控制台执行时的歧义。 </li>
</ul>
<h2 id="commit"><a href="#commit" class="headerlink" title="commit"></a>commit</h2><p>如果说branch是时间线，那么commit就是时间线上的存档点。刚刚创建或者修改完的文件，在git中并不算<strong>保存</strong>了，如果强行修改分支，这些内容就会丢失。将修改的内容保存到branch的记录中的操作，叫做<code>commit（提交）</code>. </p>
<p>执行commit之前，首先需要把准备提交的文件添加到提交名单，这个操作使用 <code>git add</code> 命令。<br>添加之前，可以通过<code>git status</code>命令来查看有哪些文件被修改了.</p>
<p>这里简单的从bash颜色上区分一下，<br>绿色的内容(Changes to be committed) 是已经准备好可以提交的内容，<br>红色的内容(Changes not staged for commit) 是还没有添加到提交列表的修改.</p>
<p>下面介绍几种简单常用的命令<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 将所有的修改添加到提交列表</span><br><span class="line">git add --all</span><br><span class="line"></span><br><span class="line"># 将指定文件添加到提交列表，多个文件的时候使用空格区分</span><br><span class="line">git add aaa.md</span><br><span class="line">git add bbb.md ccc.md path&#x2F;ddd.md</span><br><span class="line"></span><br><span class="line"># 可以使用正规表达来指定多个文件</span><br><span class="line">git add a*.md</span><br><span class="line">git add path&#x2F;\*.md</span><br></pre></td></tr></table></figure></p>
<p>文件添加完，就可以执行commit来提交了：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 提交的时候，commit信息是必须的，可以直接使用-m 来指定commit信息</span><br><span class="line">git commit -m &quot;在这里输入一条commit信息&quot;</span><br><span class="line"></span><br><span class="line"># 使用github的时候，可以在commit信息里通过 #+编号 来关联Issue，这样提交的commit可以在Issue中直接找到。</span><br><span class="line">git commit -m &quot;#001 commmit信息&quot;</span><br></pre></td></tr></table></figure></p>
<h2 id="push"><a href="#push" class="headerlink" title="push"></a>push</h2><p>将remote的最新内容同步到本地的命令是<code>git pull</code>，将本地的修改推送到remote的命令是<code>git push</code>.</p>
<ul>
<li>出现冲突的时候可以加上<code>--force</code>来强制推送，不过这个操作不推荐，推送代码尽可能在解决冲突之后，使用最简单流畅的<code>git push</code></li>
</ul>
<p>推送命令的对象是分支，所以当我们推送本地创建的新分支时，需要指定一下这个分支在remote的信息：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># git push 推送命令</span><br><span class="line"># --set-upstream 告诉git 我要设置推送目的地</span><br><span class="line"># remotename 要推送到的remote名 一般是 origin</span><br><span class="line"># branch_name 推送的branch名 含有特殊字符的话要用&quot;branch_name&quot;</span><br><span class="line">git push --set-upstream remotename branch_name</span><br></pre></td></tr></table></figure></p>
<ul>
<li>无论是切换分支，还是推送的时候，如果前面命令输入正确，git bash可以补全分支名。所以多用<code>tab</code>不仅方便，还能帮忙检查一下分支名前面的命令行有没有错。</li>
</ul>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>Git</tag>
        <tag>GitHub</tag>
      </tags>
  </entry>
  <entry>
    <title>python 中的字符串格式化</title>
    <url>/2019/04/12/python-f-strings/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<blockquote>
<p>原文： <a href="https://realpython.com/python-f-strings/" target="_blank" rel="noopener">python-f-string</a></p>
</blockquote>
<p>自<code>3.6</code>版本开始，python引入了一种叫做<code>f-strings</code>的写法，使用改写发可以极大地增强字符串的可读性（当然好处不止这些).<br>不过在学习<code>f-strings</code>之前，先看看以往的写法都有哪些。</p>
<h2 id="旧方法"><a href="#旧方法" class="headerlink" title="旧方法"></a>旧方法</h2><p>在<code>3.6</code>之前，我们常用的字符串格式化写法有两种：</p>
<ol>
<li>使用转移符 <code>%</code></li>
<li>使用字符串的格式化方法 <code>str.format()</code> </li>
</ol>
<h3 id="旧选项1"><a href="#旧选项1" class="headerlink" title="旧选项1 %"></a>旧选项1 <code>%</code></h3><p>跟其他编程语言一样，python也可以用<code>%</code>来把变量值插入到字符串中的对应位置。写法如下：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">name = <span class="string">'Tom'</span></span><br><span class="line">print(<span class="string">'Hello %s.'</span> % name)</span><br><span class="line"><span class="comment"># 输出结果: 'Hello Tom.'</span></span><br></pre></td></tr></table></figure><br>输出多个变量时，在<code>%</code>后使用括号将所有变量放到一起：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">name1 = <span class="string">'Tom'</span></span><br><span class="line">name2 = <span class="string">'Jerry'</span></span><br><span class="line">type1 = <span class="string">'cat'</span></span><br><span class="line">type2 = <span class="string">'god'</span></span><br><span class="line">print(<span class="string">'Hello %s, you are a %s.this is %s, a %s'</span> % (name1, type1, name2, type2))</span><br><span class="line"><span class="comment"># 输出结果: 'Hello Tom, you are a cat. this is Jerry, a god'</span></span><br></pre></td></tr></table></figure><br>但是转移符这种东西，一个不小心就容易写错。</p>
<h3 id="旧选项2-str-format"><a href="#旧选项2-str-format" class="headerlink" title="旧选项2 str.format()"></a>旧选项2 <code>str.format()</code></h3><p>从<code>2.6</code>开始，python为字符串添加了一个<code>format()</code>方法，可以将引入的参数按顺序替换到字符串中的<code>{}</code>处。例如：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">name1 = <span class="string">'Tom'</span></span><br><span class="line">name2 = <span class="string">'Jerry'</span></span><br><span class="line">type1 = <span class="string">'cat'</span></span><br><span class="line">type2 = <span class="string">'god'</span></span><br><span class="line">print(<span class="string">'Hello &#123;&#125;, you are a &#123;&#125;.this is &#123;&#125;, a &#123;&#125;'</span>.format(name1, type1, name2, type2))</span><br><span class="line"><span class="comment"># 输出结果: 'Hello Tom, you are a cat. this is Jerry, a god'</span></span><br></pre></td></tr></table></figure></p>
<p>使用<code>format()</code>你还可以自定义参数的插入顺序，例如：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">name1 = <span class="string">'Tom'</span></span><br><span class="line">name2 = <span class="string">'Jerry'</span></span><br><span class="line">type1 = <span class="string">'cat'</span></span><br><span class="line">type2 = <span class="string">'god'</span></span><br><span class="line">print(<span class="string">'Hello &#123;0&#125;, you are a &#123;2&#125;.this is &#123;1&#125;, a &#123;3&#125;'</span>.format(name1, name2, type1, type2))</span><br><span class="line"><span class="comment"># 输出结果: 'Hello Tom, you are a cat. this is Jerry, a god'</span></span><br></pre></td></tr></table></figure></p>
<p><code>format()</code>方法的功能还不止于此，既然<code>index</code>可以用来出入参数，那可不可以用参数名称呢？毕竟<code>0,1,2,3</code>的可读性有限。答案是可以：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">name1 = <span class="string">'Tom'</span></span><br><span class="line">name2 = <span class="string">'Jerry'</span></span><br><span class="line">type1 = <span class="string">'cat'</span></span><br><span class="line">type2 = <span class="string">'god'</span></span><br><span class="line">print(<span class="string">'Hello &#123;name_a&#125;, you are a &#123;type_a&#125;.this is &#123;name_b&#125;, a &#123;type_b&#125;'</span>.format(name_a = name1, name_b = name2, type_a = type1, type_b = type2))</span><br><span class="line"><span class="comment"># 输出结果: 'Hello Tom, you are a cat. this is Jerry, a god'</span></span><br></pre></td></tr></table></figure></p>
<p>既然可以这样写了，那干脆直接引入<code>dict</code>得了.这个也是可以的：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">character = &#123;</span><br><span class="line">  <span class="string">'name'</span> : <span class="string">'Tom'</span>,</span><br><span class="line">  <span class="string">'type'</span> : <span class="string">'cat'</span></span><br><span class="line">&#125;</span><br><span class="line">print(<span class="string">'Hello &#123;name&#125;, you are a &#123;type&#125;.'</span>.format(**character))</span><br><span class="line"><span class="comment"># 输出结果: 'Hello Tom, you are a cat.'</span></span><br></pre></td></tr></table></figure></p>
<p>这样可读性就非常好了，不过在<code>3.6</code>版本，更好的写法出现了。</p>
<h2 id="新方法"><a href="#新方法" class="headerlink" title="新方法"></a>新方法</h2><p>关于<code>f-string</code>的引入，可以参看<a href="https://www.python.org/dev/peps/pep-0498/" target="_blank" rel="noopener">PEP498</a>, 或者<a href="https://docs.python.org/3/reference/lexical_analysis.html#f-strings" target="_blank" rel="noopener">Python-doc</a>.  </p>
<p>写法非常简单，在字符串前加<code>f</code>或者<code>F</code>，在字符串中直接插入<code>{变量名}</code>，就可以了：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">name = <span class="string">'Tom'</span></span><br><span class="line">type = <span class="string">'cat'</span></span><br><span class="line">print(<span class="string">f'Hello <span class="subst">&#123;name&#125;</span>, you are a <span class="subst">&#123;type&#125;</span>.'</span>)</span><br><span class="line"><span class="comment"># 输出结果: 'Hello Tom, you are a cat.'</span></span><br></pre></td></tr></table></figure></p>
<p>在<code>{}</code>中不仅可以使用字符串，还可以使用运算公式，甚至调用方法：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">change_name</span><span class="params">(input)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> <span class="string">'Jerry'</span></span><br><span class="line">name = <span class="string">'Tom'</span></span><br><span class="line">print(<span class="string">f"Hello <span class="subst">&#123;change_name(name)&#125;</span>."</span>)</span><br><span class="line"><span class="comment"># 输出结果: 'Hello Jerry.'</span></span><br></pre></td></tr></table></figure>
<blockquote>
<p>当使用<code>object</code>时，f-string会默认调用<code>__str__()</code>方法，如果想调用<code>__repr__</code>，需要在对象名后面加<code>!r</code>：<br> <figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="string">f"<span class="subst">&#123;object&#125;</span>"</span></span><br><span class="line"><span class="comment"># 输出 object.__str__()的内容</span></span><br><span class="line"><span class="string">f"<span class="subst">&#123;object!r&#125;</span>"</span></span><br><span class="line"><span class="comment"># 输出 object.__repr__()的内容</span></span><br></pre></td></tr></table></figure></p>
</blockquote>
<p>最后一个考点：换行。在python中可以使用<code>&#39;&#39;&#39;</code>来输出带有换行的字符串，不过直接在前面加上f的结果并不怎么好看：<br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">message = <span class="string">f'''</span></span><br><span class="line"><span class="string">Hi,</span></span><br><span class="line"><span class="string">you are a </span></span><br><span class="line"><span class="string">Cat</span></span><br><span class="line"><span class="string">'''</span></span><br><span class="line"><span class="comment"># message : '\nHi, \nyou are a \nCat\n'</span></span><br></pre></td></tr></table></figure></p>
<p><code>f-string</code>的换行方法是，在每行的最后添加<code>\</code><br><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">message = <span class="string">f''</span>\</span><br><span class="line"><span class="string">f'Hi '</span>\</span><br><span class="line"><span class="string">f'you are a '</span>\</span><br><span class="line"><span class="string">f'Cat '</span></span><br><span class="line"><span class="comment"># message ： 'Hi you are a Cat'</span></span><br></pre></td></tr></table></figure></p>
<p><strong>注意</strong> 一般情况下<code>f-string</code>使用单引号和双引号没有区别，但比如引用字典，需要单引号来标记key时，f-stirng就需要使用双引号了，否则字符串中的单引号会被错误理解。</p>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>Python</tag>
        <tag>F-string</tag>
      </tags>
  </entry>
  <entry>
    <title>Hexo 说明文档笔记</title>
    <url>/2019/04/05/Hexo/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<ul>
<li><a href="https://hexo.io" target="_blank" rel="noopener">Hexo 文档</a></li>
</ul>
<h2 id="theme文件夹结构"><a href="#theme文件夹结构" class="headerlink" title="theme文件夹结构"></a>theme文件夹结构</h2><ul>
<li>_config.yml</li>
<li>languages</li>
<li>layout</li>
<li>scripts</li>
<li>source</li>
</ul>
<h3 id="config-yml"><a href="#config-yml" class="headerlink" title="_config.yml"></a>_config.yml</h3><p>主题的配置文件，修改主题的配置文件不需要重启服务器（修改网站根目录的那个_config.yml则需要重启才能生效)</p>
<h3 id="languages"><a href="#languages" class="headerlink" title="languages"></a>languages</h3><p>国际化设置，使用yml文件设置网站元素的翻译，具体参考<a href="https://hexo.io/docs/internationalization" target="_blank" rel="noopener">i18n</a></p>
<h3 id="layout"><a href="#layout" class="headerlink" title="layout"></a>layout</h3><p>网页、模板的设置。hexo默认使用Swig,不过也可以手动添加EJS,Haml,Jade或者Pug的支持。模板的写法参考 <a href="https://hexo.io/docs/templates" target="_blank" rel="noopener">Templates</a></p>
<h3 id="scripts"><a href="#scripts" class="headerlink" title="scripts"></a>scripts</h3><p>存放JavaScript代码的文件夹，Hexo会自动加载这里的javaScript文件。扩展功能参考 <a href="https://hexo.io/docs/plugins" target="_blank" rel="noopener">plugins</a></p>
<h3 id="source"><a href="#source" class="headerlink" title="source"></a>source</h3><p>顾名思义，这里是存放资源的。不属于页面模板的资源理论上都应该在此文件夹下面。</p>
<h2 id="模板"><a href="#模板" class="headerlink" title="模板"></a>模板</h2><h3 id="Layout-布局"><a href="#Layout-布局" class="headerlink" title="Layout 布局"></a>Layout 布局</h3><p>在主题文件夹下的layout中定义不同模板的页面的呈现方式，具体的模板文件定义了页面的<code>body</code>内容，而layout则定义了如何显示<code>body</code>。<br>比如在布局中这样写：<br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE html&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">body</span>&gt;</span><span class="tag">&lt;<span class="name">%-</span> <span class="attr">body</span> %&gt;</span><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure><br>系统默认的布局为layout布局，不过在post的头部可以通过定义来使用具体的布局。</p>
<h3 id="Partial-组件"><a href="#Partial-组件" class="headerlink" title="Partial 组件"></a>Partial 组件</h3><p>不同页面之间共享的内容可以写成组件来管理，比如<code>Header</code>,<code>Footer</code>,<code>Sidebar</code>。<br>引用组件时：<br><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">%-</span> <span class="attr">partial</span>('<span class="attr">partial</span>/<span class="attr">header</span>') %&gt;</span></span><br></pre></td></tr></table></figure></p>
<h2 id="变量"><a href="#变量" class="headerlink" title="变量"></a>变量</h2><h2 id="资源文件夹"><a href="#资源文件夹" class="headerlink" title="资源文件夹"></a>资源文件夹</h2><p>从<code>Hexo 3</code>开始，在<code>_config.yml</code>中启用<code>post_asset_folder=true</code>, 使用hexo的资源文件夹功能。<br>启用后，<code>_posts</code>里每生成一个post，会在相同路径下生成一个同名的文件夹作用该post的资源管理文件夹。</p>
<h3 id="引用资源文件夹中的资源"><a href="#引用资源文件夹中的资源" class="headerlink" title="引用资源文件夹中的资源"></a>引用资源文件夹中的资源</h3><p>启用了<code>asset_folder</code>后，可以使用以下几种命令，在文章中引用资源:<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#123;% asset_path name %&#125;</span><br><span class="line">&#123;% asset_img name title %&#125;</span><br><span class="line">&#123;% asset_link name title %&#125;</span><br></pre></td></tr></table></figure><br>在<code>name</code>的地方填写资源文件夹内的相对路径。</p>
<p>比如以下的文件结构：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">- sample.md</span><br><span class="line">- sample</span><br><span class="line">  - part1</span><br><span class="line">   - img.jpg</span><br></pre></td></tr></table></figure></p>
<p>在sample.md中引用img.jpg时的写法是:<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#123;% asset_img part1\img.jpg this is title %&#125;</span><br></pre></td></tr></table></figure></p>
<h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><p>使用资源文件夹与使用<code>markdown</code>的引用语法的最大不同就是：在网站首页或者归档处显示时，使用资源文件夹功能的引用可以被显示，而<code>markdown</code>语法引用的内容则无法被正确显示。</p>
<h3 id="样板"><a href="#样板" class="headerlink" title="样板"></a>样板</h3><a href="/2019/04/05/Hexo/1.JPG" class="gallery-item"><img src="/2019/04/05/Hexo/1.JPG" class="" title="i1"></a>
<h2 id="国际化-i18n"><a href="#国际化-i18n" class="headerlink" title="国际化 i18n"></a>国际化 i18n</h2><p><code>i18n</code> (Internationalization) 是指针对不同的国家、地域、语言环境，改变网站通用部分的显示语言。</p>
<h3 id="设置"><a href="#设置" class="headerlink" title="设置"></a>设置</h3><p>使用<code>YAML</code>或者<code>JSON</code>编写不同的语言文件，并统一放在主题文件夹<code>languages</code>中。</p>
<h3 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h3><p>使用<code>__</code>或者<code>_p</code>来取得对应语言的字符串。比如：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">menu：</span><br><span class="line">  titile: HOME</span><br><span class="line">  archive: ARCHIVE</span><br><span class="line">  search: SEARCH</span><br></pre></td></tr></table></figure><br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">menu：</span><br><span class="line">  titile: 首页</span><br><span class="line">  archive: 归档</span><br><span class="line">  search: 搜索</span><br></pre></td></tr></table></figure></p>
<p>在模板中使用目录时（例如语言是英文）：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;%&#x3D; __(&#39;menu.title&#39;) %&gt; &#x2F;&#x2F; HOME</span><br><span class="line">&lt;%&#x3D; __(&#39;menu.archive&#39;) %&gt; &#x2F;&#x2F; 归档</span><br><span class="line">&lt;%&#x3D; __(&#39;menu.search) %&gt; &#x2F;&#x2F; 搜索</span><br></pre></td></tr></table></figure></p>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>Hexo</tag>
        <tag>Gitpage</tag>
      </tags>
  </entry>
  <entry>
    <title>Embadded-JavaScript</title>
    <url>/2019/03/20/Embadded-JavaScript/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<h1 id="Ejs-文件是什么"><a href="#Ejs-文件是什么" class="headerlink" title="Ejs 文件是什么"></a>Ejs 文件是什么</h1><p>嵌入式JavaScript代码：Embadded JavaScript 是一种从JavaScript快速生成html的简易模板，详细说明可以阅读说明文档 <a href="https://ejs.bootcss.com" target="_blank" rel="noopener">Ejs 中文文档</a>。<br>Hexo网页使用ejs的时候，使用命令 <code>npm install hexo-renderer-ejs --save</code>， 添加ejs的支持就可以了。这里作为备忘录，简单记录一下标签的写法。</p>
<h2 id="标签"><a href="#标签" class="headerlink" title="标签"></a>标签</h2><ul>
<li>&lt;% ‘脚本’ 标签，用于流程控制，无输出。</li>
<li>&lt;%_ 删除其前面的空格符</li>
<li>&lt;%= 输出数据到模板（输出是转义 HTML 标签）</li>
<li>&lt;%- 输出非转义的数据到模板</li>
<li>&lt;%# 注释标签，不执行、不输出内容</li>
<li>&lt;%% 输出字符串 ‘&lt;%’</li>
<li>%&gt; 一般结束标签</li>
<li>-%&gt; 删除紧随其后的换行符</li>
<li>_%&gt; 将结束标签后面的空格符删除</li>
</ul>
<p>整理一下就是说，ejs代码就是写在html中的javascript，标签是<code>&lt;% %&gt;</code>,标签的开头和结尾有几中命令符可以使用，分别是</p>
<ol>
<li><code>_</code> 删除空格;</li>
<li><code>=</code>输出数据;</li>
<li><code>-</code> 在前：输出非转义数据（比如插入处理），在后：删除换行; </li>
<li><code>#</code> 注释; </li>
<li><code>%</code> 用于输出 <code>&lt;%</code></li>
</ol>
<h2 id="插入"><a href="#插入" class="headerlink" title="插入"></a>插入</h2><h2 id="自定义“转义符”-（官方文档写的是分隔符-delimiters，不是很理解）"><a href="#自定义“转义符”-（官方文档写的是分隔符-delimiters，不是很理解）" class="headerlink" title="自定义“转义符” （官方文档写的是分隔符-delimiters，不是很理解）"></a>自定义“转义符” （官方文档写的是分隔符-delimiters，不是很理解）</h2></div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>JavaScript</tag>
        <tag>未填之坑</tag>
        <tag>Ejs</tag>
      </tags>
  </entry>
  <entry>
    <title>jQuery 基本知识点备忘录</title>
    <url>/2019/03/19/jQuery-Memo/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<h2 id="document-ready"><a href="#document-ready" class="headerlink" title="$(document).ready()"></a>$(document).ready()</h2><p>页面载入需要首先准备DOM（Document Object Model),jQuery可以检测到页面的DOM是否准备完毕，<code>$(document).ready(function)</code>中的function，会等到页面的DOM准备完毕再运行。<br>类似的还有<code>$(window).on(&quot;load&quot;,funciton)</code>,这种写法下的function会在整个页面（并不仅仅是DOM）载入完毕时再执行。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">$( <span class="built_in">document</span> ).ready(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log( <span class="string">"document loaded"</span> );</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">$( <span class="built_in">window</span> ).on( <span class="string">"load"</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log( <span class="string">"window loaded"</span> );</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>将上面的代码插入页面的某个部件里执行，可以看到document很快就载入完成，等页面的所有其他元素都载入完成，<code>window loaded</code>才会被执行输出。</p>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>JavaScript</tag>
        <tag>jQuery</tag>
        <tag>未填之坑</tag>
      </tags>
  </entry>
  <entry>
    <title>LBL法还原魔方</title>
    <url>/2019/02/05/rubik-cube/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<blockquote>
<p>我写的东西，自己都看不懂</p>
</blockquote>
<h2 id="魔方的基本结构"><a href="#魔方的基本结构" class="headerlink" title="魔方的基本结构"></a>魔方的基本结构</h2><p>国际标准的魔方（Rubik’s Cube) 由黄蓝红绿橙白6个中心块，8个角，12个棱，组成。<br>魔方颜色的位置关系是：上黄下白，前蓝后绿，左橙右红。</p>
<h2 id="魔方入门级复原步骤-原参考链接"><a href="#魔方入门级复原步骤-原参考链接" class="headerlink" title="魔方入门级复原步骤 原参考链接"></a>魔方入门级复原步骤 <a href="http://www.mf100.org/beginner.htm" target="_blank" rel="noopener">原参考链接</a></h2><p>LBL(Layer By Layer)法，复原过程以上黄下白为例。</p>
<h3 id="第一层"><a href="#第一层" class="headerlink" title="第一层"></a>第一层</h3><p>目标：复原最下面一层：底面复位，层面看可以构成四个凸型</p>
<p>首先，不在意侧面颜色，将四个白色棱块移动到上方（白面朝上），转动上层，将白色棱块侧面颜色移动到对应的面，180旋转侧面，将白色棱块移动到底部。<br>然后，使用左手或者右手公式，将上方的角块移到下方对应的夹角处。如果颜色方向不符合，就使用同一公式多转几次。</p>
<h3 id="第二层"><a href="#第二层" class="headerlink" title="第二层"></a>第二层</h3><p>目标：复原中层的四个棱 - 完成两层</p>
<p>现在底面已经复原，侧面看四个小凸型也完成，使用左手公式-整体右转-右手公式（或者 右手公式-整体左转-左手公式）将上方的棱块移动到中层。</p>
<h3 id="第三层（顶面）"><a href="#第三层（顶面）" class="headerlink" title="第三层（顶面）"></a>第三层（顶面）</h3><p>目标：顶面复原</p>
<p>首先，使用拐角或者一字公式，将顶面复原成十字。<br>然后连续的使用左小鱼或者右小鱼，先转出左（右）小鱼型的顶面，再对应的解决掉，就完成了顶面。</p>
<h3 id="第三层（侧面）"><a href="#第三层（侧面）" class="headerlink" title="第三层（侧面）"></a>第三层（侧面）</h3><p>目标：完全复原</p>
<p>观察顶层侧面的颜色结构，如果发现有至少一对眼，就将有眼的一面放到左手，做一遍一字公式+拐角公式；如果没有眼，就做一遍拐角公式+一字公式；如果全是眼….就进行下一步。<br>（此时顶层的四个顶角应该都已经复原了）使用左小鱼-上层右转-右小鱼（或者右小鱼-生成左转-左小鱼），可以将面前的两个角块和左侧（右小鱼起手则是右侧）的棱块移动到一起，这样魔方就复原了。</p>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>Bubik&#39;s Cube</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript 两种常用的函数定义方法</title>
    <url>/2018/07/17/FunctionDefinition/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<p>JavaScript中定义函数，通常会遇到以下两种写法：</p>
<p>写法A<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">name1</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="comment">//...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>写法B<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> name2 = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<p>这两种写法有什么区别呢？很久很久以前,<a href="https://stackoverflow.com/questions/336859/var-functionname-function-vs-function-functionname" target="_blank" rel="noopener">stackoverflow</a>上面有人问了一样的问题。</p>
<h2 id="funciton的基本定义"><a href="#funciton的基本定义" class="headerlink" title="funciton的基本定义"></a>funciton的基本定义</h2><p>首先，在JavaScript中function的最基本定义方法是写法A，这种定义的内容在脚本被编译时就会执行，生成一个有名字的函数对象。</p>
<p>而写法B实际上是定义了一个无名的函数对象，又将这个对象赋给了一个有名字的变量。<br>这种写法的存在，方便了我们将函数定义为对象的属性：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> target = <span class="keyword">new</span> <span class="built_in">Object</span>();</span><br><span class="line">target.add = <span class="function"><span class="keyword">function</span>(<span class="params">a,b</span>)</span>&#123;</span><br><span class="line">  <span class="keyword">return</span> a+b;</span><br><span class="line">&#125;;</span><br><span class="line">target.add(<span class="number">1</span>,<span class="number">1</span>); <span class="comment">//2</span></span><br></pre></td></tr></table></figure></p>
<p>所以写法A定义的是一个完整的函数对象，而写法B定义的函数是没有名字的，<code>name2</code>是变量的名字，而不是函数的。</p>
<h2 id="扩展"><a href="#扩展" class="headerlink" title="扩展"></a>扩展</h2><p>其实还可以使用<code>new</code>来声明函数：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> name = <span class="keyword">new</span> <span class="built_in">Function</span>(<span class="string">"a"</span>, <span class="string">"b"</span>, <span class="string">"return a+b;"</span>);</span><br></pre></td></tr></table></figure><br>这里注意使用的是<code>Function</code>而不是<code>function</code>，这是具体的定义了一个<code>Function</code>类型的对象。速度慢，只有在特定情况下使用，比如想要让用户去控制函数内容的时候。</p>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>Immediately_Invoked_Function -- 立即调用函数的写法</title>
    <url>/2018/06/20/ImmediatelyInvokedFunctionException/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<h2 id="自执行-立即调用的函数表达式"><a href="#自执行-立即调用的函数表达式" class="headerlink" title="自执行-立即调用的函数表达式"></a>自执行-立即调用的函数表达式</h2><p>在某些情况下，我们不需要函数创建多个实例，也不需要对返回值做什么处理，便可以通过在函数声明的后面添加<code>()</code>来实现自执行。因为<code>foo</code>仅仅是<code>function(){}</code>这个表达式的一个引用，在函数表达时候后添加<code>()</code>便可以执行。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> foo = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="comment">//do something.</span></span><br><span class="line">&#125;();</span><br></pre></td></tr></table></figure><br>但如果我们像下面这么写，会报错：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="comment">//do somthing.</span></span><br><span class="line">&#125;();</span><br></pre></td></tr></table></figure>
<p>给括号里加入参数的话，可以不报错，但是函数不会执行，因为它会被解析成一个毫不相关的括号。<br>报错原因：解析代码的时候，function已经被识别为声明函数用，而不是一个函数表达式。<br>所以我们要做的就是把括号前面的内容解析成表达式，以下的写法都可以：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">(<span class="function"><span class="keyword">function</span> <span class="title">makeCounter</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">"invoked by ()"</span>);</span><br><span class="line">&#125;());</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> x = <span class="function"><span class="keyword">function</span> <span class="title">makeCounter</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">"invoked by var with ()"</span>);</span><br><span class="line">&#125;();</span><br><span class="line"></span><br><span class="line">!<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">"invoked by !"</span>);</span><br><span class="line">&#125;();</span><br><span class="line"></span><br><span class="line">~<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">"invoked by ~"</span>);</span><br><span class="line">&#125;();</span><br><span class="line"></span><br><span class="line">+<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">"invoked by +"</span>);</span><br><span class="line">&#125;();</span><br><span class="line"></span><br><span class="line">-<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">"invoked by -"</span>);</span><br><span class="line">&#125;();</span><br><span class="line"></span><br><span class="line"><span class="keyword">new</span> <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">"invoked by new"</span>);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">new</span> <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">"invoked by new with ()"</span>);</span><br><span class="line">&#125;();</span><br></pre></td></tr></table></figure></p>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>Java 中的NaN</title>
    <url>/2018/05/29/NaN/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<h2 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h2><p>在学习JavaScript时看到了Number类型中的NaN和Infinity，因为概念本身是通用的，所以趁机总结了一下Java中的NaN和Infinity。</p>
<h2 id="基本"><a href="#基本" class="headerlink" title="基本"></a>基本</h2><p>Java的Float型和Double型各有三个很特别的常量：NaN(非数），POSITIVE_INFINITY(正无穷)，NEGATIVE_INFINITY(负无穷)</p>
<p><strong>Float</strong><br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * A constant holding the positive infinity of type</span></span><br><span class="line"><span class="comment"> * &#123;<span class="doctag">@code</span> float&#125;. It is equal to the value returned by</span></span><br><span class="line"><span class="comment"> * &#123;<span class="doctag">@code</span> Float.intBitsToFloat(0x7f800000)&#125;.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">float</span> POSITIVE_INFINITY = <span class="number">1.0f</span> / <span class="number">0.0f</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * A constant holding the negative infinity of type</span></span><br><span class="line"><span class="comment"> * &#123;<span class="doctag">@code</span> float&#125;. It is equal to the value returned by</span></span><br><span class="line"><span class="comment"> * &#123;<span class="doctag">@code</span> Float.intBitsToFloat(0xff800000)&#125;.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">float</span> NEGATIVE_INFINITY = -<span class="number">1.0f</span> / <span class="number">0.0f</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * A constant holding a Not-a-Number (NaN) value of type</span></span><br><span class="line"><span class="comment"> * &#123;<span class="doctag">@code</span> float&#125;.  It is equivalent to the value returned by</span></span><br><span class="line"><span class="comment"> * &#123;<span class="doctag">@code</span> Float.intBitsToFloat(0x7fc00000)&#125;.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">float</span> NaN = <span class="number">0.0f</span> / <span class="number">0.0f</span>;</span><br></pre></td></tr></table></figure><br><strong>Double</strong><br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * A constant holding the positive infinity of type</span></span><br><span class="line"><span class="comment"> * &#123;<span class="doctag">@code</span> double&#125;. It is equal to the value returned by</span></span><br><span class="line"><span class="comment"> * &#123;<span class="doctag">@code</span> Double.longBitsToDouble(0x7ff0000000000000L)&#125;.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">double</span> POSITIVE_INFINITY = <span class="number">1.0</span> / <span class="number">0.0</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * A constant holding the negative infinity of type</span></span><br><span class="line"><span class="comment"> * &#123;<span class="doctag">@code</span> double&#125;. It is equal to the value returned by</span></span><br><span class="line"><span class="comment"> * &#123;<span class="doctag">@code</span> Double.longBitsToDouble(0xfff0000000000000L)&#125;.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">double</span> NEGATIVE_INFINITY = -<span class="number">1.0</span> / <span class="number">0.0</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * A constant holding a Not-a-Number (NaN) value of type</span></span><br><span class="line"><span class="comment"> * &#123;<span class="doctag">@code</span> double&#125;. It is equivalent to the value returned by</span></span><br><span class="line"><span class="comment"> * &#123;<span class="doctag">@code</span> Double.longBitsToDouble(0x7ff8000000000000L)&#125;.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">double</span> NaN = <span class="number">0.0</span>d / <span class="number">0.0</span>;</span><br></pre></td></tr></table></figure></p>
<h2 id="NaN"><a href="#NaN" class="headerlink" title="NaN"></a>NaN</h2><p>NaN是<code>Not a Number</code>的缩写，表示未定义或者不可表示的值，NaN有一个特性，那就是它与任何数都不相等，包括它自己。判断一个数是不是NaN，<strong>只能</strong>使用Float或者Double的<code>isNaN()</code>方法，那么这个方法是怎么判断的呢？我们看一看(查看的是Float版，Double除了参数类型都一样，下面二者没有区别的部分都直接使用Float的内容来说明)：<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Returns &#123;<span class="doctag">@code</span> true&#125; if the specified number is a</span></span><br><span class="line"><span class="comment"> * Not-a-Number (NaN) value, &#123;<span class="doctag">@code</span> false&#125; otherwise.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span>   v   the value to be tested.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@return</span>  &#123;<span class="doctag">@code</span> true&#125; if the argument is NaN;</span></span><br><span class="line"><span class="comment"> *          &#123;<span class="doctag">@code</span> false&#125; otherwise.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">boolean</span> <span class="title">isNaN</span><span class="params">(<span class="keyword">float</span> v)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (v != v);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>所以，跟自己不相等的数字，就是NaN.</p>
<h3 id="计算中产生的NaN"><a href="#计算中产生的NaN" class="headerlink" title="计算中产生的NaN"></a>计算中产生的NaN</h3><p>什么时候会产生NaN呢？首先，任何与NaN进行的运算，其结果都是NaN.比如下面的运算，不要去想太多，结果就是<code>NaN</code><br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span> </span>&#123;</span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">var</span> x = Float.NaN;</span><br><span class="line">		<span class="keyword">var</span> y = x - x + <span class="number">1</span>;</span><br><span class="line">		System.out.println(y);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>其次，从数学角度讲，无法进行的运算(<code>N/0</code>)或者会产生复数的运算(<code>sqrt(-N)</code>)，都会产生NaN。</p>
<p>(咕咕咕，后面的笔记被鸽了)</p>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript 基本知识备忘录</title>
    <url>/2018/04/26/JavaScript/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<h2 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h2><p>JavaScript使用的是ECMA(European Computer Manufacturers Association)的ECMAScript标准,也就是人们常说的ES6,ES7,ES8.而JavaScript本身是网景公司(Netscape)对ES标准的一种实现,JavaScript是该公司的注册商标.(这也是前阵子苹果下架了所有名称里带JavaScript的app的原因)</p>
<blockquote>
<p>JavaScript 是Brendan Eich用两周写出来的一种语言.所以有点设计缺陷也可以理解,比如<code>typeof null == &quot;object&quot;</code><br>由于浏览器确定了使用的JavaScript版本,所以写JavaScript程序时,要注意ES版本与浏览器的支持是否匹配.</p>
</blockquote>
<h2 id="基本"><a href="#基本" class="headerlink" title="基本"></a>基本</h2><h3 id="写在哪里"><a href="#写在哪里" class="headerlink" title="写在哪里"></a>写在哪里</h3><p>JavaScript的代码可以直接写在网页html里,也可以单独写在js文件中.<br>html里的JavaScript代码要写在 <code>&lt;scrpt&gt;</code> 标签内,标签的 <code>type</code> 属性不需要定义：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;head&gt;</span><br><span class="line">  &lt;script&gt;</span><br><span class="line">    alert(&#39;Hello, World!&#39;);</span><br><span class="line">  &lt;&#x2F;script&gt;:</span><br><span class="line">&lt;&#x2F;head&gt;</span><br><span class="line">&lt;body&gt;</span><br><span class="line">  ...</span><br><span class="line">&lt;&#x2F;body&gt;</span><br><span class="line">&lt;&#x2F;html&gt;</span><br></pre></td></tr></table></figure></p>
<p>我们也可以把<code>alert(&#39;Hello, World!&#39;)</code>单独写在一个js文件中,取个名字<code>hello.js</code>,然后把js文件的引用写入html的head中(其实写在body里也能跑)<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;html&gt;</span><br><span class="line">&lt;head&gt;</span><br><span class="line">  &lt;script src&#x3D;&quot;hello.js&quot;&gt;&lt;&#x2F;script&gt;</span><br><span class="line">&lt;&#x2F;head&gt;</span><br><span class="line">&lt;body&gt;</span><br><span class="line">  ...</span><br><span class="line">&lt;&#x2F;body&gt;</span><br><span class="line">&lt;&#x2F;html&gt;</span><br></pre></td></tr></table></figure></p>
<h3 id="简单语法"><a href="#简单语法" class="headerlink" title="简单语法"></a>简单语法</h3><p>JavaScript每个语句以<code>;</code>结束,JavaScript的引擎会自动添加,所以这里并不强制要求,不过因为自动添加的<code>;</code>有时会造成歧义,所以手动添加还是最保险的.</p>
<p>赋值语句<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> x = <span class="number">1</span>;</span><br></pre></td></tr></table></figure><br>条件判断<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span>( <span class="number">1</span> &gt; <span class="number">0</span> )&#123;</span><br><span class="line">    x = <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<blockquote>
<p>一般规范：4空格缩进,80字换行</p>
</blockquote>
<p>另外跟Java一样,JavaScript的注释写法也是<code>//</code>和<code>/* */</code>,JavaScript也对大小写敏感.</p>
<h3 id="变量"><a href="#变量" class="headerlink" title="变量"></a>变量</h3><ol>
<li>JavaScript是动态语言,变量的声明使用<strong>var</strong>,赋值使用<strong>=</strong>.  </li>
<li>使用var声明的变量其<a href="##heading-作用域">作用域</a>都在函数内，而不使用var声明的变量则是全局变量.  </li>
<li>变量名要求由大小写英文,数字,<code>$</code>和<code>_</code>组成,不可以用数字开头,也不可以使用JavaScript的关键字作为变量名.</li>
</ol>
<h2 id="数据类型"><a href="#数据类型" class="headerlink" title="数据类型"></a>数据类型</h2><h3 id="基本型"><a href="#基本型" class="headerlink" title="基本型"></a>基本型</h3><p>JavaScript有五种基本类型：</p>
<ul>
<li>number</li>
<li>string</li>
<li>boolean</li>
<li>undefined</li>
<li>null</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typeof</span> <span class="number">1</span>;<span class="comment">//"number"</span></span><br><span class="line"><span class="keyword">typeof</span> <span class="string">"abc"</span>;<span class="comment">//"string"</span></span><br><span class="line"><span class="keyword">typeof</span> <span class="literal">true</span>;<span class="comment">//"boolean"</span></span><br><span class="line"><span class="keyword">typeof</span> <span class="literal">undefined</span>;<span class="comment">//"undefined"</span></span><br><span class="line"><span class="keyword">typeof</span> <span class="literal">null</span>;<span class="comment">//"object"</span></span><br></pre></td></tr></table></figure>
<h4 id="数字型-Number"><a href="#数字型-Number" class="headerlink" title="数字型 Number"></a>数字型 Number</h4><p>JavaScript中数字型不区分整数和浮点数,正、负、整数、浮点数、NaN、Infinity都是Number型,可直接进行运算.</p>
<blockquote>
<p>NaN: Not a Number </p>
<p>Infinity: ∞ 无穷大</p>
</blockquote>
<h4 id="字符串"><a href="#字符串" class="headerlink" title="字符串"></a>字符串</h4><p>JavaScript中字符串可以用双引号<code>&quot;</code>,也可以用单引号<code>&#39;</code>来表示.</p>
<h4 id="布尔型"><a href="#布尔型" class="headerlink" title="布尔型"></a>布尔型</h4><p>JavaScript中的布尔型由true和false构成,以及 <strong>与,或,取反</strong>的运算都与Java相同.</p>
<blockquote>
<p>JavaScript中进行比较的时候,如果使用<code>==</code>,会自动转换类型再进行比较,不转换类型的比较则需使用三个等号<code>===</code>.<br><a href="/2018/04/26/JavaScript/equals.PNG" class="gallery-item"><img src="/2018/04/26/JavaScript/equals.PNG" class="" title="equals"></a></p>
<p>NaN与所有Number都不相等,包括它自己,判断NaN的时候只能使用<code>isNaN()</code>.<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">isNaN</span>(<span class="literal">NaN</span>) <span class="comment">//true</span></span><br><span class="line">&gt;<span class="comment">// function isNaN(a)&#123;</span></span><br><span class="line">&gt;<span class="comment">//     return a != a;</span></span><br><span class="line">&gt;<span class="comment">// &#125;</span></span><br></pre></td></tr></table></figure></p>
<p>JavaScript的浮点数在运算时会产生误差,直接进行比较会出现错误结果.这点在Java中也是一样，所以要进行精确比较的时候,不应该使用浮点型.<br>　<br><a href="/2018/04/26/JavaScript/float.PNG" class="gallery-item"><img src="/2018/04/26/JavaScript/float.PNG" class="" title="float"></a></p>
<p>可以通过检测误差绝对值来进行比较:<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Math</span>.abs(<span class="number">1</span>/<span class="number">3</span> - (<span class="number">1</span> - <span class="number">2</span>/<span class="number">3</span>)) &lt; <span class="number">0.000001</span>; \\<span class="literal">true</span></span><br></pre></td></tr></table></figure></p>
</blockquote>
<h4 id="空值和未定义"><a href="#空值和未定义" class="headerlink" title="空值和未定义"></a>空值和未定义</h4><p>JavaScript中类似于Java,也通过<strong>null</strong>来表示空值,空值与长度为零的字符串不同.JavScript同时还定义了一个表示未定义的常量<strong>undefined</strong>.一般在检测参数时可以用到,未输入的参数会被当做<strong>undefined</strong>处理.</p>
<h3 id="引用类型"><a href="#引用类型" class="headerlink" title="引用类型"></a>引用类型</h3><h4 id="数组-Array"><a href="#数组-Array" class="headerlink" title="数组 Array"></a>数组 Array</h4><p>JavaScript通过<code>[]</code>来表示数组,使用<code>,</code>来分隔元素.也可以用<code>new Array(1,2,3);</code>来定义数组.数组中的元素可以是任意的数据类型,通过索引<code>arr[index]</code>来访问.</p>
<h4 id="对象-Object"><a href="#对象-Object" class="headerlink" title="对象 Object"></a>对象 Object</h4><p>JavaScript的对象是由成对的<strong>key-value</strong>组成的无序集合.<strong>key</strong>都是字符串,<strong>value</strong>可以是任意数据类型.<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> person = &#123;</span><br><span class="line">  name: <span class="string">'Bob'</span>,</span><br><span class="line">  age: <span class="number">23</span>,</span><br><span class="line">  tags: [<span class="string">'person'</span>, <span class="string">'young'</span>],</span><br><span class="line">  married: <span class="literal">false</span>,</span><br><span class="line">  code: <span class="literal">null</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>通过以下格式获取对象的属性：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">person.name;<span class="comment">// Bob</span></span><br><span class="line">person.age; <span class="comment">// 23</span></span><br></pre></td></tr></table></figure></p>
<h2 id="作用域"><a href="#作用域" class="headerlink" title="作用域"></a>作用域</h2><p>传统的编程语言,作用域一般是块级,也就是一个<code>{}</code>内是一个作用域,所以在<code>if else while for</code>的范围内定义的局部变量不会影响到外面.而JavaScript的作用域是函数级,一个<code>function</code>为一个作用域,在函数内部定义的变量,不受函数内部语法块的影响.<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> x = <span class="number">1</span>; <span class="comment">// 1</span></span><br><span class="line"><span class="keyword">if</span>(<span class="literal">true</span>)&#123;</span><br><span class="line">  <span class="keyword">var</span> x = <span class="number">2</span>; <span class="comment">//2</span></span><br><span class="line">&#125;</span><br><span class="line">condole.log(x); <span class="comment">//2</span></span><br></pre></td></tr></table></figure><br>如果一定要实现块作用域的话,便需要插入函数来起到划分的作用<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> x = <span class="number">1</span>; <span class="comment">//1</span></span><br><span class="line"><span class="keyword">if</span>(<span class="literal">true</span>)&#123;</span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">print</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="keyword">var</span> x = <span class="number">2</span>;<span class="comment">//2</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(x);<span class="comment">//1</span></span><br></pre></td></tr></table></figure></p>
<h4 id="函数作用域提升"><a href="#函数作用域提升" class="headerlink" title="函数作用域提升"></a>函数作用域提升</h4><p>函数声明分为：声明式 和 变量式</p>
<p>声明式会自动将声明放在函数最前面,并执行赋值的内容.<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">name</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>所以下面的函数没有问题<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">test(<span class="string">"test"</span>);</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">test</span>(<span class="params">arg</span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(arg); <span class="comment">//test</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>变量式会将声明提升到函数最前,而后再赋值<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> name = funciton()&#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>所以下面的代码中,函数被声明之后,还没有赋值,便被执行,会报错.<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">baz(<span class="string">"baz"</span>); <span class="comment">//baz is not a function</span></span><br><span class="line"><span class="keyword">var</span> baz = <span class="function"><span class="keyword">function</span>(<span class="params">arg</span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(arg);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><br>需要注意的是,变量式声明中的函数,是不会被提升作用域的<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> baz = <span class="function"><span class="keyword">function</span> <span class="title">spam</span>(<span class="params">arg</span>)</span>&#123;</span><br><span class="line">  <span class="keyword">if</span>(arg &lt; <span class="number">5</span>)&#123;</span><br><span class="line">    spam(arg + <span class="number">1</span>);<span class="comment">//只在函数的作用域内有效</span></span><br><span class="line">  &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(arg);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line">baz(<span class="number">1</span>); <span class="comment">//5</span></span><br><span class="line">spam(<span class="number">1</span>);<span class="comment">//spam is not defined</span></span><br></pre></td></tr></table></figure></p>
<blockquote>
<blockquote>
<p>使用var声明的变量或者函数,要放到其作用域的顶端.</p>
</blockquote>
</blockquote>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>IDE使用备忘录</title>
    <url>/2017/12/22/IDE_memo/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<blockquote>
<p>“这里应该怎么设置来着？ ”</p>
</blockquote>
<h2 id="Eclipse"><a href="#Eclipse" class="headerlink" title="Eclipse"></a>Eclipse</h2><h3 id="Web-Application-Library添加本地jar包"><a href="#Web-Application-Library添加本地jar包" class="headerlink" title="Web Application Library添加本地jar包"></a>Web Application Library添加本地jar包</h3><p>在项目上右键 -&gt; Deployment Assembly -&gt; add -&gt; project.</p>
<h3 id="显示-隐藏-空白符号"><a href="#显示-隐藏-空白符号" class="headerlink" title="显示/隐藏 空白符号"></a>显示/隐藏 空白符号</h3><p>在 <code>Windows/Preferences/General/Editors/Text Editors</code> 中<br>通过勾选 <code>Show whitespace characters</code> 来设置是否显示空白符号<br>点击右侧的<code>configure visibility</code>，还可以进行具体(比如换行符)的设置.</p>

<p>隐藏状态<br></p>
<p>显示状态<br></p>
<h2 id="Intellj-IDEA"><a href="#Intellj-IDEA" class="headerlink" title="Intellj IDEA"></a>Intellj IDEA</h2><h3 id="propertity文件相关"><a href="#propertity文件相关" class="headerlink" title="propertity文件相关"></a>propertity文件相关</h3><h4 id="显示文字而不是ASCII编码"><a href="#显示文字而不是ASCII编码" class="headerlink" title="显示文字而不是ASCII编码"></a>显示文字而不是ASCII编码</h4><p>直接打开propertity文件的话，UTF-8编码会直接显示ASCII编码而不是文字，可以在设置里修改：<br><code>File -&gt; Setting -&gt; Editor -&gt; File Encoding</code>  </p>
<p>大约中间靠下的位置，勾选<br><code>transparent native to ascii conversion</code><br>就可以显示文字而不是直接显示ASCII编码了。</p>
<h4 id="修改ASCII编码的默认大小写"><a href="#修改ASCII编码的默认大小写" class="headerlink" title="修改ASCII编码的默认大小写"></a>修改ASCII编码的默认大小写</h4><p>使用ASCII码的property文件在IDEA中编辑会默认使用大写字母(例：使用 <strong>\u00E3</strong> 而不是 <strong>\u00e3</strong>)<br>如果想要保存成小写字母，需要在IntellJ的根目录下找到 <strong>idea.properties</strong>，在其中添加：<br><code>idea.native2ascii.lowercase=true</code></p>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>Eclipse</tag>
        <tag>IntellJ IDEA</tag>
      </tags>
  </entry>
  <entry>
    <title>Markdown 语法笔记</title>
    <url>/2017/11/18/Markdown/</url>
    <content><![CDATA[<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/css/lightgallery.min.css" /><div class=".article-gallery"<blockquote>
<p>内容整理自 <a href="https://www.markdowntutorial.com/" target="_blank" rel="noopener">markdowntutorial</a>.</p>
</blockquote>
<h2 id="粗体和斜体"><a href="#粗体和斜体" class="headerlink" title="粗体和斜体"></a>粗体和斜体</h2><h3 id="斜体"><a href="#斜体" class="headerlink" title="斜体"></a><em>斜体</em></h3><p><code>_Italic_</code><br>输入内容的前后添加下划线，使字体变为斜体。</p>
<h3 id="粗体"><a href="#粗体" class="headerlink" title="粗体"></a><strong>粗体</strong></h3><p><code>**Bold**</code><br>输入内容的前后添加星号，使字体变为粗体。</p>
<h3 id="删除线"><a href="#删除线" class="headerlink" title="删除线"></a><del>删除线</del></h3><p><code>~~删除线~~</code><br>输入内容的前后加波浪线，在文字上添加删除划线。</p>
<h2 id="标题"><a href="#标题" class="headerlink" title="标题"></a>标题</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># Header one</span><br><span class="line">## Header two </span><br><span class="line">### Header three</span><br><span class="line">#### Header four</span><br><span class="line">##### Header five</span><br><span class="line">###### Header six</span><br></pre></td></tr></table></figure>
<h2 id="链接"><a href="#链接" class="headerlink" title="链接"></a>链接</h2><h3 id="完整的链接"><a href="#完整的链接" class="headerlink" title="完整的链接"></a>完整的链接</h3><p><code>[XSUN的主页](https://xsun4231.github.io/)</code><br>效果： <a href="https://xsun4231.github.io/">XSUN的主页</a><br>链接的书写格式： 方括号+圆括号的组合 方括号内为页面显示内容，圆括号内为链接URL</p>
<h3 id="参照"><a href="#参照" class="headerlink" title="参照"></a>参照</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[从这里进入][url名]</span><br><span class="line">[从这里也可以][url名]</span><br><span class="line">[这里也行][url名]</span><br><span class="line"></span><br><span class="line">[url名]: https:&#x2F;&#x2F;xsun4231.github.io&#x2F;</span><br></pre></td></tr></table></figure>
<p><a href="https://xsun4231.github.io/">从这里进入</a><br><a href="https://xsun4231.github.io/">从这里也可以</a><br><a href="https://xsun4231.github.io/">这里也行</a></p>
<p>设置参照与设置链接方法基本相同，只是在链接URL前面单独设置一个标签，标签可反复使用。</p>
<h2 id="插入（图片）"><a href="#插入（图片）" class="headerlink" title="插入（图片）"></a>插入（图片）</h2><p><code>![这是一张很可爱的图片](https://octodex.github.com/images/spidertocat.png)</code></p>
<p><a href="https://octodex.github.com/images/spidertocat.png" title="这是一张很可爱的图片" class="gallery-item"><img src="https://octodex.github.com/images/spidertocat.png" alt="这是一张很可爱的图片"></a></p>
<p>图片的是指方法与链接的设置方法基本相同，只是在方括号前面添加叹号。</p>
<blockquote>
<p>图片的链接同样可以设置为参照</p>
</blockquote>
<h2 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h2><p><code>&gt; 孟子曰：孔子说的对。</code></p>
<blockquote>
<p>孟子曰：孔子说的对。</p>
</blockquote>
<p>在文字块(以换行结束)前添加大于号(右尖括号)，则将整个文字块设置成引用。<br>将多个段落设置成一个引用块时，在每一个新段落(换行后)添加大于号。</p>
<h2 id="列表"><a href="#列表" class="headerlink" title="列表"></a>列表</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">* milk</span><br><span class="line">* egg</span><br><span class="line">* apple</span><br><span class="line">    1. fuji</span><br><span class="line">    2. fuji2</span><br><span class="line">    3. fuji3</span><br><span class="line">* banana</span><br><span class="line">* people</span><br><span class="line">    1. AAA</span><br><span class="line">    1. BBB</span><br><span class="line">    1. CCC</span><br></pre></td></tr></table></figure>
<ul>
<li>milk</li>
<li>egg</li>
<li>apple<ol>
<li>fuji</li>
<li>fuji2</li>
<li>fuji3</li>
</ol>
</li>
<li>banana<ol>
<li>AAA</li>
<li>BBB</li>
<li>CCC</li>
</ol>
</li>
</ul>
<p>列表用星号来标记，在星号前添加缩进来增加层次，使用数字代替星号可以将标签变为数字。</p>
<h2 id="段落"><a href="#段落" class="headerlink" title="段落"></a>段落</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">第一段</span><br><span class="line">第二段</span><br><span class="line">第三段</span><br></pre></td></tr></table></figure>
<p>代码块外的单一换行不会被识别，可以使用多个换行来制造强制换行，不过会造成很大的间距。</p>
<p><strong>第一段</strong><br><strong>第二段</strong></p>
<p><strong>第三段</strong></p>
<p>标准的换行是在行位添加两个空格：</p>
<p><strong>第一段</strong><br><strong>第二段</strong><br><strong>第三段</strong></p>
<h2 id="代码块"><a href="#代码块" class="headerlink" title="代码块"></a>代码块</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@requires_authorization</span><br><span class="line">class SomeClass:</span><br><span class="line">    pass</span><br><span class="line"></span><br><span class="line">if __name__ &#x3D;&#x3D; &#39;__main__&#39;:</span><br><span class="line">    # A comment</span><br><span class="line">    print &#39;hello world&#39;</span><br></pre></td></tr></table></figure>
<p><strong>` 是键盘左上角Esc下面那个，不是单引号’</strong><br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">三个&#96; </span><br><span class="line">@requires_authorization</span><br><span class="line">class SomeClass:</span><br><span class="line">    pass</span><br><span class="line"></span><br><span class="line">if __name__ &#x3D;&#x3D; &#39;__main__&#39;:</span><br><span class="line">    # A comment</span><br><span class="line">    print &#39;hello world&#39;</span><br><span class="line"></span><br><span class="line">三个&#96;</span><br></pre></td></tr></table></figure></p>
<h2 id="图表"><a href="#图表" class="headerlink" title="图表"></a>图表</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">| ID     | NAME | AGE   |</span><br><span class="line">| :---- | ----: | :---: |</span><br><span class="line">| 00001 | AAAA |  5    |</span><br><span class="line">| 002 | BB |  12   |</span><br><span class="line">| 03 | CCC |  9  |</span><br></pre></td></tr></table></figure>
<div class="table-container">
<table>
<thead>
<tr>
<th style="text-align:left">ID</th>
<th style="text-align:right">NAME</th>
<th style="text-align:center">AGE</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">00001</td>
<td style="text-align:right">AAAA</td>
<td style="text-align:center">5</td>
</tr>
<tr>
<td style="text-align:left">002</td>
<td style="text-align:right">BB</td>
<td style="text-align:center">12</td>
</tr>
<tr>
<td style="text-align:left">03</td>
<td style="text-align:right">CCC</td>
<td style="text-align:center">9</td>
</tr>
</tbody>
</table>
</div>
</div><script src="https://cdn.jsdelivr.net/lightgallery.js/1.0.1/js/lightgallery.min.js"></script><script>if (typeof lightGallery !== 'undefined') {
        var options = {
            selector: '.gallery-item'
        };
        lightGallery(document.getElementsByClassName('.article-gallery')[0], options);
        }</script>]]></content>
      <tags>
        <tag>Markdown</tag>
      </tags>
  </entry>
</search>
